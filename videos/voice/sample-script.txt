Orleans.Lattice is a platform for building durable, distributed state on Microsoft Orleans. At its centre is a sorted, conflict-free key-value store that lives inside your own cluster. Every write reaches the write-ahead log before it is acknowledged. Replicas merge through CRDTs, so they converge without locks and without consensus. The code that reads and writes your data on one laptop is the same code that runs across three regions.
