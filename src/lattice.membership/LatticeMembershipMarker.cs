namespace Orleans.Lattice.Membership;

/// <summary>
/// Internal placeholder that keeps the (otherwise empty) scaffolding assembly
/// non-empty. It carries no behaviour and exists only so the package builds,
/// packs, and gives later membership features a compiling home. It will be
/// removed once real types populate the namespace.
/// </summary>
internal sealed class LatticeMembershipMarker;
