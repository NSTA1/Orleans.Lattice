# Orleans.Lattice Roadmap

Potential improvements and new features.

---

## ✅ Completed

### API

- [x] **F-001 - Range Delete (`DeleteRangeAsync`)**: Delete all keys within a lexicographic range (`startInclusive`, `endExclusive`) in a single call by walking the leaf chain and tombstoning matching entries in bulk.
- [x] **F-002 - `CountAsync` / `CountPerShardAsync`**: Return the number of live keys without streaming them across the wire. A per-shard variant aids diagnostics and load-balancing analysis.
- [x] **F-003 - `GetOrSetAsync` (conditional write)**: Set a key only if it does not already exist, avoiding a read-then-write roundtrip. The leaf grain short-circuits when a live value is present.
- [x] **F-004 - Typed value helpers**: A thin generic wrapper `ILattice<T>` and extension methods that accept serializer/deserializer delegates (or default to `System.Text.Json`) to eliminate per-caller `byte[]` boilerplate.
- [x] **F-005 - `EntriesAsync` (key + value scan)**: Stream `KeyValuePair<string, byte[]>` in sorted order, complementing the existing `KeysAsync`. Useful for exports, migrations, and analytics without a separate `GetAsync` per key.
- [x] **F-016 - TTL on `SetAsync`**: New `ILattice.SetAsync(string key, byte[] value, TimeSpan ttl, CancellationToken)` overload (with matching typed `SetAsync<T>` overloads on `TypedLatticeExtensions`) writes an entry that expires after `ttl`. `LwwValue<T>` gains an `[Id(3)] ExpiresAtTicks` field (defaults to `0` - no expiry; wire-compatible with existing persisted state) and an `IsExpired(long nowUtcTicks)` helper. The TTL is resolved to an absolute UTC `DateTimeOffset.UtcNow.Add(ttl).UtcTicks` on the silo handling the call, so per-entry lifetimes are not shifted by client-clock skew. Expired entries are hidden from every read path - `GetAsync`, `GetWithVersionAsync`, `ExistsAsync`, `GetManyAsync`, `GetOrSetAsync`, `SetIfVersionAsync`, `CountAsync`, `KeysAsync`, `EntriesAsync`, `GetLiveEntriesAsync`, and `DeleteRangeAsync` - and reaped by the existing `CompactTombstonesAsync` alongside tombstones past the configured `LatticeOptions.TombstoneGracePeriod`. The grace retention protects against a stale-clock peer resurrecting an expired entry via CRDT merge: replication paths (`GetDeltaSinceAsync`, `MergeEntriesAsync`, `MergeManyAsync`) deliberately keep expired entries visible so they propagate correctly and win LWW by timestamp. `LeafCacheGrain` also filters expired on read. `SetAsync` throws `ArgumentOutOfRangeException` when `ttl <= TimeSpan.Zero`. **TTL metadata is preserved end-to-end across every lifecycle path**: shard-split shadow-forwards (`ForwardLocalWriteToShadowIfNeededAsync` reads the raw `LwwValue` including `ExpiresAtTicks` before merging into the target shard); saga compensation rollback (`AtomicWriteGrain.PrepareAsync` resolves routing via the public-internal `ILattice.GetRoutingAsync` hook and then captures the pre-saga `LwwValue` by calling `IShardRootGrain.GetRawEntryAsync` directly on the guarded internal interface - the raw `LwwEntry` DTO never crosses the public `ILattice` surface - and `CompensatePhaseAsync` restores TTL'd entries through the TTL-aware `SetAsync(key, value, TimeSpan)` overload with `ttl = expiresAtTicks - now`; past-due entries are equivalent to "absent" and are tombstoned); and tree snapshot / restore (`TreeSnapshotGrain.CopyShardAsync` drains via a new raw `IBPlusLeafGrain.GetLiveRawEntriesAsync` and bulk-loads via `IShardRootGrain.BulkLoadRawAsync`, both of which carry the source HLC and `ExpiresAtTicks` through verbatim). The raw / bulk-load paths exchange a new `LwwEntry` DTO (alias `ol.lwe`, `[EditorBrowsable(Never)]`) that lives exclusively on guarded internal grain interfaces (`IBPlusLeafGrain`, `IShardRootGrain`) - it is never exposed on `ILattice`. Its fields are stored flat rather than embedding `LwwValue<byte[]>` as a property, because the Orleans type-alias encoder has a codec-generation race with `Task<LwwValue<byte[]>?>` grain signatures that produces malformed alias strings under concurrent cold activation.
- [x] **F-017 - Compare-and-swap (CAS)**: Optimistic concurrency via `SetIfVersionAsync(key, value, expectedVersion)` - the write succeeds only if the current entry's HLC matches, enabling safe read-modify-write patterns without distributed locks.
- [x] **F-026 - Operation status queries**: Expose `IsMergeCompleteAsync()`, `IsSnapshotCompleteAsync()`, and `IsResizeCompleteAsync()` on `ILattice` so callers can poll long-running maintenance operations without reaching into coordination grains. Returns `true` vacuously when no operation has ever been initiated; uses existing reminder-based crash recovery for fire-and-forget completion.

### Performance & Scalability

- [x] **F-006 - Leaf-side continuation filtering for `EntriesAsync`**: Pass the continuation token down to the leaf grain as an `afterExclusive` parameter so it filters entries at the source, avoiding unnecessary `byte[]` serialization across the grain boundary during forward pagination.
- [x] **F-007 - Leaf-side continuation filtering for `KeysAsync`**: Apply the same `afterExclusive` leaf-side filtering to `GetSortedKeysBatchAsync`, eliminating shard-level skip loops for key-only scans.
- [x] **F-008 - Reverse-scan leaf-side filtering**: Add a `beforeExclusive` parameter to leaf `GetKeysAsync` / `GetEntriesAsync` so reverse pagination also filters at the leaf, avoiding unnecessary serialization in `GetSortedKeysBatchReverseAsync` / `GetSortedEntriesBatchReverseAsync`.
- [x] **F-009 - Parallel shard pre-fetch for `KeysAsync`**: Double-buffer the k-way merge by pre-fetching the next page from each shard in parallel, hiding per-shard latency during ordered key scans. Controlled via `LatticeOptions.PrefetchKeysScan` or the per-call `prefetch` parameter.
- [x] **F-024 - Parallel shard pre-fetch for `EntriesAsync`** *(required F-009 ✓)*: Extends the F-009 double-buffering strategy to `EntriesAsync`, hiding per-shard grain-call latency during ordered entry scans. Controlled via `LatticeOptions.PrefetchEntriesScan` (new, default `false`) or an optional `prefetch` parameter on `ILattice.EntriesAsync` (and the typed `EntriesAsync<T>` extensions). Gated separately from `PrefetchKeysScan` because entry pages carry `byte[]` values and increase in-flight memory by `shardCount × KeysPageSize × avgValueSize` - callers opt in only when the memory trade-off is acceptable. Ordering, completeness, and scan-reconciliation semantics are unchanged; the prefetch kick-off mirrors `ShardCursor.MoveNextAsync` so `MovedAwaySlots` observed on a pre-fetched page are still consumed on the next loop iteration before any further dequeue. Regression tests: 8 `EntriesPrefetchTests` integration cases (sorted, reverse, range, reverse range, forced pagination, `prefetch: false` override, empty tree, prefetch-vs-non-prefetch parity) and one typed-extension forwarding test.
- [x] **F-013 - Internal shard hotness counters**: Volatile in-memory counters (`reads`, `writes`, `countersSince`) on `ShardRootGrain`, incremented at zero persistence cost. Exposed via `GetHotnessAsync()` returning a `ShardHotness` struct so coordinators can poll all shards in parallel. Counters reset on grain deactivation.
- [x] **F-028 - Shard map indirection**: Replaced fixed `XxHash32 % shardCount` routing with a persistent `ShardMap` mapping virtual shard indices to physical `ShardRootGrain` identities over a large fixed virtual slot space (4 096 slots). `LatticeGrain` caches the map in memory and invalidates on topology changes.
- [x] **F-030 - Route `BulkLoadAsync` through the shard map**: The streaming `BulkLoadAsync` overload resolves the per-tree `ShardMap` up-front via `ILattice.GetRoutingAsync()` and routes each entry through `ShardMap.Resolve`. The legacy `int shardCount` overload is preserved but marked `[Obsolete]`.
- [x] **F-011 - Adaptive shard splitting** *(required F-013 ✓, F-028 ✓, F-030 ✓)*: Fully-online shadow-write split: source shard continues serving reads/writes; concurrent writes to migrating slots are mirrored via `MergeManyAsync`; after a background drain, the `ShardMap` is swapped and the source enters a reject phase emitting `StaleShardRoutingException`. `HotShardMonitorGrain` triggers splits autonomically when ops/sec exceeds `HotShardOpsPerSecondThreshold` (default 200 ops/s). Because `HotShardMonitorGrain` is keyed per-tree, `MaxConcurrentAutoSplits` (default 2) bounds concurrent splits **per tree**; in a multi-tree cluster each tree has its own independent cap. Scans use per-slot reconciliation with a `HashSet<string>` deduplicator, bounded by `LatticeOptions.MaxScanRetries` (default 3), so the complete live key set is returned with no missing or phantom entries. Strict lexicographic ordering of `KeysAsync` / `EntriesAsync` output is preserved across mid-scan splits via **F-032** (in-line reconciliation-cursor injection into the k-way merge).
- [x] **F-032 - Scan ordering preservation under topology change** *(required F-011 ✓)*: `KeysAsync` and `EntriesAsync` preserve strict lexicographic (or reverse) output ordering even when a shard split commits mid-scan. Before each priority-queue dequeue, the orchestrator checks whether any live shard cursor has reported new `MovedAwaySlots` since the last reconciliation step; if so, it drains the affected virtual slots from their current owners into an in-memory buffer, sorts it with the same comparer, and injects it as an additional cursor into the same k-way merge priority queue. A post-drain final-stability check handles splits that commit after all live cursors have finished. Reconciliation is bounded by `LatticeOptions.MaxScanRetries` (default 3); the per-call `HashSet<string>` continues to suppress duplicates across pre- and post-swap views. `CountAsync` is unaffected (it reduces rather than streams). A complete stateful-cursor design for long-running exports remains tracked by F-033.
- [x] **F-027 - Leaf-grouped merge routing**: `ShardRootGrain.MergeManyAsync` groups entries by target leaf before issuing grain calls, reducing tree traversals from O(entries) to O(leaves) and collapsing multiple `WriteStateAsync` calls per leaf into one. Root-is-leaf trees fast-path the whole batch through a single grain call. Per-group re-traversal preserves correctness under mid-batch splits - distinct groups target distinct leaves, so a split produced by one group cannot re-route another group's keys (internal splits create sibling internsals but preserve child `GrainId`s; leaf splits only affect the one leaf being written to). Per-group retry on transient `OrleansException` / `TimeoutException` / `IOException` is retained; the leaf's `MergeManyAsync` is LWW-idempotent so replay is safe. No interface or wire-format changes - every caller (`ShardRootGrain`, `TreeMergeGrain`, `TreeShardSplitGrain` shadow-forward and drain, continuous merge) benefits transparently. Regression tests: `ShardRootGrainMergeManyTests` covering empty batch, root-is-leaf fast path, multi-leaf grouping, single-leaf fan-in, transient-exception retry, and non-transient propagation.

### Reliability

- [x] **F-020 - Merge trees (`MergeAsync`)**: Merge all entries from a source tree into the current tree using LWW semantics. Useful for combining snapshots, migrating data, or rejoining forked datasets.
- [x] **F-031 - Atomic multi-key writes (saga)** *(covers audit #6 - `SetMany` atomicity)*: `ILattice.SetManyAtomicAsync` routes a batch of key-value pairs through a dedicated `IAtomicWriteGrain` saga coordinator (keyed per `{treeId}/{operationId}`). The saga reads pre-saga values, applies writes sequentially, and - on any failure - compensates already-committed entries by rewriting their pre-saga values (or tombstoning previously-absent keys) with fresh HLC ticks, relying on LWW to win over the partial write. A keepalive reminder registered at saga start drives reminder-based crash recovery: on reactivation the grain consults its persisted phase (`Prepare` / `Execute` / `Compensate` / `Completed`) and resumes. Duplicate keys and null values fail fast with `ArgumentException`; post-compensation failures surface as `InvalidOperationException` with the original failure's message preserved. `SetManyAsync` (the non-atomic, parallel fan-out path) is retained and now documents its non-atomicity. Readers may observe a partial-visibility window between the first and last committed write - inherent to the saga pattern and documented in `docs/api.md`.
- [x] **F-033 - Stateful cursor / iterator grain** *(covers audit #10 `DeleteRange` pagination and #15 scan fallback; completes scan liveness story started by F-032)*: `ILatticeCursorGrain` checkpoints scan progress server-side after every page so multi-minute exports and resumable range deletes survive silo failover, client restarts, and topology changes. Exposed via `ILattice.OpenKeyCursorAsync` / `OpenEntryCursorAsync` / `OpenDeleteRangeCursorAsync` / `NextKeysAsync` / `NextEntriesAsync` / `DeleteRangeStepAsync` / `CloseCursorAsync`; the cursor grain is keyed `{treeId}/{cursorId}` and persists `LastYieldedKey` plus phase so a new activation resumes exactly where it left off. Each step is routed through the normal `ILattice.KeysAsync` / `EntriesAsync` / `DeleteRangeAsync` path, inheriting F-032 ordering preservation under concurrent shard splits. **Self-cleaning:** the grain registers a sliding idle-TTL reminder (`cursor-ttl`) refreshed on every call; if it fires without activity, the grain clears state, unregisters the reminder, and deactivates. Interval is configured by `LatticeOptions.CursorIdleTtl` (default 48h, clamped to a 1-minute floor, disabled with `Timeout.InfiniteTimeSpan`). The same retention-reminder pattern was layered onto `AtomicWriteGrain` (F-031) via `LatticeOptions.AtomicWriteRetention` (default 48h) so completed saga state does not leak indefinitely. Public DTOs (`LatticeCursorSpec`, `LatticeCursorKeysPage`, `LatticeCursorEntriesPage`, `LatticeCursorDeleteProgress`, `LatticeCursorKind`) are visible to IntelliSense as first-class public API; only the internal `ILatticeCursorGrain` is marked `[EditorBrowsable(Never)]`. Stateless `KeysAsync` / `EntriesAsync` / `DeleteRangeAsync` overloads are retained for short scans.
- [x] **F-034 - Resilient client-side scan iterators** *(complements F-033 for in-process scans)*: `ScanKeysAsync` and `ScanEntriesAsync` extension methods on `ILattice` (plus the typed `ScanEntriesAsync<T>` overloads on `TypedLatticeExtensions`) transparently recover from `Orleans.Runtime.EnumerationAbortedException` when the `LatticeGrain` enumerator is reclaimed mid-scan (idle expiry, cold start, silo failover, scale-down). The wrapper tracks the last yielded key and deterministically resumes by tightening the range bound - forward scans reopen with `startInclusive = lastKey + "\u0000"`, reverse scans reopen with `endExclusive = lastKey` - so callers see a single uninterrupted `IAsyncEnumerable<T>` with no duplicates, no gaps, and original `CancellationToken` semantics preserved. Retry budget is configurable via `LatticeOptions.ScanReconnectAttempts` (default 8) with bounded linear backoff; exhaustion rethrows the last `EnumerationAbortedException` verbatim. Callers hardening multi-minute exports against *client restart* continue to use F-033's stateful `ILatticeCursorGrain`; F-034 covers the far more common case of a single in-process scan that outlives one grain activation. Interacts cleanly with F-032's in-flight reconciliation cursors: the reopened enumerator inherits the current `ShardMap`, so a mid-scan shard split that commits during a reconnect is picked up on the next attempt without special casing. The HelloWorld sample and the chaos integration suites (`ChaosIntegrationTests`, `ChaosReshardIntegrationTests`, `ChaosResizeIntegrationTests`, `ChaosWithFaultsIntegrationTests`) all consume the resilient wrappers instead of hand-rolled retry loops. Regression tests: 13 `ResilientScanExtensionsTests` covering single-abort resume, multi-abort resume without duplicates, budget exhaustion, reverse resume, non-abort exception propagation, cancellation, null-argument guards, typed deserializer round-trip, and default-serializer forwarding. Documented in [`docs/api.md`](../../docs/lattice/api.md#scan-reliability).
- [x] **F-019 - Online (non-blocking) resize** *(required F-028 ✓)*: Fully online structural reshaping of a live tree with no read/write downtime. Delivered as three composed PRs that together form a cohesive capability - grow the physical shard count, re-tune the B+ fan-out, and lock the structural parameters into a drift-proof source of truth:

  - **F-019a - Online grow-only reshard** ([#61](https://github.com/NSTA1/Orleans.Lattice/pull/61)). `ILattice.ReshardAsync(int newShardCount, CancellationToken)` grows a tree's physical shard count online via `ITreeReshardGrain`, dispatching up to `LatticeOptions.MaxConcurrentMigrations` concurrent per-shard splits (each using its own shadow-write + swap + reject phases). Grow-only; idempotent on same target; crash-safe via reminder-anchored coordinator. Progress via `IsReshardCompleteAsync`. Interlocked against `HotShardMonitorGrain`.

  - **F-019b - Online `ResizeAsync` via shadow-forwarding** ([#62](https://github.com/NSTA1/Orleans.Lattice/pull/62)). `ILattice.ResizeAsync(int newMaxLeafKeys, int newMaxInternalChildren)` runs online. A new `ShardRootGrain` shadow-forwarding primitive (`BeginShadowForwardAsync` / `MarkDrainedAsync` / `EnterRejectingAsync` / `ClearShadowForwardAsync`, LWW-commutative by construction) lets `TreeSnapshotGrain` run a strictly-consistent `SnapshotMode.Online` drain while live traffic is mirrored to the destination physical tree. The drain merges via `IShardRootGrain.MergeManyAsync` on the destination so concurrent shadow-forward writes converge with the drain batch under LWW. `TreeResizeGrain` adds a `ResizePhase.Reject` transition, a phase-aware dual-window `UndoResizeAsync` (drain-window gated on `Phase == Snapshot`; all later phases route through after-swap recovery), and threads a `logicalTreeId` through `SnapshotWithOperationIdAsync` so the transient per-shard `StaleTreeRoutingException` at swap carries the caller's logical name. `HotShardMonitorGrain` polls `ITreeResizeGrain.IsCompleteAsync`; `ReshardAsync` rejects while a resize is running. Verified end-to-end by `ChaosResizeIntegrationTests.Chaos_resize_under_concurrent_load_preserves_all_data`. See [Tree Sizing](../../docs/lattice/tree-sizing.md#resizing-an-existing-tree).

  - **F-019c - Registry-authoritative structural sizing** ([#63](https://github.com/NSTA1/Orleans.Lattice/pull/63)). Removes the `MaxLeafKeys`, `MaxInternalChildren`, `ShardCount`, and `VirtualShardCount` knobs from `LatticeOptions` entirely so they can never silently diverge from the layout a tree was built with. Canonical defaults live in `LatticeConstants` (`DefaultMaxLeafKeys = 128`, `DefaultMaxInternalChildren = 128`, `DefaultShardCount = 64`, `DefaultVirtualShardCount = 4096`) and the per-tree values are seeded into `TreeRegistryEntry` on first tree use via `LatticeRegistryGrain.RegisterAsync` (idempotent). After seeding, the registry is the only source of structural truth: `ShardRootGrain`, `BPlusLeafGrain`, and `BPlusInternalGrain` all read through the new `LatticeOptionsResolver`, which lazy-seeds on a missing pin and otherwise returns the per-tree values verbatim. `ResizeAsync` and `ReshardAsync` remain the only mutation paths for the pinned fields, and both ship with an empty-tree fast-path that atomically updates the registry pin without activating the coordinator machinery; `ReshardAsync`'s grow-only restriction is relaxed when the tree is empty so callers who want non-default sizing (including test fixtures) simply call `ResizeAsync` and/or `ReshardAsync` on the freshly-created tree - or pre-register the pin via `ILatticeRegistry.RegisterAsync` (a new `FourShardClusterFixture.CreateTreeAsync` helper encapsulates this for integration tests). `VirtualShardCount` is now an immovable library constant because changing it would invalidate every persisted `ShardMap`. System trees (e.g. `_lattice_trees`) resolve directly from `LatticeConstants` without consulting the registry, closing the system-tree corruption surface that FX-013's monitor-fallback design left open. Pre-production breaking change: callers that previously set the removed `LatticeOptions` properties must migrate to `ResizeAsync` / `ReshardAsync` on the new tree or pre-register through the registry; FX-013's registry-first resolver, drift-throw, and "pin wins over monitor" rules are now moot.

  **Explicit boundaries (by design, not gaps).** `ReshardAsync` is grow-only on non-empty trees - shrink would require live-merging shards and is out of scope. `ResizeAsync` fan-out changes are bidirectional (grow or shrink) because the snapshot + alias-swap path rebuilds the tree regardless of direction. Split concurrency is bounded per-tree via `MaxConcurrentAutoSplits` and `MaxConcurrentMigrations`; cluster-wide aggregate admission control is tracked separately as **G-008**.
- [x] **G-011 - Caller-supplied idempotency key for `SetManyAtomicAsync`** *(pre-v3.0.0 API lock-in)*: A new `ILattice.SetManyAtomicAsync(entries, operationId)` overload (plus two matching typed overloads on `TypedLatticeExtensions`) accepts a stable caller-supplied idempotency key. The saga grain is keyed `{treeId}/{operationId}` as before, so re-submitting with the same `operationId` re-attaches to the original saga: if it has completed, the second call observes the terminal outcome (or rethrows the original compensation-surrogate `InvalidOperationException`); if it is still in flight, the call awaits the terminal state. This turns a transport-level failure (silo crash mid-call, client-side timeout) into a safe client retry. `operationId` validation rejects null / empty / whitespace and any value containing `'/'` (reserved as the grain-key separator) with `ArgumentException`. Key-set stability is enforced via a SHA-256 fingerprint over the sorted key set, persisted on first `Prepare` as `[Id(7)] AtomicWriteState.KeyFingerprint` (wire-compatible: missing field on legacy state decodes to `null` and skips the check). Re-submissions whose key set differs (added, removed, or renamed keys) throw `InvalidOperationException`; reordering keys and changing values is allowed. The default `SetManyAtomicAsync(entries)` overload is unchanged and continues to generate a fresh `Guid`. Retention semantics are unchanged: completed saga state lives for `LatticeOptions.AtomicWriteRetention` (default 48h) and is then purged, after which the same `operationId` becomes eligible for a fresh saga. Regression tests: 7 unit tests on `AtomicWriteGrain` (fingerprint stability, mismatch detection, legacy-state compatibility) and 6 integration tests on `AtomicWriteIntegrationTests` (idempotent replay, mismatched-key-set rejection, invalid-operationId rejection, typed overload). Documented in [`docs/atomic-writes.md`](../../docs/lattice/atomic-writes.md#caller-supplied-idempotency-keys) and [`docs/api.md`](../../docs/lattice/api.md).

### Observability

- [x] **F-014 - Per-shard health / diagnostics**: New `ILattice.DiagnoseAsync(bool deep = false, CancellationToken)` method returns a `TreeDiagnosticReport` with per-shard depth, live key count, tombstone count (deep mode only), hotness (ops/sec, reads, writes, window), split-in-progress and bulk-operation-pending flags, plus a bounded 32-entry ring buffer of recent adaptive-split events. Aggregation runs through a new internal, single-activation `ILatticeStats` grain keyed by logical tree id: `LatticeGrain.DiagnoseAsync` forwards to it, it fans out to every physical `IShardRootGrain.GetDiagnosticsAsync(deep)`, and coalesces repeat callers through a dual-mode (shallow/deep) cache bounded by `LatticeOptions.DiagnosticsCacheTtl` (default 5 s, `TimeSpan.Zero` disables). Shard-level depth is computed by walking `RootNodeId` via `IBPlusInternalGrain.GetLeftmostChildWithMetadataAsync`; deep tombstone counts walk the leaf chain via `IBPlusLeafGrain.GetStatsAsync`. `TreeShardSplitGrain.FinaliseAsync` pushes a best-effort `RecordSplitAsync(shardIndex, utcNow)` to the diagnostics grain as a fire-and-forget notification that also invalidates the cache, guaranteeing the next call after a topology change returns a fresh report. Per-shard RPC failures surface as empty shard entries rather than failing the whole report, so `DiagnoseAsync` is safe to call during an ongoing resize or reshard. Public DTOs (`TreeDiagnosticReport`, `ShardDiagnosticReport`, `RecentSplit`) are `readonly record struct`, `[GenerateSerializer][Immutable]`, and aliased in `TypeAliases`. Regression tests: 10 unit tests on `LatticeStatsGrainTests` (shallow/deep aggregation, dual-mode cache independence, ring-buffer capacity, cache invalidation on split, pre-cancelled token, shard-index stamping) and 7 integration tests on `DiagnosticsIntegrationTests` (empty tree, populated vs `CountAsync`, deep tombstones, depth growth after splits, cancellation fail-fast, hotness counters, cache-within-TTL identical `SampledAt`). Documented in [`docs/diagnostics.md`](../../docs/lattice/diagnostics.md) and [`docs/api.md`](../../docs/lattice/api.md).

- [x] **F-015 - Tree events (Orleans Streams)**: Metadata-only event publication on a per-tree Orleans stream, opt-in via `LatticeOptions.PublishEvents` (default `false`) and configurable via `LatticeOptions.EventStreamProviderName` (default `"Default"`). Stream namespace `"orleans.lattice.events"`, stream id = logical tree id, payload `LatticeTreeEvent` (`readonly record struct`, alias `ol.lte`) with `Kind`, `TreeId`, `Key`, `ShardIndex`, `OperationId`, `AtUtc`. Twelve kinds (`LatticeTreeEventKind`, alias `ol.ltk`): `Set`, `Delete`, `DeleteRange`, `AtomicWriteCompleted`, `SplitCommitted`, `CompactionCompleted`, `SnapshotCompleted`, `ResizeCompleted`, `ReshardCompleted`, `TreeDeleted`, `TreeRecovered`, `TreePurged`. Producers: `LatticeGrain` (per-key write events, with conditional emission on `SetIfVersion` / `GetOrSet`; `DeleteRange` collapsed to a single `{start}..{end}` event when anything was deleted), `AtomicWriteGrain` (stamps the saga's `operationId` into `RequestContext` on every phase so downstream per-key `Set` events carry the correlation id, plus a terminal `AtomicWriteCompleted`), and the four coordinator grains (`TreeShardSplitGrain`, `TombstoneCompactionGrain`, `TreeSnapshotGrain`, `TreeResizeGrain`, `TreeReshardGrain`) plus `TreeDeletionGrain` for the three tree-lifecycle kinds. Publication is fire-and-forget with log-and-swallow semantics: missing provider, serialization failures, and downstream queue exceptions never propagate to the write path. Subscribers consume via `LatticeExtensions.SubscribeToEventsAsync(ILattice, IClusterClient, Func<LatticeTreeEvent, Task>, string providerName = "Default", CancellationToken)` which returns a `StreamSubscriptionHandle<LatticeTreeEvent>` and throws `InvalidOperationException` with an actionable remediation message when the client-side provider is missing. Lattice does not register a stream provider on your behalf - opt in on both silo and client (e.g. `AddMemoryStreams("Default")` + `AddMemoryGrainStorage("PubSubStore")`). Regression tests: 4 unit tests on `LatticeEventPublisherTests` (gating, missing-provider no-throw, `OperationId` round-trip through `RequestContext`) and 5 integration tests on `EventStreamIntegrationTests` (per-key `Set`, `Delete`, `DeleteRange`, `SetManyAtomicAsync` with correlated `OperationId`, `SetIfVersionAsync` conditional emission). Documented in [`docs/events.md`](../../docs/lattice/events.md), [`docs/api.md`](../../docs/lattice/api.md), and [`docs/configuration.md`](../../docs/lattice/configuration.md).
- [x] **F-029 - External metrics / telemetry export** *(required F-013 ✓)*: Orleans.Lattice publishes `System.Diagnostics.Metrics` instruments on a single static `Meter` named `orleans.lattice` (pinned by FX-008's regression test), exposed via `Orleans.Lattice.LatticeMetrics` so OpenTelemetry pipelines subscribe once with `.AddMeter("orleans.lattice")` and receive every instrument. Eighteen instruments across five tiers: **shard-level** counters (`orleans.lattice.shard.reads`, `orleans.lattice.shard.writes`, `orleans.lattice.shard.splits_committed`) emitted from `ShardRootGrain.RecordRead` / `RecordWrite` and `TreeShardSplitGrain.FinaliseAsync` respectively; **leaf-level** histograms and counters (`orleans.lattice.leaf.write.duration`, `orleans.lattice.leaf.scan.duration` tagged `operation=keys|entries`, `orleans.lattice.leaf.compaction.duration`, `orleans.lattice.leaf.tombstones.created`, `orleans.lattice.leaf.tombstones.reaped`, `orleans.lattice.leaf.tombstones.expired`, `orleans.lattice.leaf.splits`) emitted from `BPlusLeafGrain` - a new `PersistAsync()` helper (partial file `BPlusLeafGrain.Metrics.cs`) wraps every `state.WriteStateAsync()` call with `Stopwatch.GetTimestamp()` deltas so the write histogram captures storage-provider latency from the grain boundary where I/O actually occurs, scan durations are timed around the synchronous loop body in `GetKeysAsync` / `GetEntriesAsync`, and `CompactTombstonesAsync` counts explicit-delete tombstones and TTL-expired entries into separate counters so operators can distinguish delete throughput from TTL churn; **cache** counters (`orleans.lattice.cache.hits`, `orleans.lattice.cache.misses`) emitted from `LeafCacheGrain.GetAsync` / `ExistsAsync` / `GetManyAsync`; **saga / coordinator / lifecycle** counters (`orleans.lattice.atomic_write.completed` tagged `outcome=committed|compensated|failed` from `AtomicWriteGrain.CompleteSagaAsync`; `orleans.lattice.coordinator.completed` tagged `kind=snapshot|resize|reshard|merge|compaction` from the five coordinator completion paths; `orleans.lattice.tree.lifecycle` tagged `kind=deleted|recovered|purged` emitted **unconditionally** from `TreeDeletionGrain.PublishTreeLifecycleEventAsync` so lifecycle visibility does not depend on the event stream being enabled); and **events & configuration** counters (`orleans.lattice.events.published` tagged `kind`; `orleans.lattice.events.dropped` tagged `reason=missing_provider|publish_error` so operators detect a misconfigured `PublishEvents` / provider pairing before it consumes silo resources; `orleans.lattice.config.changed` tagged `config=publish_events` emitted from `ILattice.SetPublishEventsEnabledAsync` so runtime policy changes show up on the same pipeline as the traffic they affect). All instruments carry a `tree` tag; shard instruments additionally carry `shard` (physical shard index, parsed once at activation from the grain-key suffix); scan histograms carry `operation`. Leaf grain ids are deliberately not tagged - in a large tree they would produce unbounded cardinality. The meter is created at assembly load, so callers wire it up on the silo host before the cluster starts and every subsequently-activated grain publishes into the already-subscribed pipeline. Durations are reported in milliseconds as `double`. Measurement callbacks are elided when no listener is attached, so there is no runtime cost on the hot path for callers who do not opt in. Regression tests: 14 integration tests (9 in `LatticeMetricsIntegrationTests` + 5 in `LatticeMetricsCoordinatorIntegrationTests` covering atomic-write `committed`, tree-lifecycle `deleted` / `recovered`, the events-quiescence invariant, and `config.changed` on `publish_events` toggles) plus 2 unit tests in `BPlusLeafGrainTests.Compaction` pinning the explicit-vs-TTL counter split, all using a `MeterListener` filtered by `ReferenceEquals(inst.Meter, LatticeMetrics.Meter)` so the tests cannot drift with the meter name. Documented in [`docs/metrics.md`](../../docs/lattice/metrics.md) and [`docs/api.md`](../../docs/lattice/api.md).

### Replication enablers

- [x] **F-035 - Grain-side mutation observer hook** *(unblocks replication R-010)*: New public `IMutationObserver` interface + `LatticeMutation` DTO + `MutationKind` enum in the `Orleans.Lattice` namespace. Observers are registered in the silo DI container (resolved as `IEnumerable<IMutationObserver>`) and invoked synchronously after every durably-committed mutation, from inside the grain write path, **after** `state.WriteStateAsync()` has returned. Per-key `Set` and `Delete` events are emitted from `BPlusLeafGrain.SetCoreAsync` / `DeleteAsync` (with `Timestamp`, `IsTombstone`, `ExpiresAtTicks`, and `OriginClusterId` preserved verbatim from the committed `LwwValue`); a `DeleteRange` event is emitted from `ShardRootGrain.DeleteRangeAsync` **once per shard that received the range** (not per user call and not per tombstoned key) after the full leaf-chain walk - a single `ILattice.DeleteRangeAsync` call against an N-shard tree therefore produces up to N identical-payload `DeleteRange` mutations (same `Key` / `EndExclusiveKey`, `Timestamp = HybridLogicalClock.Zero`), which is idempotent by design but noisy; consumers that need exactly-once delivery per user call dedup on `(TreeId, Key, EndExclusiveKey)`. Every shard publishes even when it matched zero live keys so replication consumers propagate the range unconditionally. Merge paths (`MergeEntriesAsync` / `MergeManyAsync`) and other internal convergence paths are deliberately silent - they are downstream of the originating write which already published. Dispatch is via a new silo-scoped singleton `MutationObserverDispatcher`: per-observer exceptions are caught and logged as `ILogger<MutationObserverDispatcher>` warnings with `ObserverType` / `TreeId` / `Key` / `Kind`, and the dispatcher continues with the remaining observers - a faulty observer cannot short-circuit its peers or the write path. The hook is **zero-cost when no observer is registered**: `MutationObserverDispatcher.HasObservers` is checked by the publish helpers (and by the `BPlusLeafGrain.SetCoreAsync` call sites) before allocating the `LatticeMutation` payload or doing any dictionary lookups on the hot path. `LatticeMutation` uses a flat shape (aliased `ol.mut`, fields `TreeId`, `Kind`, `Key`, `EndExclusiveKey`, `Value`, `Timestamp`, `IsTombstone`, `ExpiresAtTicks`, `OriginClusterId`) rather than embedding `LwwValue<byte[]>`, matching the convention that keeps the public extensibility contract independent of internal wire DTOs. Strict-vs-best-effort propagation is the consumer's concern (the grain write is already durable by the time the hook fires). Regression tests: 8 `MutationObserverDispatcherTests` (null guards, `HasObservers`, in-order invocation, swallow-and-log on throw with `NSubstitute`-verified `ILogger` warning, payload round-trip), 7 `BPlusLeafGrainTests.MutationObserver` partials (Set publish, Set with TTL preserving `ExpiresAtTicks`, Delete publish, no-publish on absent Delete, no-per-key-publish on leaf `DeleteRangeAsync`, no-publish on `MergeEntriesAsync`, default `CreateGrain` works with `NoObservers`), 3 `ShardRootGrainMutationObserverTests` (range publish shape, publish-even-when-zero-deleted, no-observer path), and 5 `MutationObserverIntegrationTests` end-to-end through the full `ILattice` pipeline (Set / Delete / empty-range / populated-range with per-shard fan-out bounds / two-sets-monotonic-HLC). Documented in [`docs/api.md`](../../docs/lattice/api.md#mutation-observers).

- [x] **F-036 - `OriginClusterId` on `LwwValue` / `LwwEntry`** *(unblocks replication R-020)*: Adds a `string?` cluster-of-origin stamp to every mutation the library persists. Authoring is **inbound-only**, via the ambient Orleans `RequestContext` (internal key `"ol.ocid"`, mirroring F-015's `"ol.opid"` saga-correlation precedent). A new public helper `Orleans.Lattice.LatticeOriginContext` - static `Current` getter/setter plus a scoped `With(string?)` disposable - is the sanctioned entry point: a caller forwarding a remote mutation wraps the call in `using (LatticeOriginContext.With(peerClusterId)) { await tree.SetAsync(...); }` before reaching the public `ILattice` surface. Inside the grain, `BPlusLeafGrain.SetCoreAsync` / `DeleteAsync` / `DeleteRangeAsync` read the context at the HLC-tick site and stamp it via a `with { OriginClusterId = ... }` expression onto the freshly-constructed `LwwValue` / tombstone; local writes that leave the context unset produce `OriginClusterId = null`. F-036 is the **inbound authoring** side; F-035's post-commit `IMutationObserver` hook is the **outbound consumer** side - observers read `LatticeMutation.OriginClusterId` for loop-prevention so replication can skip re-forwarding mutations that originated elsewhere. Preserved end-to-end through every lifecycle path the library already guarantees for `ExpiresAtTicks`: shard-split shadow-forward (`ForwardLocalWriteToShadowIfNeededAsync` round-trips via `LwwEntry`), saga prepare / compensate (`AtomicPreValue` gained `[Id(4)] OriginClusterId` - `PrepareAsync` captures the pre-saga origin off the raw `LwwEntry` and `CompensatePhaseAsync` wraps each rollback in `LatticeOriginContext.With(pre.OriginClusterId)` so the leaf re-stamps the original origin), tree snapshot / restore (`BulkLoadRawAsync` preserves via `LwwEntry.ToLwwValue()`), `MergeManyAsync` / `MergeEntriesAsync` (operate on `LwwValue<byte[]>` directly, so the field flows through at zero cost), and tombstone compaction. Range deletes at the shard level read the ambient context once in `ShardRootGrain.PublishDeleteRangeAsync` and stamp it onto the single per-shard `LatticeMutation`. Wire-compatible: legacy persisted state missing any of the new `[Id]` slots decodes to `null` / `0`. Slot map: `LwwValue<T>` at `[Id(4)]`, `LwwEntry` at `[Id(5)]`, `LatticeMutation` at `[Id(8)]`, `AtomicPreValue` at `[Id(4)]`. Neither `ILattice` nor `TypedLatticeExtensions` exposes the field on the public surface - it is metadata for the replication package, which will document origin-cluster configuration in its own public API docs. Regression tests: 5 `LwwValueTests` additions (default null, `with`-set, tombstone round-trip, `Merge` winner preservation), 4 `LwwEntryTests` (ctor copy, `ToLwwValue` round-trip, default null, tombstone round-trip), 9 `LatticeOriginContextTests` (default, round-trip, null-clears, scoped `With`, null restore, nested scopes, idempotent dispose, async-flow propagation), 7 `BPlusLeafGrainTests.OriginClusterId` partials (stamp on Set / Set-with-TTL / Delete / DeleteRange, null default, observer payload on Set and Delete), and 2 `ShardRootGrainMutationObserverTests` additions (range-delete stamps from context / null default), and 2 `MutationObserverIntegrationTests` additions (end-to-end origin round-trip through the real cluster fixture, null default).
- [x] **F-037 - Replication write-ahead-log prefix reservation as a hard guarantee** *(unblocks replication R-011)*: The tree-name prefix `_lattice_replog_` is reserved as a named subset of the existing `_lattice_` umbrella `SystemTreePrefix`, giving the forthcoming `Orleans.Lattice.Replication` package a collision-free namespace for its internal write-ahead-log trees. The reservation is **unbypassable from external callers** - `grainFactory.GetGrain<ILattice>("_lattice_trees").SetAsync(...)` and `GetGrain<ILattice>("_lattice_replog_foo").GetAsync(...)` both throw on reads as well as writes, closing the corruption surface against the registry tree's backing storage and the future replog trees. Delivered in three composed layers:

  - **Layer 1 - `LatticeConstants.ReplogTreePrefix` + registry defence-in-depth.** Constant added alongside `SystemTreePrefix` with an XML doc that points downstream code at the constant by name rather than the literal. `LatticeRegistryGrain.RegisterAsync` and `UpdateAsync` reject any `treeId` starting with `SystemTreePrefix` (which subsumes `ReplogTreePrefix` and `RegistryTreeId`) via a shared `ThrowIfReservedPrefix` helper with `ArgumentException`. Catches third-party code that builds on the public `ILatticeRegistry` interface directly, and is the only guard point the registry itself needs. Regression tests: 8 partials in `LatticeRegistryGrainTests.ReservedPrefix.cs` covering register/update rejection of `_lattice_*`, `_lattice_replog_*`, and the literal `RegistryTreeId`; the with-entry `RegisterAsync` overload; the embedded-prefix happy path; and a compile-time invariant that `ReplogTreePrefix.StartsWith(SystemTreePrefix)`.

  - **Layer 2 - Public-surface guard on `LatticeGrain`.** A new private `ThrowIfSystemTree()` helper on `LatticeGrain` runs at the entry of every `ILattice` method - reads (`GetAsync`, `GetWithVersionAsync`, `ExistsAsync`, `GetManyAsync`, `KeysAsync`, `EntriesAsync`, `CountAsync`, `CountPerShardAsync`, `DiagnoseAsync`, every cursor open / step / close), writes (`SetAsync` and its TTL overload, `SetIfVersionAsync`, `GetOrSetAsync`, `SetManyAsync`, both `SetManyAtomicAsync` overloads, `DeleteAsync`, `DeleteRangeAsync`, `BulkLoadAsync`), and tree-lifecycle operations (`DeleteTreeAsync`, `RecoverTreeAsync`, `PurgeTreeAsync`, `ResizeAsync`, `UndoResizeAsync`, `SnapshotAsync`, `TreeExistsAsync`, `GetAllTreeIdsAsync`, `SetPublishEventsEnabledAsync`, `MergeAsync`, `IsMergeCompleteAsync` / `IsSnapshotCompleteAsync` / `IsResizeCompleteAsync`, `ReshardAsync`, `IsReshardCompleteAsync`) - rejecting any activation whose primary key starts with `SystemTreePrefix` with `InvalidOperationException` and an actionable remediation message. Namespace enumeration is as sensitive as mutation for a reserved tree, so reads are guarded too. `GetRoutingAsync` is deliberately **not** guarded because it is pure routing metadata consulted by internal coordinator grains before dispatching further internal calls; shard grains enforce the real boundary on the actual reads/writes. The async-iterator `KeysAsync` was split into a non-iterator public forwarder + a private `KeysAsyncCore([EnumeratorCancellation]...)` so the guard throws at call site rather than on first `MoveNextAsync`. `EntriesAsync` was split the same way for symmetry, with its corresponding `EntriesAsyncCore` reachable from both `ILattice` and `ISystemLattice` via explicit interface implementation.

  - **Layer 3 - Internal bypass via `ISystemLattice`.** A new `internal interface ISystemLattice : IGrainWithStringKey` (with a stable `[Alias]`) declares the narrow subset of `LatticeGrain` methods that the library itself needs against system trees (`GetAsync`, `SetAsync`, `DeleteAsync`, `ExistsAsync`, `KeysAsync`, `EntriesAsync`, and any others audited during implementation). `LatticeGrain` implements both `ILattice` and `ISystemLattice`; `ISystemLattice` uses **explicit interface implementation** that delegates to the same private core methods as `ILattice` but without the `ThrowIfSystemTree` guard. Because the interface is `internal`, user code in other assemblies cannot call `grainFactory.GetGrain<ISystemLattice>` - the type is not visible - so the guard on `ILattice` is genuinely unbypassable for external callers. Internal call sites (the registry's self-hosted `Registry` field that backs `LatticeRegistryGrain`, any tree-lifecycle grain that touches `_lattice_trees` or the replog prefix, etc.) switch from `ILattice` resolution to `ISystemLattice` resolution for system trees, keeping one audited crossing point instead of scattering `RequestContext` trust flags through the codebase. `LatticeOptionsResolver.ResolveAsync`'s existing system-tree branch (which bypasses the registry and resolves directly from `LatticeConstants`) is unchanged - it's the correct fast path for system-tree activations and sits below the `ILattice` / `ISystemLattice` split.

  Strictly additive and non-breaking for users who never addressed a `_lattice_*` tree in the first place. Regression tests for layers 2–3: a new `LatticeGrainSystemTreeGuardTests` pinning public rejection on every `ILattice` method (reads and writes) for both `_lattice_trees` and `_lattice_replog_foo`; a `SystemLatticeIntegrationTests` pair exercising `ISystemLattice` access from inside the library end-to-end through the real cluster fixture; and an extension of the existing registry tests to confirm the registry's self-hosted storage continues to work after the switchover.

- [x] **F-038 - Typed CRDT primitive value surface** *(unblocks replication R-031)*: New public CRDT primitives and typed value-surface accessors over `ILattice`'s `byte[]` storage so users author CRDT mutations through a typed API instead of hand-rolling bytes. Three primitive state types: `OrSet` (observed-remove set, alias `ol.ors`) with per-element add / tombstone dot maps and pointwise-union `Merge`; `OrSetDot` (`readonly record struct`, alias `ol.osd`) carrying `(ReplicaId, Counter)`; `PnCounter` (positive-negative counter, alias `ol.pnc`) with per-replica monotonic `Increments` / `Decrements` dictionaries and pointwise-max `Merge`. The existing `VersionVector` is promoted from `internal` to `public`. Three accessors in `Orleans.Lattice.BPlusTree` - `OrSetAccessor`, `PnCounterAccessor`, `VersionVectorAccessor`, all `readonly record struct` over `(ILattice, string key)` - implement read-modify-write via the existing `GetWithVersionAsync` + `SetIfVersionAsync` CAS path with a 16-attempt default retry budget; values are JSON-serialised through `JsonLatticeSerializer<T>.Default`. `OrSetAccessor.AddAsync(element, replicaId)` derives the next dot counter as `max(observed counter for replicaId) + 1` so concurrent `Add` calls from the same replica don't collide on dot identity. `CrdtLatticeExtensions.OrSet(key)` / `PnCounter(key)` / `VersionVector(key)` extension methods on `ILattice` are the user-facing entry points, with `ArgumentNullException.ThrowIfNull` and `ArgumentException.ThrowIfNullOrEmpty` guards. The set of primitives is scoped to exactly the modes R-030 / R-032 reserve in the replication wire contract (`LwwRegister` is already covered by the existing `TypedLatticeExtensions<T>` over `LwwValue<T>`); MV-Register, OR-Map, and RGA are tracked as separate follow-ons (F-039 / F-040 / F-041) because each adds a new `LatticeMergeMode` enum member and a new delta type to the wire contract. Regression tests: 49 unit tests (18 `OrSetTests`, 3 `OrSetDotTests`, 19 `PnCounterTests`, 9 `CrdtLatticeExtensionsTests` argument-guard) and 24 integration tests (`CrdtAccessorIntegrationTests` covering Get / Add / Remove / Merge / Contains for OR-Set, Get / Value / Increment / Decrement / Merge for PN-Counter, Get / Tick / Merge for VersionVector, plus argument guards) end-to-end through the real cluster fixture. Documented in [`docs/api.md`](../../docs/lattice/api.md#crdt-value-surface-accessors).

- [x] **F-039 - MV-Register primitive + accessor** *(unblocks replication R-034)*: New public `MvRegister<T>` CRDT (multi-value register) that preserves concurrent writes as a set of dotted values rather than collapsing them like LWW. State shape: a dot-context-tagged set of `(replicaId, counter, value)` triples; `Set(replicaId, value)` ticks the per-replica counter and stores the new dot, dropping any dots dominated by the new dot's vector; `Values()` returns the set of currently-live values (one if writes were sequential, multiple under concurrency). `Merge` is pointwise: keep dots not dominated by the other side's dot context. New `MvRegisterAccessor` follows the F-038 CAS pattern. New aliases `ol.mvr` (state) / `ol.mvd` (dot record). User-facing entry: `ILattice.MvRegister<T>(key)` extension on `CrdtLatticeExtensions`. Receiver-side replication merge lands as R-034. Distinguishing semantic from `LwwRegister`: app code resolves concurrent writes ("show all the merge candidates") rather than the wire contract silently picking one - the right answer when two clusters legitimately have different valid views (e.g. a contact card edited from two devices) and LWW would erase one of them.

- [ ] **F-040 - OR-Map primitive + accessor** *(unblocks replication R-035)*: New public `OrMap<TKey, TValue>` CRDT - an observed-remove map of CRDT-typed values where keys themselves follow OR-Set add-wins semantics and values are recursively-mergeable CRDTs. Constraint: `TValue` must implement a new internal `ICrdt<TValue>` shape (`TValue Merge(TValue other)` + a `bool IsBottom` predicate to detect "absent after remove") so the map can recurse correctly through nested registers / counters / sets. State: `Adds` keyed by `(key, dot)` with the value snapshot and `Tombstones` keyed by dot. `Set(key, replicaId, value)` mints a new dot and stores the value; `Remove(key)` tombstones every observed dot for that key (add-wins variant - concurrent `Set` after `Remove` survives). `Merge` unions adds and tombstones, then merges values per surviving dot. New aliases `ol.orm` / `ol.ome` (entry). User-facing entry: `ILattice.OrMap<TKey, TValue>(key)`. The recursive value-merge requirement is why this is a distinct primitive from `OrSet<T>`: the map cares about *value* convergence within a key as well as key-set convergence. Receiver-side replication merge lands as R-035.

- [ ] **F-041 - RGA sequence primitive + accessor** *(unblocks replication R-036)*: New public `Rga<T>` CRDT (Replicated Growable Array) for collaborative ordered lists / text. State: a tree of `(dot, parentDot, value, isTombstone)` nodes ordered by a deterministic in-order traversal; `InsertAfter(parentDot, replicaId, value)` mints a new dot and links under the parent (concurrent inserts under the same parent break ties by `(counter, replicaId)` lexicographic order so every replica converges on the same sequence); `Remove(dot)` tombstones the node (preserved for causal-stability so a re-insert after the same parent still resolves correctly). `Merge` unions nodes and tombstones. New aliases `ol.rga` / `ol.rgn` (node). User-facing entry: `ILattice.Sequence<T>(key)` returning an `RgaAccessor<T>` with `InsertAtAsync(index, value)`, `RemoveAtAsync(index)`, `ToListAsync()`, plus the lower-level dot-explicit `InsertAfterAsync(parentDot, value)` for tooling that needs stable cursor identity. The materialised `index ↔ dot` map is rebuilt on every read; an internal cache layer is a follow-on optimisation, not part of F-041's surface. Receiver-side replication merge lands as R-036. The dominant use case is collaborative editing - pairing this with mutation observers (F-035) gives a real-time text/list channel out of the box.

- [ ] **F-042 - Cluster-internal queue abstraction (`ILatticeQueue<T>`)** *(prerequisite for any future replication FIFO-queue generalisation)*: New public `ILatticeQueue<T>` primitive - a typed, **single-cluster, FIFO** queue backed by a reserved system tree under a new `LatticeConstants.QueueTreePrefix` (e.g. `_lattice_queue_`), one logical queue per coordinator grain. Surface: `EnqueueAsync(item, ct)` returning a monotonic `long` entry id; `TryDequeueAsync(ct)` returning `QueueItem<T>?`; `PeekAsync(ct)` non-destructive head read; `CountAsync(ct)` cached count; `ListAsync(ct)` ascending-id snapshot for diagnostics. Bounded variant exposes a `QueueCapacity` option with FIFO eviction on overflow (the same shape the DLQ inlines today ضد `_lattice_replog_dlq_{treeId}`). Per-item payload is `(EntryId, T)` serialised through an injectable `ILatticeSerializer<T>` (default: the same `JsonLatticeSerializer<T>.Default` the F-038 CRDT accessors use).

  **Backing model.** Each queue is one coordinator grain (`ILatticeQueueGrain<T>`, internal) over one `ISystemLattice` system tree, with rows keyed `e/{19-padded-id}` so lexicographic order matches insertion order byte-for-byte. The grain bulk-loads on activation, serves reads from an in-memory cache, and writes through to the system tree on every mutation. `_nextEntryId` is recomputed on activation as `max(stored entry-id) + 1` so monotonicity survives silo restart. Big-O: enqueue is one leaf write (O(log n) via the shard map), dequeue is one leaf read + delete (O(log n)); the cache makes `Count` / `Peek` / `List` O(1) / O(1) / O(N).

  **Known performance hazard.** Lexicographic monotonic ids concentrate every enqueue / dequeue on the head and tail leaves of a single shard, so a sustained high-throughput queue does not get the load-balancing benefit of the rest of the tree. Mitigations available out of the box: a head-cursor row that lets a steady-state dequeue reuse a single leaf rather than re-walking from the root; batched range-delete-on-rotation so trimmed ids are reaped in chunks rather than one tombstone per dequeue; documented operator guidance to scope queue throughput to "diagnostic / control-plane" rather than "primary data-plane" workloads. A non-monotonic key hash (which would spread writes across the tree but lose FIFO ordering on read) is explicitly **not** offered - FIFO is the contract, and a queue without it would be `IReadOnlyDictionary<long, T>` plus a counter.

  **Strict cluster-internal scope.** `ILatticeQueue<T>` is **not** a CRDT-replicated primitive and **never** ships an associated `LatticeMergeMode`. Two reasons: (1) destructive dequeue is fundamentally non-monotonic - under the CALM theorem any coordination-free distributed FIFO requires consensus, which is outside the library's CRDT-merge model; (2) concurrent enqueues across replicas would have to agree on offset assignments to preserve FIFO, and offset assignment does not commute even though increment of a tail counter does (a PN-counter for the tail does not solve the problem because two replicas can claim the same offset). A replicated cross-cluster log primitive - per-cluster append lanes plus an OR-set of consumed offsets, providing **at-least-once** consumption with idempotent consumers rather than exactly-once destructive FIFO - is a separate, much larger design tracked under `G-014` framing if it is ever pursued; it would more honestly be named `ILatticeReplicatedLog<T>` or `ILatticeEventStream<T>` to avoid implying destructive-dequeue semantics that CRDTs cannot deliver.

  **Existing inline implementations to consolidate.** Today the replication DLQ (`IReplicationDeadLetterGrain`, see [`docs/lattice.replication/dead-letter-queue.md`](../../docs/lattice.replication/dead-letter-queue.md)) inlines exactly this pattern - bounded FIFO over a reserved system tree with capacity-driven eviction and a `dead_letter.removed{reason=evicted}` metric on overflow. The replication WAL pending-batch shape (R-071) is a related but distinct queue-of-batches structure. F-042 promotes the DLQ's inline buffer to the typed primitive, the DLQ becomes a thin specialisation (parked-entry payload, eviction-reason tagging), and any future FIFO-shaped replication staging area builds on the same abstraction rather than introducing a third inline copy.

  **Sequencing.** Land F-042 **before** any replication-side item that would generalise the DLQ's inline FIFO behaviour (or introduce a new FIFO-shaped buffer) - the replication package then consumes `ILatticeQueue<T>` rather than re-implementing the system-tree-backed buffer. Tracked as a forward dependency in [`../lattice.replication/roadmap.md`](../lattice.replication/roadmap.md).

- [x] **F-043 - `VectorClock` slot on `LwwValue` / `LwwEntry`** *(unblocks replication R-093)*: Adds an optional `VersionVector? VectorClock` to every mutation the library persists, carrying the sparse `{originClusterId → HLC}` commit-time frontier so receivers can perform causal-consistency checks at apply time without consulting an external HWM table. Authoring is **inbound-only**, via the ambient Orleans `RequestContext` (internal key `"ol.vc"`, mirroring F-036's `"ol.ocid"` precedent). A new public helper `Orleans.Lattice.LatticeVectorClockContext` - static `Current` getter/setter plus a scoped `With(VersionVector?)` disposable - is the sanctioned entry point: a forwarding caller wraps the call in `using (LatticeVectorClockContext.With(frontier)) { await tree.SetAsync(...); }` before reaching the public `ILattice` surface, and the leaf grain reads the context at the HLC-tick site and stamps it via a `with { VectorClock = ... }` expression onto the freshly-constructed `LwwValue` / tombstone. Local writes that leave the context unset produce `VectorClock = null`. Preserved end-to-end through every lifecycle path the library already guarantees for `OriginClusterId`: shard-split shadow-forward (round-trips via `LwwEntry`), saga prepare / compensate (`AtomicPreValue` gained `[Id(5)] VectorClock` - `PrepareAsync` captures the pre-saga VC off the raw `LwwEntry` and `CompensatePhaseAsync` wraps each rollback in nested `using (LatticeOriginContext.With(...)) using (LatticeVectorClockContext.With(...))` scopes so the leaf re-stamps both pieces of metadata), tree snapshot / restore (`BulkLoadRawAsync` preserves via `LwwEntry.ToLwwValue()`), `MergeManyAsync` / `MergeEntriesAsync` (operate on `LwwValue<byte[]>` directly, so the field flows through at zero cost), tombstone compaction, and shard-level range deletes (which read the ambient context once in `ShardRootGrain.PublishDeleteRangeAsync` and stamp it onto the single per-shard `LatticeMutation`). F-035's post-commit `IMutationObserver` payload exposes the field as `LatticeMutation.VectorClock` so replication consumers can read the frontier without re-capturing it. Wire-compatible: legacy persisted state missing any of the new `[Id]` slots decodes to `null`. Slot map: `LwwValue<T>` at `[Id(5)]`, `LwwEntry` at `[Id(6)]`, `LatticeMutation` at `[Id(9)]`, `AtomicPreValue` at `[Id(5)]`. **Explicit non-scope (deferred to follow-on items):** the replication apply paths that consume `LatticeMutation.VectorClock` for causal-consistency checks land as R-093; structural-rewrite VC inheritance through `LatticeOriginContext` lands as F-046; an atomic-transaction boundary signal so a saga batch shares one VC across every emit lands as F-044. Regression tests: 4 `LwwValueTests` additions (default null, `with`-set, tombstone round-trip, `Merge` winner preservation), 4 `LwwEntryTests` (ctor copy, `ToLwwValue` round-trip, default null, tombstone round-trip), 10 `LatticeVectorClockContextTests` (default, round-trip, null-clears, scoped `With`, null restore, nested scopes, idempotent dispose, async-flow propagation, independence from `LatticeOriginContext`), 7 `BPlusLeafGrainTests.VectorClock` partials (stamp on Set / Set-with-TTL / Delete / DeleteRange, null default, observer payload on Set and Delete), 2 `ShardRootGrainMutationObserverTests` additions (range-delete stamps from context / null default), and 2 `MutationObserverIntegrationTests` additions (end-to-end VC round-trip through the real cluster fixture, null default).

- [x] **F-044 - Atomic-transaction boundary on the mutation observer** *(unblocks replication R-089)*: Adds a stable correlation id to every `LatticeMutation` so replication observers can detect when several emits belong to the same enclosing atomic transaction and capture VC / origin / metadata exactly once for the batch. Implemented as **Option A (minimal surface)** - a new `[Id(10)] Guid TransactionId` field on `LatticeMutation` (default `Guid.Empty` for wire-compat) populated at the core mutation site, with no new lifecycle hooks on `IMutationObserver`. Authoring is via the ambient Orleans `RequestContext` (internal key `"ol.txid"`, mirroring F-036's `"ol.ocid"` and F-043's `"ol.vc"` precedents) and a new internal `Orleans.Lattice.LatticeTransactionContext` helper exposing `Current` / `EnsureCurrent()` / `Set(Guid)`. Every public `ILattice` write entry-point on `LatticeGrain` (`SetAsync` / `SetAsync` with TTL / `SetIfVersionAsync` / `GetOrSetAsync` / `SetManyAsync` / `DeleteAsync` / `DeleteRangeAsync`) calls `LatticeTransactionContext.EnsureCurrent()` immediately after parameter validation, which mints a fresh `Guid` if the context is unset and otherwise reuses the existing id - so single-key writes get a unique id per call, while a saga or forwarding caller that pre-stamps via `LatticeTransactionContext.Set(...)` shares the id across every nested emit. The atomic-write saga (`AtomicWriteGrain`) gained an `[Id(8)] Guid TransactionId` slot on `AtomicWriteState`: `PrepareAsync` mints once (after the key fingerprint is initialised) and persists, and `RunSagaAsync` calls a new `StampTransactionIdContext()` alongside the existing `StampOperationIdContext()` so every per-key `lattice.SetAsync` / `lattice.DeleteAsync` issued by the saga (including compensation rolls) inherits the saga's persisted Guid - surviving crash recovery because the id is durable on `AtomicWriteState` rather than the activation. Per-shard `DeleteRange` fan-out shares one id by construction (the public entry stamps the context once, every shard publishes under that id). Publish helpers (`BPlusLeafGrain.MutationObserver.PublishSetAsync` / `PublishDeleteAsync` and `ShardRootGrain.MutationObserver.PublishDeleteRangeAsync`) read `LatticeTransactionContext.Current` and stamp it on the emitted `LatticeMutation` via `with { TransactionId = ... }`; convergence-only paths that leave the context unset emit `Guid.Empty`, which is the documented default and what every existing observer (the replication package included) already tolerates without inspection. Wire-compatible: legacy `LatticeMutation` payloads decode to `Guid.Empty`; legacy `AtomicWriteState` payloads decode to `Guid.Empty` and the saga lazily mints on first activation. Slot map: `LatticeMutation` at `[Id(10)]`, `AtomicWriteState` at `[Id(8)]`. Regression tests: 7 `LatticeTransactionContextTests` (default, mint, preserve, replace empty, overwrite, clear-on-empty, default-mutation-empty), 3 `BPlusLeafGrainTests.MutationObserver` partials (stamp from context on Set, empty when unset, stamp from context on Delete), and 5 `MutationObserverIntegrationTests` additions (per-call non-empty id, two successive calls produce distinct ids, `DeleteRangeAsync` fan-out shares one id across shards, `SetManyAtomicAsync` shares one id across every per-key emit, `SetManyAsync` shares one id across every per-key emit).

- [x] **F-045 - `MutationCategory` classification on `LatticeMutation`** *(unblocks replication R-090)*: New public `MutationCategory { User = 0, Maintenance = 1 }` enum (alias `ol.muc`) and a new `[Id(11)] MutationCategory Category` slot on `LatticeMutation` with default `User` for wire compatibility. The slot is named `Category` rather than `Kind` because the existing `MutationKind` enum (Set/Delete/DeleteRange) already occupies that name on the same type. Authoring is via a new internal ambient context `LatticeMaintenanceContext` (RequestContext key `ol.maint`, a `bool` flag mirroring the F-036 / F-043 / F-044 RequestContext-based ambient-context precedents) exposing `Current` and a scoped `BeginScope()` disposable. The publish helpers (`BPlusLeafGrain.MutationObserver.PublishSetAsync` / `PublishDeleteAsync` and `ShardRootGrain.MutationObserver.PublishDeleteRangeAsync`) read `LatticeMaintenanceContext.Current` and stamp the field via a `with { Category = ... }` expression on every emit. Every public `ILattice` write entry-point - `SetAsync`, `DeleteAsync`, `DeleteRangeAsync`, `SetIfVersionAsync`, `GetOrSetAsync`, `SetManyAsync`, `SetManyAtomicAsync`, `BulkLoadAsync`, and saga compensation rolls - emits with `MutationCategory.User` because no internal site wraps them in a maintenance scope. Library-internal structural-rewrite sites (resize / rebalance / compaction / internal rewrite) opt-in to `MutationCategory.Maintenance` by wrapping their work in `using (LatticeMaintenanceContext.BeginScope()) { ... }` - F-045 lays the producer-side groundwork as zero-cost ambient infrastructure; current internal sites do not yet emit observer events (F-035 documents merge / convergence paths as deliberately silent), so the only behavioural change today is that every emit now carries the new field. Replication consumers read `LatticeMutation.Category` and skip the WAL append for `Maintenance` on replicated trees (R-090). Independent of `OriginClusterId` - a remote-origin maintenance emit would still be `Maintenance` and still skip the WAL. Wire-compatible: legacy persisted `LatticeMutation` payloads decode to `Category = MutationCategory.User` (the zero default), matching the documented default. Slot map: `LatticeMutation` at `[Id(11)]`. Regression tests: 7 `LatticeMaintenanceContextTests` (User default, scope flips to Maintenance, restore on dispose, nested scopes, idempotent dispose, async-flow propagation, default `LatticeMutation` carries User), 4 `BPlusLeafGrainTests.MutationObserver` partials (User default on Set, Maintenance inside scope on Set, Maintenance inside scope on Delete, return to User after scope disposes), and 2 `ShardRootGrainMutationObserverTests` additions (User default on range delete, Maintenance inside scope on range delete).

- [x] **F-046 - VC + origin context preservation through structural ops** *(unblocks replication R-091)*: Every structural rewrite that shadow-forwards a user write now preserves both `OriginClusterId` (F-036) and `VectorClock` (F-043) end-to-end with no recapture. Audit of structural sites confirmed the leaf write path (`BPlusLeafGrain.SetCoreAsync` / `DeleteAsync` / `DeleteRangeAsync` reads both ambient contexts at the HLC-tick site), saga compensate (`AtomicWriteGrain.CompensatePhaseAsync` already wraps each rollback in nested `using (LatticeOriginContext.With(...)) using (LatticeVectorClockContext.With(...))` scopes), shard-split shadow-forward (`ShardRootGrain.ForwardLocalWriteToShadowIfNeededAsync` round-trips via the raw `LwwEntry` which carries both fields verbatim), tree snapshot / restore (`BulkLoadRawAsync` round-trips via `LwwEntry.ToLwwValue()`), `MergeManyAsync` / `MergeEntriesAsync` (operate on `LwwValue<byte[]>` directly), and tombstone compaction all already preserve both fields. The single remaining gap was the receiver-side replication apply seam: `IReplicationApplyGrain.ApplySetAsync` / `ApplyDeleteAsync` / `ApplyDeleteRangeAsync` previously stamped `with { OriginClusterId = ... }` from the parameter but constructed `LwwValue` / range-walk scopes without a `VectorClock`. Closed by adding a `VersionVector? sourceVectorClock` parameter to all three apply-grain methods (`LatticeGrain.ReplicationApply.cs`): `ApplySetAsync` and `ApplyDeleteAsync` now stamp `VectorClock = sourceVectorClock` on the freshly-constructed `LwwValue`, and `ApplyDeleteRangeAsync` wraps the leaf-chain walk in nested `using (LatticeOriginContext.With(originClusterId)) using (LatticeVectorClockContext.With(sourceVectorClock))` scopes so per-shard `LatticeMutation` emits inherit both via the shard-level publish helper. The default `Orleans.Lattice.Replication.ReplicationApplier` threads `sourceVectorClock: null` for now - the WAL-side `WalRecord` extension that carries the commit-time frontier is R-091 / R-093 in the replication roadmap. Wire-compatible: every existing call site forwards `null` and observers continue to see `VectorClock = null` exactly as before. Regression tests: 2 `LatticeGrainReplicationApplyTests` additions (raw-entry verification of `ApplySetAsync` with a non-null VC and with `null`, asserted via `IShardRootGrain.GetRawEntryAsync`) and 2 `MutationObserverIntegrationTests` additions (per-shard `ApplyDeleteRangeAsync` observer payload carries the supplied VC; null context emits null), end-to-end through the real cluster fixture.

- [x] **F-054 - `ApplyManyAtomicAsync` source-HLC-preserving atomic apply seam** *(unblocks replication R-098 ✓)*: New method on the existing `IReplicationApplyGrain` (per-tree, internal) so the receiver side of cross-cluster atomic-batch delivery (replication Phase 9, R-094 → R-104) can apply every key in an atomic batch under a single saga that preserves each entry's source-side `(Timestamp, OriginClusterId, VectorClock, ExpiresAtTicks)` verbatim. Shape: `Task<AtomicApplyResult> ApplyManyAtomicAsync(IReadOnlyList<AtomicApplyEntry> entries, Guid transactionId, string originClusterId, VersionVector? sourceVectorClock, CancellationToken ct)` where `AtomicApplyEntry` is a new public `readonly record struct` (alias `ol.aae`) carrying `Key`, `Value` (or `IsTombstone`), per-key `Timestamp`, per-key `ExpiresAtTicks`, and per-key `VectorClock` (the saga-wide VC frontier captured by R-089 - every entry in a batch shares it, but the field is per-entry on the wire so a future producer that captures per-key VCs need not break the contract). `AtomicApplyResult` mirrors the existing `ApplyResult` shape (`Outcome`, `AppliedCount`, `FailureReason?`). Implementation reuses `AtomicWriteGrain.RunSagaAsync` end-to-end: the apply grain spins up an `AtomicWriteGrain` keyed by `(treeId, transactionId)` (deterministic so a retry of the same atomic apply is idempotent through the grain's existing saga-replay path) and calls `Run` with each entry rewritten as a per-key write op; the saga's `RunSagaAsync` then iterates the operations and the apply grain's `BeforeCallAsync` hook wraps each per-key call in nested `using (LatticeOriginContext.With(originClusterId)) using (LatticeVectorClockContext.With(entry.VectorClock)) using (LatticeHlcOverrideContext.With(entry.Timestamp))` scopes so the leaf grain re-stamps the source-side metadata verbatim - preserving the R-022 source-HLC-preservation invariant the existing point-apply seam already enforces. The new `LatticeHlcOverrideContext` (RequestContext key `"ol.hlc"`, `HybridLogicalClock?`) is the missing piece versus the point-apply seam, which threads `Timestamp` directly through the per-method parameter; the saga indirection means the leaf grain reads the value from ambient context instead. Compensation rolls inherit the same scopes so a partial-apply rollback re-stamps the **pre-saga** values' metadata, matching the behaviour the saga already provides for local writes via `AtomicPreValue`. The atomic apply is thus indistinguishable to a downstream observer from a `SetManyAtomicAsync` issued at the source cluster: every emitted `LatticeMutation` carries the source-side `Timestamp` / `OriginClusterId` / `VectorClock` / `TransactionId`, the batch is all-or-nothing, and reader-isolation behaviour matches the existing local saga (per-key linearizable, batch atomic, non-isolated - closing the cross-cluster atomic-visibility gap is R-097's receiver-side staging buffer, not the apply seam itself). Wire-additive: a new method on an internal grain interface, no observable change for existing consumers; the public `ILattice` surface is unchanged. Slot map: `AtomicApplyEntry` at `[Id(0..4)]`, `AtomicApplyResult` at `[Id(0..2)]`. Aliases: `ol.aae`, `ol.aar`. Regression tests: 8 `LatticeGrainReplicationApplyTests` additions (single-entry batch round-trips identically to point-apply, multi-entry batch all-or-nothing on success, all-or-nothing on injected mid-saga failure with full rollback verified via `IShardRootGrain.GetRawEntryAsync`, source-HLC preservation per-key, source-VC preservation per-key, source-OriginClusterId preservation, idempotent retry on same `transactionId`, tombstone entries supported) and 3 `MutationObserverIntegrationTests` additions (per-key `LatticeMutation` carries source-side `TransactionId` shared across the batch, source-side `VectorClock` shared across the batch, source-side `OriginClusterId` shared across the batch).

### Replication enablers - WAL-as-commit-point promotion (v2 commit path)

The items below promote the replication package's per-shard WAL (`IWalShardGrain`, replication R-011) to the **sole durable commit point** for the core library, with the leaf grain reduced to a materialised projection rebuilt from the WAL on activation. Together they retire the dual-write cost of "leaf `WriteStateAsync` + WAL append" that exists today (every committed mutation hits storage twice) and unlock several second-order wins: faster recovery (rebuild the projection from the trimmed WAL tail rather than scanning the leaf state), cheaper structural rewrites (resize / rebalance / compaction become deterministic log entries that every silo replays identically rather than coordinated rewrites against persisted leaf state), and a single source of truth for replication and local apply (the v2 local materialiser is just another `IChangeFeed` subscriber, indistinguishable from a remote peer modulo target).

The model has shipped under F-047 → F-052 (and F-049a–d) and is documented from the runtime perspective in [`docs/lattice/wal.md`](../../docs/lattice/wal.md). The items below are the landable work that converted the original design into shipped reality. The promotion itself (F-049) shipped in three phases: F-049b introduced the dual-durability commit on `BPlusLeafGrain` with the legacy state row still written behind a feature flag for reversible rollback; F-049d flipped the default and removed the shadow-write code path entirely in v3.4.0.

#### Design points worth pinning before implementation

- **The WAL is already the canonical mutation record.** Replication R-080 (causal+ schema) deliberately captures `OriginClusterId` (F-036), `VectorClock` (F-043), `MutationCategory` (F-045), and `TransactionId` (F-044) on every WAL entry - exactly the metadata a local materialiser needs to rebuild the leaf projection deterministically. The work below leverages those slots; it does not extend them.
- **Two HWM tables, distinct shapes.** The cross-cluster per-origin HWM (replication R-023, key `(tree, originClusterId) → HLC`) tracks remote-peer apply progress. The intra-cluster materialiser HWM (F-051) tracks local-projection apply progress per `(tree, shardIndex) → offset`. Both are first-class consumers of the WAL via `ILatticeReplicationCursorRegistry`, but they key differently because their dedupe semantics differ: cross-cluster dedupe is by `(origin, hlc)` and is necessarily HLC-shaped because remote peers do not share offset spaces; intra-cluster dedupe is by `(shard, offset)` and is necessarily offset-shaped because the materialiser walks one WAL shard at a time and offsets are dense and monotonic. The two tables coexist; neither subsumes the other. Documented up-front so a future reader does not "simplify" them into one and break either correctness invariant.
- **Structural ops stay on `WriteStateAsync()` for now.** The original design committed resize / rebalance / compaction to the WAL as `MutationCategory.Maintenance` entries that every silo replays identically. Under decision C of the F-049 design review this is **deferred** - the WAL-as-sole-commit-point flip is scoped to **leaf data only** (`BPlusLeafGrain`), and `BPlusInternalGrain` / `ShardRootGrain` / every coordinator grain keeps its existing `IPersistentState<T>` commit path. The retraction is on risk grounds, not value grounds: encoding structural rewrites as deterministic log entries requires a determinism contract enforcement surface (likely a Roslyn analyser) and a wire-format growth on `MutationKind` that would coordinate with the replication package's `LatticeMergeMode` enum, neither of which is justified before the leaf-only flip has accumulated operational evidence. Tracked as **G-015** for revisit once F-049 has shipped without incident.
- **Pre-merge delta capture, not post-merge state.** Every replication-side delta type (R-030: `LwwRegisterDelta`, `OrSetDelta`, `PnCounterDelta`, `VersionVectorDelta`) is shaped against the **author's intent** at the call site, not the post-merge state of the value. F-038's typed accessors and F-039 / F-040 / F-041 are explicitly designed this way. Carrying that discipline into the WAL means a v2 leaf projection rebuild applies the same deltas the producer authored - convergence is guaranteed by the CRDT merge function, not by replay order. F-047 lays the producer-side groundwork.
- **No new wire format.** Every item below is internal to the core library or strictly additive on existing slots. The replication wire format (R-041, alias `olr.be`, wire version 1) is unchanged.

**Recommended implementation order:** F-047 ✓ → F-048 ✓ → F-051 ✓ → F-050 ✓ → F-049a ✓ → F-049b ✓ → F-049c ✓ → F-049c.2 ✓ → F-049d ✓ → F-052. F-049 itself is split into five sequential sub-items (see entry below): F-049a lands the dormant commit-log adapter seams, F-049b introduces the dual-durability commit on `BPlusLeafGrain` with `LeafShadowWrites = true` so the legacy state row continues to be written, F-049c lands the per-shard replay coordinator + fall-off-log recovery + the F-052 digest API (which ships alongside F-049c so cross-silo divergence can be detected the moment WAL becomes the rebuild source), F-049c.2 promotes `IWalStorageProvider` from the replication package to `Orleans.Lattice` so single-cluster deployments under F-049d's flip do not need a hard reference on the replication package, and F-049d flips the `LeafShadowWrites` default to `false` and removes the legacy state row one minor later. F-047 / F-048 / F-051 / F-050 each ship dormant or behind a feature flag and are individually reversible; F-049b is the first step that observably changes commit behaviour, but `LeafShadowWrites = true` keeps it reversible until F-049d. F-052's `RebuildLeafProjectionAsync` and `GetMaterialiserLagAsync` (the rebuild and lag halves) ship as a follow-up because they only matter once the WAL is the durable source of truth.

- [x] **F-047 - Pre-merge delta capture on the observer payload** *(unblocks F-049b)*: Adds two new slots to `LatticeMutation` - `[Id(12)] MutationDeltaKind DeltaKind` and `[Id(13)] byte[]? DeltaPayload` - so observers receive the **author's intent** (a typed CRDT op, an LWW `Set` / `Delete` / `DeleteRange`, or a saga `SetMany` batch) alongside the post-merge state already carried by the existing slots. The F-038 / F-039 / F-040 / F-041 typed-accessor call sites populate `DeltaKind` + `DeltaPayload` through the existing `BPlusLeafGrain.MutationObserver` / `ShardRootGrain.MutationObserver` / `AtomicWriteGrain` publish helpers; LWW `Set` / `Delete` / `DeleteRange` emit a delta whose payload is the same shape the replication wire format already encodes (R-030 / R-032). Wire-compatible: legacy persisted `LatticeMutation` payloads decode to `DeltaKind = MutationDeltaKind.None` and `DeltaPayload = null`, which every existing observer (the replication package included) already tolerates without inspection. **Truthful scope note.** F-047 is the **observer-payload enrichment** half of the original "pre-merge delta capture" design. The commit-order inversion half - making the WAL append the first durable step of the leaf write path so a future reader can replay the author's delta against an in-memory projection rather than reconstruct intent from post-merge state - is deliberately **not** part of F-047 as shipped, because doing it without F-049b's `LeafShadowWrites` toggle and F-049c's per-shard replay coordinator would rip out the leaf's existing storage commit point with no rollback path. The commit-order inversion now lives in **F-049b** below; F-047 ships the producer-side delta on the observer payload so a future replication consumer can be built and validated against real workloads before F-049b changes the durability boundary. The `LwwValue<byte[]>` post-merge form continues to be the leaf's persisted commit shape today and remains so until F-049d removes the dual-write. No public API change beyond the two new slots.

  Acceptance (shipped): for every typed-accessor call site (F-038 OR-Set / PN-Counter / VersionVector, F-039 MV-Register, F-040 OR-Map, F-041 RGA) the observer payload carries a non-`None` `DeltaKind` whose `DeltaPayload` round-trips through `LwwValue<byte[]>.Merge` with the post-merge `Value` as a fixed point. For LWW `Set` / `Delete` / `DeleteRange` the observer payload carries the matching `MutationDeltaKind` shape. Replaying the observer-payload stream against an empty projection produces a state byte-equivalent to the leaf's persisted state under the existing F-048 `ILeafProjection.Apply` seam.

- [x] **F-048 - Leaf-grain projection rebuild seam** *(unblocks F-049, F-050)*: New internal `ILeafProjection` interface in `Orleans.Lattice.BPlusTree.Grains` implemented by `BPlusLeafGrain` (partial file `BPlusLeafGrain.Projection.cs`, explicit interface implementation) exposing `void Apply(in LatticeMutation mutation)`, `Task<long> GetCheckpointOffsetAsync(CancellationToken)`, and `Task SetCheckpointOffsetAsync(long offset, CancellationToken)`. The `Apply` parameter type was substituted from the original spec's `WalRecord` to `LatticeMutation` so the seam lives entirely inside the core library - `WalRecord` is owned by the `Orleans.Lattice.Replication` package and would invert the dependency direction. `LatticeMutation` already carries every field the seam needs: `Kind` (Set / Delete / DeleteRange), `Key` / `EndExclusiveKey`, `Value`, `Timestamp`, `IsTombstone`, `ExpiresAtTicks`, `OriginClusterId`, and `VectorClock`; the future replication-driven replay path produces a `LatticeMutation` from each `WalRecord` it consumes. `Apply` is the deterministic replay function - for `Set` and `Delete` it constructs an `LwwValue<byte[]>` from the mutation's metadata and merges it into the in-memory `Entries` dictionary via `LwwValue<byte[]>.Merge`, advancing the leaf's HLC if the incoming timestamp dominates; for `DeleteRange` it walks the half-open `[Key, EndExclusiveKey)` range over the existing entries and merges a tombstone for each match using the mutation's batch HLC. LWW makes `Apply` idempotent and commutative by construction - replaying the same mutation twice or replaying two mutations in either order converges to the same projection. `Apply` mutates only in-memory state; it never calls `state.WriteStateAsync()` itself. `GetCheckpointOffsetAsync` returns the persisted `LeafNodeState.ProjectionCheckpointOffset` (new `[Id(11)]` slot, default `0`, wire-compatible with legacy state); `SetCheckpointOffsetAsync` rejects backward advances with `ArgumentOutOfRangeException` and otherwise persists via the existing `PersistAsync()` helper. Idempotent same-offset re-assertion still flushes pending in-memory `Apply` work - that's the deliberate seam point at which a future caller commits a batch of replayed mutations atomically against the persisted projection. Activation-time replay budget bounded by `LatticeOptions.MaxLeafReplayEntries` (default `10_000`, validator `>= 1`); a leaf whose gap exceeds the budget will fall back to a full projection rebuild under F-049 / F-050. The seam ships dormant: today's leaf grain continues to write through its existing storage provider on every commit, and no foreground caller drives `Apply`. The structural-determinism contract (rejecting non-deterministic `Apply` implementations at build time via an analyser, or at replay time via a projection checksum) is deferred to F-049 / F-050 because it requires the WAL-as-sole-commit-point flip to have a meaningful enforcement surface - F-048 lays the in-memory replay primitive that those items will drive. Regression tests: 22 `BPlusLeafGrainTests.Projection` partials covering `Apply` for Set / Delete / DeleteRange (insert, no-persist, expiry preserved, origin / vector-clock preserved, LWW higher-wins / lower-loses, idempotent replay, commutative reorder, delete tombstones, delete loses to higher-Set, half-open range semantics, no-match noop, inverted-bounds noop, null-end noop, unknown-kind throws), checkpoint methods (default zero, persist + `WriteCount`, monotonic advance, backward-rejection, idempotent advance flushes pending `Apply`, pre-cancelled token throws on Get / Set), and an end-to-end replay-vs-foreground equivalence test that records every observer payload from a foreground-write grain and asserts a fresh grain replayed from those payloads observes the identical key-set.

  Acceptance: a fixture loads 1000 entries via the replication WAL directly (bypassing the leaf), activates the leaf grain, and asserts `GetAsync` returns the correct value for every key (replay path); a structural op (a manufactured leaf split) is committed to the WAL and replayed across three silos with bit-identical post-state; a non-deterministic `Apply` implementation is detected (either at build or at replay).

- [x] **F-051 - Materialiser-side HWM (offset-keyed)** *(unblocks F-049, F-050; complements replication R-023)*: New internal `ILeafMaterialiserHwmGrain` per-tree, per-shard, persisting `(tree, shardIndex) → offset` so the materialiser's apply progress survives silo restart. Distinct from replication R-023's `(tree, originClusterId) → HLC` per-origin HWM table - see "Two HWM tables" in the design points above. Backed by the leaf's own state row (one extra `[Id]` slot on the leaf state, default `0` for wire-compat) so it activates with the leaf and never adds a separate grain hop on the read path. The replay-on-activation path (F-048's `Apply` loop) starts from this offset; a checkpoint advance is coalesced to every K entries or T seconds via `LatticeOptions.MaterialiserCheckpointInterval` (default 1 second / 1000 entries, whichever first), with a synchronous checkpoint on graceful deactivation. Crash-recovery cost on a worst-case unflushed checkpoint is bounded by the interval times the steady-state apply rate.

  Acceptance: a fixture writes N entries, kills the silo, restarts, and asserts the materialiser resumes from the persisted checkpoint (replay budget bounded by the configured interval); a fixture configures `MaterialiserCheckpointInterval = TimeSpan.Zero` (every-entry checkpoint) and confirms zero replay on restart at the cost of one extra storage write per commit (operators with strict RTO budgets opt in).

- [x] **F-050 - Leaf-grain cursor-registry integration** *(unblocks F-049)*: The leaf grain itself is the materialiser. F-048's `ILeafProjection.Apply` is the projection step and F-051's coalesced `FlushPendingCheckpointAsync` is the durability step; F-050 closes the loop by wiring the persisted checkpoint offset into the replication package's `ILatticeReplicationCursorRegistry` (replication R-061) under a reserved `consumerId` of the form `"_lattice_materialiser_{treeId}_{leafGrainId}"` so the per-shard WAL GC pins the log under the slowest of {remote peers, local materialiser}. Independence is the key contract: removing every remote peer (no replication configured) leaves the leaf-as-materialiser as the sole consumer, the WAL is GC'd against its cursor alone, and the system continues to function - exactly the v2 local-only deployment shape that F-049 commits to permanently.

  Implementation is a thin partial `BPlusLeafGrain.CursorRegistry.cs` (~50 lines) that resolves `ILatticeReplicationCursorRegistry` from DI as `IServiceProvider.GetService<>` (nullable - replication package is optional) and hooks the existing checkpoint-persist seam in `BPlusLeafGrain.Projection.cs`: every successful `FlushPendingCheckpointAsync` publishes the freshly-persisted offset via `AdvanceCursorAsync(consumerId, offset)`. Failures to advance the cursor are logged-and-swallowed at warning level - a transient registry storage hiccup must not stall the foreground write path or block the leaf's own checkpoint advance, because the cursor is monotonic and the next checkpoint flush will catch it up. **No new grain, no new public API surface**. The dedicated `ILeafMaterialiserGrain` shape is explicitly deferred: the operator-rebuild use case is better served by deactivating the leaf grain and letting it replay from the WAL on next activation (F-048's existing path); the secondary-projection use case (a denormalised index that consumes the same feed and writes elsewhere) is genuinely a net-new grain when it lands, but it is not the same shape as "the leaf is the projection of itself" and conflating the two would force speculative generalisation into F-050.

  **Sequencing - lazy registration to coexist with pre-F-049 behaviour.** F-050 lands before F-049 in the recommended order, but pre-F-049 nothing drives `ILeafProjection.Apply` on the foreground write path: the leaf grain's checkpoint offset stays at its persisted value (`0` for a freshly-activated leaf) and never advances. An eagerly-registered cursor at offset `0` would pin the WAL at offset `0` indefinitely and block all GC - exactly the failure mode the registry is designed to *prevent*. The fix is **lazy registration**: the leaf calls `RegisterCursorAsync` only on the first checkpoint persist whose offset is strictly greater than `0`, and `AdvanceCursorAsync` on every subsequent flush. Pre-F-049, the projection-checkpoint persist seam (`FlushPendingCheckpointAsync`) is never driven on the foreground write path, so the cursor-report helper is unreachable and no leaf is ever registered; the WAL GC behaves exactly as it does today. (Foreground writes _do_ tick the leaf HLC for LWW timestamps, but the cursor report is gated on the projection-seam call site, not on HLC progression - the lazy `Clock > Zero` check inside the helper is defence-in-depth, not the primary protection.) Post-F-049, the first foreground-driven `Apply` advances the projection past `0`, F-051 persists the new checkpoint, and F-050 registers the cursor on that same flush. Symmetrically, a leaf grain that deactivates does **not** deregister - silo failover and idle deactivation are routine, and a deregister-on-deactivate would let the WAL GC the entries the next activation needs to replay. Deregistration happens only on intentional, terminal lifecycle events (tree deletion via `TreeDeletionGrain`, leaf eviction during a tree purge) - F-050 wires `DeregisterCursorAsync` into the same call sites that already tear down per-leaf state. **F-049c upgrade.** Once the commit-log adapter is wired up by F-049a and the foreground path drives `Apply` under F-049b, F-049c switches to **eager registration on `OnActivateAsync`** at `min(persistedCheckpoint, walHeadAtActivation)` - under WAL-as-truth a quiescent leaf must be visible to GC immediately to prevent a trim-past, so the lazy-on-flush behaviour described above is the correct fallback when no replication package is registered but is superseded by eager registration when it is.

  **Granularity - per-leaf consumerId, per-shard GC.** The reserved consumerId carries the leaf grain id, not the shard index, because each leaf's checkpoint advances independently as different keyspaces see different write rates. Cursor cardinality is bounded by the existing leaf activation density and is the same order of magnitude as the leaf grain count - well within the registry's design envelope. The per-shard WAL trim point is the `min` over every leaf cursor whose shard the registry maps to, computed by the registry's GC predicate (replication R-083). R-083 already computes `min` over a heterogeneous set of remote peer cursors, so widening the predicate to include local-materialiser cursors is a one-line change on the replication side that lands alongside the replication package's initial release.

  Acceptance: a fixture configured with no replication peers writes 1000 entries through the foreground path under F-049 and asserts (a) the leaf cursor advances to the WAL head, (b) the WAL is GC'd up to the leaf cursor, (c) no entries below the cursor remain after the GC pass; a fixture stalls a single leaf's `Apply` (substitute that throws) under F-049 and asserts the WAL for that leaf's shard is *not* trimmed past the stalled cursor even when every remote peer has caught up; a fixture exercising pre-F-049 behaviour (leaf grain activated but no foreground `Apply` driver) writes 1000 entries through the legacy commit path, asserts the leaf is never registered as a cursor, and confirms the WAL GC behaves identically to today's baseline; a fixture deletes a tree via `ILattice.DeleteTreeAsync` and asserts every leaf's cursor is deregistered before the WAL is truncated.

- [x] **F-049 - WAL-as-sole-commit-point promotion** *(shipped; replaces G-014, depends on F-047 ✓ + F-048 ✓ + F-050 ✓ + F-051 ✓)*: Flip the **leaf-data** commit path so the per-shard WAL append is the sole durable step. Under decision C of the F-049 design review, the flip is scoped to `BPlusLeafGrain` only - `BPlusInternalGrain`, `ShardRootGrain`, and the eleven coordinator grains (`AtomicWriteGrain`, `LatticeCursorGrain`, `HotShardMonitorGrain`, `TombstoneCompactionGrain`, `TreeDeletionGrain`, `TreeMergeGrain`, `TreeReshardGrain`, `TreeResizeGrain`, `TreeShardSplitGrain`, `TreeSnapshotGrain`, `LatticeRegistryGrain`) keep their existing `IPersistentState<T>` commit path. Encoding structural rewrites (split / resize / rebalance / compaction) as deterministic `MutationCategory.Maintenance` WAL entries that every silo replays identically is **deferred work** tracked as **G-015** below - F-048's original "manufactured leaf split replayed across three silos with bit-identical post-state" acceptance criterion is **retracted** under this decision. F-049 ships in five sequential sub-items, each individually reversible up to F-049d:

  - **F-049a - Commit-log adapter seams (dormant)** *(no behaviour change)*: Three new internal interfaces in `Orleans.Lattice.BPlusTree.Grains` - `ICommitLogWriter.AppendAsync(LatticeMutation, CancellationToken) → Task<long>` returning the assigned WAL offset; `ICommitLogReader.ReadAsync(treeId, shardIndex, fromOffsetExclusive, CancellationToken) → IAsyncEnumerable<(long Offset, LatticeMutation Mutation)>` plus `Task<long> GetHeadOffsetAsync(treeId, shardIndex, CancellationToken)`; `ILeafSnapshotProvider.StreamAsync(treeId, shardIndex, leafKeyRangeStart, leafKeyRangeEnd, CancellationToken) → IAsyncEnumerable<LatticeMutation>` plus `Task<long> GetSnapshotOffsetAsync(treeId, shardIndex, CancellationToken)`. All three are resolved via `IServiceProvider.GetService<>` (nullable - when no replication package is registered the seams are simply absent and pre-F-049b behaviour applies unchanged). The replication package registers concrete adapters (`WalCommitLogWriter` over `IWalShardGrain.AppendAsync`, `WalCommitLogReader` over the change-feed primitive, `LeafSnapshotProvider` over the existing `ISnapshotProvider`) inside `AddLatticeReplication`. The seams are pure abstractions over what the replication package already exposes internally - they invert the dependency direction so the core library can consume the WAL without taking a hard reference on `Orleans.Lattice.Replication`. Ships dormant: no foreground call site invokes the writer, no replay driver invokes the reader, and the snapshot provider is unused until F-049c. The replication package's existing test suite is unaffected.

  - **F-049b - Dual-durability commit on `BPlusLeafGrain`** ([#136](https://github.com/NSTA1/Orleans.Lattice/pull/136)) *(introduces `LeafShadowWrites`)*: The foreground commit path on `BPlusLeafGrain.SetCoreAsync` / `DeleteAsync` / `DeleteRangeAsync` is reordered as: build the `LatticeMutation` (with the same `Kind` / `Key` / `Value` / `Timestamp` / `IsTombstone` / `ExpiresAtTicks` / `OriginClusterId` / `VectorClock` / `MutationCategory` / `TransactionId` / `DeltaKind` / `DeltaPayload` fields the observer pipeline already populates) → `await commitLogWriter.AppendAsync(mutation, ct)` (when the seam is wired up; falls through to legacy behaviour when null) → `((ILeafProjection)this).Apply(in mutation)` to update the in-memory projection deterministically → `if (LeafShadowWrites) await PersistAsync()` to write the legacy state row → publish observer. New `LatticeOptions.LeafShadowWrites` (default `true` for the version that ships F-049b) gates the legacy `WriteStateAsync()`. **Crash-safety matrix** under WAL-as-truth + LWW-idempotent `Apply`: (1) WAL append throws → the foreground call surfaces the exception and no state changes; (2) WAL appends but `Apply` throws → the WAL is truth, the next replay reapplies the mutation and `Apply` is LWW-idempotent so the second application converges; (3) WAL appends, `Apply` succeeds, shadow `WriteStateAsync` throws → the WAL is truth, replay closes the gap on next activation, the in-memory projection is already correct so the foreground call returns success and the operator sees a `leaf.shadow_write.failed` warning; (4) every step succeeds but observer dispatch throws → the existing F-035 dispatcher catches per-observer exceptions and continues. **Causal+ preservation** is structural: every WAL-appended mutation already carries `OriginClusterId` (F-036), `VectorClock` (F-043), `MutationCategory` (F-045), and `TransactionId` (F-044), so the WAL is the complete causal record and a future replay restores both the LWW projection and the metadata stamps verbatim. **Replication double-WAL-append guard.** Replication observers receive every mutation `BPlusLeafGrain` commits and would, naively, re-append it to the WAL - closing the loop into a duplicate. The replication-side `ReplicationMutationObserver` short-circuits when the dispatching context is itself the commit-log adapter (a new `LatticeMutation.SourceIsCommitLog` ambient signal set by `ICommitLogWriter.AppendAsync` for the duration of the synchronous publish step). Because the replication package has not yet shipped, the suppression mechanism is folded into its initial release rather than tracked as a separate v2 follow-on. New metric `orleans.lattice.leaf.shadow_write.duration` (histogram, ms) and `orleans.lattice.leaf.commit.duration` tagged `step={wal|apply|shadow|observer}` so operators can attribute latency. Reads continue to serve from the in-memory projection (no change). F-049b ships with `LeafShadowWrites = true` so a misconfigured deployment (no replication package, seams absent) falls through to the pre-F-049 path; F-049d flips the default.

  - **F-049c - Per-shard replay coordinator + fall-off-log recovery + incremental projection digest** ([#137](https://github.com/NSTA1/Orleans.Lattice/pull/137)) *(shipped)*: New internal `ILeafReplayCoordinatorGrain` keyed `{treeId}/{shardIndex}` (one coordinator per WAL shard, decision **B** from the F-049 design review). On leaf activation `BPlusLeafGrain.OnActivateAsync` (a) reads the persisted `ProjectionCheckpointOffset` via `ILeafProjection.GetCheckpointOffsetAsync`, (b) eagerly registers the leaf cursor at `min(persistedCheckpoint, walHeadAtActivation)` via `ILatticeReplicationCursorRegistry.RegisterCursorAsync` - **superseding F-050's lazy-on-flush registration when the commit-log adapter is wired up**, because once WAL-as-truth is the durability boundary the cursor must be visible to GC immediately to prevent a trim-past on a quiescent leaf; the lazy path remains the fallback when no replication package is registered, (c) calls into the replay coordinator which reads `(checkpointOffset, walHead]` once via `ICommitLogReader.ReadAsync` and routes each mutation to the leaves whose key range covers it, awaiting `Apply` + `SetCheckpointOffsetAsync` per leaf - the per-shard coordinator amortises the WAL scan across every leaf in the shard rather than each leaf re-reading the same WAL slice. Three **fall-off-log triggers** drive recovery via `ILatticeFallOffLogDetector`: (1) the WAL has been trimmed past the leaf's persisted checkpoint (`commitLogReader.GetTailOffsetAsync > checkpoint`); (2) the replay budget `LatticeOptions.MaxLeafReplayEntries` (default 10 000) is exceeded; (3) the leaf has been cold past `LatticeOptions.LeafProjectionRetention` (default 7 days) - long enough that even a healthy WAL has likely been trimmed. New `LatticeOptions.ProjectionRebuildPolicy` enum: `SnapshotThenWal` (default - chosen for reliability and correctness), `FullRebuildFromWal` (diagnostic - replay from the absolute tail; fails fast if the WAL has been trimmed), `Fail` (operator-gated - surface a `LeafProjectionStaleException` and require an explicit `RebuildLeafProjectionAsync` call). `SnapshotThenWal` recovery: drain `ILeafSnapshotProvider.StreamAsync` for the leaf's key range against `Apply`, persist the snapshot offset as the new checkpoint, then tail-replay `(snapshotOffset, walHead]`. New metrics: `orleans.lattice.leaf.replay.duration` (histogram, ms) tagged `outcome={tail|snapshot_then_wal|full_rebuild}`, `orleans.lattice.leaf.replay.entries` tagged `outcome={applied|skipped}`.

    **Incremental projection hash - O(1) digest reads, no leaf rehashing.** The original F-049c spec computed `GetLeafProjectionDigestAsync` as an on-demand XxHash128 walk over every entry in the leaf's `Entries` dictionary on every call. That cost is unacceptable for the digest's primary use case (steady-state cross-silo divergence detection under WAL-as-truth post-F-049d): a whole-tree poll would activate every leaf in every shard and rehash every entry on every call, defeating Orleans' idle deactivation and warming the entire tree on a timer. F-049c instead maintains the digest **incrementally** on the leaf state. A new `[Id(12)] byte[]? ProjectionHash` slot on `LeafNodeState` (default `null`, treated as 16-byte zero, wire-compatible with legacy state) carries an XOR fold over per-key XxHash128 contributions `H(key, hlc, isTombstone, expiresAtTicks, originClusterId, vc-fingerprint, value-or-empty)`. XOR-of-per-key-hashes is commutative and self-inverse, and XxHash128 outputs are uniformly distributed, so a single-key mutation updates the projection hash in O(1) regardless of leaf size: insert is `hash ^= H(K, V_new)`, update is `hash ^= H(K, V_old) ^ H(K, V_new)` (the V_old contribution cancels), live-to-tombstone re-stamps the same key under its new tuple. Because the contribution is a function of the **post-merge** `LwwValue`, an idempotent re-application of the same mutation is a no-op on the hash (XOR of the same value twice cancels), matching the LWW idempotence `Apply` already provides; reordered application of two commutative LWW writes converges on the same final hash because XOR is associative and commutative. The hash is persisted alongside the rest of `LeafNodeState` on every `PersistAsync()`, so a silo restart resumes from the persisted value without rebuilding it. `BPlusLeafGrain.SetCoreAsync` / `DeleteAsync` / `DeleteRangeAsync` and `ILeafProjection.Apply` all maintain the fold through a shared internal `UpdateProjectionHash(in LwwValue<byte[]>? oldValue, in LwwValue<byte[]>? newValue)` helper, so the digest is meaningful in both the foreground-commit world (today) and the WAL-replay world (post-F-049d).

    **Public surface unchanged, cost inverted.** `ILattice.GetLeafProjectionDigestAsync(shardIndex, ct)` still walks the shard's leaf chain and chains per-leaf hashes through a shard-level XxHash128, but **no leaf is rehashed** - each leaf returns its persisted `ProjectionHash` field directly. Per-leaf grain-call cost: one field read, no `Entries` enumeration, no hashing work on the leaf side. Whole-tree poll cost is `O(shardCount × leafCount)` grain calls of microsecond-scale work each, and crucially the call no longer activates cold leaves to rehash them - the digest visits exactly the leaves a normal scan would visit anyway. A future optimisation can chain leaf hashes through `IBPlusInternalGrain` so each shard returns one hash without touching leaves at all; that's an internal change behind the existing public surface. Documentation framing in `docs/lattice/projection-rebuild.md` shifts from "diagnostic only, runs on demand" to "cheap enough for steady-state monitoring, including periodic cross-silo equality canaries".

    **Determinism oracle.** A walk-the-state XxHash128 is retained **as a regression-test oracle only** (`BPlusLeafGrainTests.Projection.IncrementalHash_matches_walk_state_hash`): after random Set / Delete / Set-with-TTL / DeleteRange sequences - including pairs of operations that converge on the same final state through different intermediate orderings - the test computes a one-shot hash over the leaf's sorted entry tuples and asserts equality with the persisted `ProjectionHash`. The walk path is never invoked in production code; it exists solely to catch "running hash got out of sync" bugs in the incremental fold, providing the structural-determinism enforcement F-048 deferred without requiring a Roslyn analyser.

    **F-052's `GetLeafProjectionDigestAsync` ships alongside F-049c** (decision 4 from the design review). A chaos test asserts cross-silo `ProjectionHash` equality across a multi-silo cluster under sustained writes; with the incremental fold the test imposes no measurable activation overhead. `RebuildLeafProjectionAsync` and `GetMaterialiserLagAsync` remain in F-052's scope.

    **Shipped scope:** the public `ILattice.GetLeafProjectionDigestAsync` surface, the `LeafProjectionDigest` DTO, the `ProjectionRebuildPolicy` enum, the `LeafProjectionStaleException` type, the three `LatticeOptions` knobs (`MaxLeafReplayEntries`, `LeafProjectionRetention`, `ProjectionRebuildPolicy`), the per-shard `ILeafReplayCoordinatorGrain`, the `ILatticeFallOffLogDetector`, the new `[Id(12)] LeafNodeState.ProjectionHash` slot, and the shared `UpdateProjectionHash` helper invoked from `BPlusLeafGrain.SetCoreAsync` / `DeleteAsync` / `DeleteRangeAsync` / `ILeafProjection.Apply`. Test coverage: 11 unit tests + 8 integration tests + the walk-state oracle regression test + 1 chaos test asserting cross-silo digest stability under sustained writes. The `orleans.lattice.leaf.replay.duration` and `orleans.lattice.leaf.replay.entries` instruments are declared on the meter and dormant. **Deferred to F-049d:** the activation-driven replay loop that wires `BPlusLeafGrain.OnActivateAsync` into the replay coordinator and emits the replay metrics - under the dual-durability default (`LeafShadowWrites = true`) the leaf still recovers from its persisted state row, so the activation path consults the cursor registry (eager-register at `min(persistedCheckpoint, walHeadAtActivation)`) but does not yet drive `Apply`. The incremental projection hash, by contrast, is maintained by both call paths today, so the digest is a first-class production surface immediately rather than dormant infrastructure waiting for F-049d.
  - **F-049c.2 - Promote `IWalStorageProvider` to the core library** *(shipped; required for single-cluster durability under F-049d's default flip; depends on Replication R-070 ✓, R-071 ✓)*: Re-home the public `IWalStorageProvider` interface, the `WalEntry` DTO, and the default `InMemoryWalStorageProvider` from `Orleans.Lattice.Replication` to `Orleans.Lattice`. Under F-049d the per-shard WAL is the leaf's sole durable commit point, so a single-cluster deployment that does **not** call `AddLatticeReplication` still needs a durable WAL backend to function - leaving the seam in the replication package would force every single-cluster host to take a hard reference on `Orleans.Lattice.Replication` purely to obtain the durable-storage abstraction, inverting the package boundary. New core DI extension `ISiloBuilder.AddWalStorage(...)` registers an `IWalStorageProvider` on the core service collection; `AddLatticeReplication` continues to register the replication-specific concerns (per-tree `WalStorageProvider` resolver on `LatticeReplicationOptions`, the gRPC ship-side encoder hook) on top of the core seam. The replication package consumes the core type verbatim - no re-export, no wrapper - so wire identity, the `WalEntry` Orleans-serialization alias, and the existing R-070 / R-071 test coverage are preserved bit-compatibly (only the declaring assembly moves). The F-049a `ICommitLogWriter` adapter (`WalCommitLogWriter`) layers over the same provider so the foreground leaf write path under F-049b remains storage-pluggable in both single-cluster and multi-cluster topologies; the `IWalShardGrain` (replication-internal) remains the consumer in the replicated topology. The `Orleans.Lattice.Replication.AzureTableStorage` sibling package envisaged under R-073 retargets `Orleans.Lattice` rather than `Orleans.Lattice.Replication` so it is usable by single-cluster hosts - durable-WAL backends are now properly a core concern. Acceptance: a single-silo single-cluster harness without `AddLatticeReplication` registers `InMemoryWalStorageProvider` via the new core extension and commits leaf writes through `ICommitLogWriter` against it (F-049b dual-durability mode) and through it alone (F-049d default-flipped mode); a multi-cluster harness with `AddLatticeReplication` continues to see the same provider type with no behaviour change; the existing R-070 / R-071 test suite passes unchanged because the `IWalStorageProvider` / `WalEntry` / `InMemoryWalStorageProvider` type identities are preserved across the move.

  - **F-049d - Default flip and shadow-write removal** *(shipped; release engineering; depends on F-049c.2 ✓)*: Default-flipped and the legacy state-row commit path removed in a single release (v3.4.0, `breaking`). New deployments commit exactly once: every leaf mutation appends to the per-shard WAL via `ICommitLogWriter` and updates the in-memory projection; the foreground `WriteStateAsync()` call site, the `LatticeOptions.LeafShadowWrites` toggle, the `orleans.lattice.leaf.shadow_write.duration` histogram, and the `step=shadow` tag value on `leaf.commit.duration` are all gone. Reads continue to serve from the in-memory projection, which is rebuilt from the WAL on activation via the F-049c replay coordinator. The leaf's persisted state row is still written by the F-049c projection-checkpoint flush, the F-051 coalesced checkpoint, and the `OnDeactivateAsync` flush - none of those changed; only the foreground commit path lost its persist call. The per-shard WAL's `IWalStorageProvider` (promoted to the core library by F-049c.2) is the core library's sole foreground-commit durability dependency. Replication consumers are unaffected: the WAL has been the replication source of truth since R-011 shipped. Hosts that have not yet wired up an `ICommitLogWriter` (no WAL adapter registered, single-cluster deployments without `AddLatticeReplication` and without `AddWalStorage`) **must** wire one before upgrading to v3.4.0 - without an adapter, foreground commits become non-durable (the in-memory projection updates and the call returns success, but a silo crash before the next checkpoint flush loses the write). The core `AddWalStorage(...)` extension shipped under F-049c.2 is the supported path for single-cluster hosts.

  Acceptance: F-049a - the seams resolve to non-null when `AddLatticeReplication` is registered and to null otherwise; the existing replication test suite is unchanged. F-049b - a fixture writes 1000 entries with `LeafShadowWrites = true` and confirms WAL head, in-memory projection, and persisted state row are all consistent; a fixture induces each of the four crash points (WAL throw, `Apply` throw, shadow throw, observer throw) and confirms convergence on next activation; a fixture asserts replication observers do not double-append when the commit-log adapter is the source. F-049c - a fixture trims the WAL past a leaf's checkpoint and confirms `SnapshotThenWal` recovery converges to the same digest as a tail-replay; a fixture sets `MaxLeafReplayEntries = 100` and writes 10 000 entries through the foreground path, then deactivates and reactivates and confirms the snapshot path is taken; a chaos fixture asserts cross-silo digest equality across a multi-silo cluster under sustained writes. F-049c.2 - a single-silo single-cluster harness without `AddLatticeReplication` registers `InMemoryWalStorageProvider` via the new core extension and commits leaf writes through `ICommitLogWriter` against it; a multi-cluster harness with `AddLatticeReplication` continues to see the same provider type with no behaviour change; the existing R-070 / R-071 test suite passes unchanged. F-049d - a fixture commits a sequence of mutations through the WAL-only foreground path, deactivates and reactivates every leaf, and confirms reads return identical values after rebuild from the WAL via the F-049c replay coordinator; the replication package's existing test suite passes unchanged.

- [x] **F-052 - Operator tooling for projection inspection and rebuild** *(operational follow-up to F-049; depends on F-048 ✓, F-049 ✓, F-050 ✓, F-051 ✓)*: New public admin API on `ILattice` (or a dedicated `ILatticeAdmin` if the surface area grows). Two of the three planned methods land here as the operational follow-up to F-049: `Task RebuildLeafProjectionAsync(treeName, shardIndex, ct)` rebuilds a single shard's projection from the WAL (used after a corrupt-projection incident or a `MaxLeafReplayEntries` blow-out); `Task<long> GetMaterialiserLagAsync(treeName, ct)` returns the largest `WAL_head - materialiser_checkpoint` gap across the tree's shards (back-pressure surface, complements replication R-064's `apply.lag`). The third - `Task<LeafProjectionDigest> GetLeafProjectionDigestAsync(treeName, shardIndex, ct)` - **ships alongside F-049c** rather than as part of F-052, because cross-silo divergence detection must land the same release as the WAL-as-rebuild-source flip; see F-049c for its scope. The operator surface deliberately does not expose "edit the projection in place" or "skip a WAL entry" - those would defeat the determinism contract; the recourse for a deterministically-failing entry is the replication DLQ (R-060) or, for a structural rewrite, an explicit compensating WAL entry committed via `MutationCategory.Maintenance`.

  Acceptance: a fixture corrupts the leaf projection (substitute storage), runs `RebuildLeafProjectionAsync`, and asserts the projection matches a fresh replay's digest (the digest API itself is verified by F-049c's chaos fixture); the lag metric correctly reports under a stalled materialiser fixture.

- [x] **F-053 - Chain leaf projection digests through internal grains** *(performance follow-up to F-049c; depends on F-049c ✓)*: Today's `ILattice.GetLeafProjectionDigestAsync(shardIndex, ct)` walks the shard's leaf chain and chains each leaf's persisted `ProjectionHash` through a shard-level XxHash128 - every queried shard activates every one of its leaves to read one 16-byte field. Even with the F-049c incremental fold reducing per-leaf work to a field read, a whole-tree poll still pays `O(leafCount)` grain-call hops per shard, which is wasted work in the steady state when most leaves have not changed since the previous poll. F-053 promotes the digest fold to `IBPlusInternalGrain`: each internal node maintains an XOR-folded `SubtreeProjectionHash` over its descendant leaves' `ProjectionHash` fields plus an `EntryCount` and a `HighestCheckpointOffset` (max over descendants), updated incrementally when a child reports a new digest. Per-leaf updates propagate up the tree on the same call path that already persists the leaf - `BPlusLeafGrain` notifies its parent (via a new internal `IBPlusInternalGrain.OnChildDigestChangedAsync(childIdx, oldHash, newHash, oldCount, newCount, oldCheckpoint, newCheckpoint)` hook) only when the leaf's hash actually changes, the parent XORs the delta into its `SubtreeProjectionHash`, and the chain continues to the root if the parent's hash itself changed. With the fold maintained at every internal level, `GetLeafProjectionDigestAsync(shardIndex, ct)` becomes one grain call per shard - a read of the root internal node's `SubtreeProjectionHash` - and a whole-tree poll costs `O(shardCount)` rather than `O(shardCount × leafCount)`. The public surface is unchanged: `LeafProjectionDigest` is still the return type, the digest is still defined as XOR-fold over per-key XxHash128 contributions, and the cross-silo divergence canary continues to compare digests bit-for-bit. The walk-state regression-test oracle from F-049c is widened to also assert that a shard's chained digest matches a one-shot walk over all of its leaves, catching `running internal hash got out of sync` bugs in the propagation path. Internal-only change behind the existing public surface; no wire-format growth. A new opt-out, `LatticeOptions.MaintainProjectionDigest` (`bool`, default `true`), accompanies the chained-fold so write-amplification-sensitive deployments that do not need cross-silo drift telemetry can disable the per-mutation XOR fold and the upward `ChildDigestSnapshot` publication entirely; when `false` the leaf-mutation funnels take a trimmed path, the persisted `ProjectionHash` is left untouched, and `ILattice.GetLeafProjectionDigestAsync` fast-fails with `InvalidOperationException` rather than returning a stale aggregate. The opt-out is configurable **per-tree** via a new `TreeRegistryEntry.MaintainProjectionDigest` column that the resolver consults ahead of the silo-wide default, mirroring how `PublishEvents` is already overridden per tree. Disabling is a **one-way operation per tree**: the first mutation that lands while maintenance is disabled stamps an irreversible registry latch (`TreeRegistryEntry.ProjectionDigestPermanentlyDisabled`) that supersedes both the per-tree override and the silo-wide default on every subsequent activation, because any mutation accepted while the fold is off permanently invalidates the persisted aggregate and silently re-engaging maintenance would publish a known-stale digest as if it were authoritative. System trees (those whose id begins with `_lattice_`) always resolve as `false` regardless of configuration, because they are not replicated and have no cross-silo drift-detection consumer. The `LeafProjectionDigest` DTO gains an additive `[Id(3)] int Version` field (`LeafProjectionDigest.CurrentVersion = 0` for the shipping shape) so future cross-cluster reconciliation (see the replication-roadmap automatic Merkle-based drift remediation track) can negotiate compatibility before comparing hashes; the field is purely additive on the wire because Orleans serialization defaults unknown ids to zero, which matches the shipping `CurrentVersion`. Documented in `docs/lattice/projection-rebuild.md` and `docs/lattice/configuration.md`. Sequencing: ship after F-049c has accumulated operational evidence, because the chained-fold's correctness depends on every leaf-level mutation faithfully calling the propagation hook - the F-049c walk-state oracle's coverage is the prerequisite signal that the per-leaf hash itself is sound before adding a second layer of incremental maintenance on top.

  Acceptance: a fixture writes a randomised mix of Set / Delete / DeleteRange / Set-with-TTL operations across multiple shards, then asserts `GetLeafProjectionDigestAsync(shardIndex, ct)`'s returned hash matches a fresh one-shot walk that re-XORs every leaf's persisted `ProjectionHash` under that shard's root; a chaos fixture induces concurrent writes during the propagation hop and confirms convergence after quiescence; an instrument-counter fixture confirms the whole-tree poll issues exactly one grain call per shard.

- [x] **F-056 - Promote WAL cursor registry and GC to the core library** *(completes the core-side durability seam started by F-049c.2 ✓; required for single-cluster deployments under F-049d's default flip ✓)*: Re-home `ILatticeReplicationCursorRegistry`, `InMemoryReplicationCursorRegistry`, `ReplicationCursorSnapshot`, `ILatticeReplicationGc`, `LatticeReplicationGc`, and `ReplicationGcReport` from `Orleans.Lattice.Replication` to `Orleans.Lattice`, renamed to `IWalCursorRegistry`, `InMemoryWalCursorRegistry`, `WalCursorSnapshot`, `ILatticeWalGc`, `LatticeWalGc`, and `LatticeWalGcReport` respectively. Motivation parallels F-049c.2: under F-049d the WAL is the leaf's sole durable commit point and the leaf grain itself is a WAL consumer via F-050's cursor-registry integration, so a single-cluster deployment that does **not** call `AddLatticeReplication` still needs the cursor registry (to register the leaf-as-materialiser cursor for GC pinning) and the WAL GC (to trim the log against the leaf cursor) to function - leaving the seams in the replication package forces every single-cluster host to take a hard reference on `Orleans.Lattice.Replication` purely to obtain durable WAL maintenance. The rename drops the misleading `Replication` prefix because the WAL cursor concept is now native to core (the leaf-as-materialiser is the primary in-core consumer); the `Wal` prefix disambiguates from F-033's scan-iterator `LatticeCursor*` family which represents a different concept (paged scan progress, not WAL consumer positions). New core DI extensions `ISiloBuilder.AddWalCursorRegistry(...)` and `ISiloBuilder.AddLatticeWalGc(...)` on `LatticeServiceCollectionExtensions` register the in-memory defaults; `LatticeOptions` is extended with a new `WalRetention` knob (the per-tree partition count already lives on `LatticeOptions.WalPartitions` from the F-049b/F-049c shape and the per-tree `WalStorageProvider` resolver already lives on `LatticeOptions.WalStorageProvider` from F-049c.2 ✓ - no new options class is needed). The replication package's `AddLatticeReplication` continues to register the same impls (now resolved from the core types) and overlays per-tree config - `LatticeReplicationOptions.WalRetention` / `ReplogPartitions` / `WalStorageProvider?` resolver continue to apply for replicated trees by mirroring their values onto the corresponding `LatticeOptions` slots through the existing `MirrorReplicationOptionsToLatticeOptions` post-configure, so a host that sets retention on the replication options observes the same behaviour at the GC pass without an extra plumbing step. The `LatticeReplicationMetrics.WalEntriesTrimmed` counter is **also** promoted to core (`LatticeMetrics.WalEntriesTrimmed`, meter `orleans.lattice`) because the GC now runs in single-cluster mode where the replication meter is not registered; the counter retains its `tree` tag and emission semantics so existing OpenTelemetry pipelines pick it up after a one-line `.AddMeter` switch. The `LeafCursorReporter` adapter in `src/lattice.replication/Adapters/` is deleted because the in-core `ILeafCursorReporter` now resolves the core `IWalCursorRegistry` directly through DI without a replication-package bridge. Wire-compatible: `WalCursorSnapshot` and `LatticeWalGcReport` are plain `readonly record struct`s with no `[GenerateSerializer]` / `[Alias]`, so the move carries no Orleans-serialization risk (only the namespace and name change). Pre-production breaking change at the replication-package public-API surface: callers that referenced the old replication-package type names directly must rename and switch their `using` from `Orleans.Lattice.Replication` to `Orleans.Lattice`; the change set is mechanical and is documented in `docs/lattice/wal.md`. The sibling `Orleans.Lattice.Replication.AzureTableStorage` package (per R-073) retargets `Orleans.Lattice` rather than `Orleans.Lattice.Replication` for the cursor-registry / WAL-GC surface, consistent with how F-049c.2 retargeted the WAL storage backend. Acceptance: a single-silo single-cluster harness without `AddLatticeReplication` registers `InMemoryWalCursorRegistry` + `LatticeWalGc` via the new core extensions, drives them from leaf-as-materialiser cursor reports under F-050, and observes `LatticeWalGc.RunOnceAsync` trimming an `InMemoryWalStorageProvider`-backed WAL when the leaf cursor advances; a multi-cluster harness with `AddLatticeReplication` sees the same type identities and behaviour unchanged; the existing R-061 / R-083 test suites pass after namespace/name updates; the public-API contract tests in `test/lattice.replication/PublicApiContract/PublicReplicationApiContractTests.CursorRegistry.cs` and `.Gc.cs` are split into a new `test/lattice/PublicApiContract/PublicLatticeApiContractTests.WalGc.cs` (asserting the core surface) and a slimmed replication-side contract test retaining only the options-overlay assertions.

These items are strictly additive and non-breaking. They can land in any order; R-010 / R-011 / R-020 only require them before starting, not before each other.

---

## 🔲 Outstanding

Items are ordered by estimated impact. Dependencies on completed features are noted inline; outstanding items that depend on other outstanding items are indented beneath their prerequisite.

Audit follow-up fixes (`FX-###`) are tracked in a [dedicated section below](#-audit-follow-up-fixes) and take operational precedence over feature work when they touch data-loss or liveness risks.

### 1 · F-025
**Feature / high impact**

Incremental, ongoing merge from one or more source trees using `VersionVector` to track a per-source high-water mark, so each cycle transfers only entries newer than the last. Requires a delta-aware leaf scan (`GetEntriesNewerThanAsync(HybridLogicalClock threshold)`) and a merge-state tracking grain to persist the vector per source tree. Should replace the current full-shard drain in `TreeMergeGrain.MergeShardAsync` with a chunked or cursor-based leaf-chain iteration.

**Shared primitive with replication.** The `GetEntriesNewerThanAsync(HybridLogicalClock threshold)` leaf scan introduced by F-025 is the same primitive required by the replication package's `ISnapshotProvider` (R-050 in [`../lattice.replication/roadmap.md`](../lattice.replication/roadmap.md)) - both need a streaming, HLC-threshold-filtered scan of the primary tree. Whichever lands first, the other consumes it verbatim; do not introduce a second, parallel scan API. F-025 and replication remain otherwise orthogonal - F-025 is in-cluster tree-to-tree merge on a scheduled pull cycle, replication is cross-cluster push of a single tree to peer clusters via a commit-time WAL (R-010 / R-011). The per-source-tree HWM table introduced by F-025 is structurally compatible with replication's per-origin-cluster HWM table (R-023), differing only in key shape.

---

### 2 · F-012
**Reliability / medium impact**

Optionally pre-warm `LeafCacheGrain` activations for recently-accessed leaves after a silo restart to reduce cold-start read-latency spikes. No dependencies.

---

### 3 · F-018
**Feature / lower impact (complex)**

Associate tags with keys and query by tag. Implementable as a secondary Lattice tree mapping `tag → Set<key>`, maintained transactionally alongside the primary write. High complexity; deferred until core feature set is stable.

---

### 4 · F-064 ✅ shipped
**Feature / reliability / high impact (extends the per-call strict isolation already established for `GetManyAsync` / `CountAsync` to multi-page reads, both streaming and durable)** *(depends on Core F-033 ✓, F-055 ✓, F-058 ✓, F-062 ✓)*

Point-in-time read views over multi-page enumerations. Today the `LatticeRegistrySnapshotContext` mechanism shipped under F-055 / F-058 / F-062 makes every *single-call* fan-out read (`GetManyAsync`, `CountAsync`, `CountPerShardAsync`) linearizable against the per-tree `ITxRegistryGrain` decision view, via a `snap1` / fan-out / `snap2` retry loop in `LatticeGrain.cs` (lines 285-310, 973, 1200-1247) that defaults out-of-snapshot decisions to `TxStatus.InFlight` on every participating leaf. The mechanism is precisely the one needed to deliver "no observable mid-saga `pre=N, post=M` split" within a single call. What it does **not** cover is **multi-page reads**: every streaming `KeysAsync` / `EntriesAsync` page (`LatticeGrain.Keys.cs:84`, `LatticeGrain.Entries.cs:59`) and every `Next*Async` step of a durable cursor (`docs/lattice/durable-cursors.md`, `BPlusLeafGrain.CursorRegistry.cs`) issues an independent `ScanKeysAsync` / `ScanEntriesAsync` fan-out with no registry-snapshot scope, so a saga that transitions `InFlight → Committed` (or `→ Aborted`) between pages presents a torn view: page *i* hides the saga's keys, page *i+1* surfaces them. This is the exact same gap F-055 closed for single-call reads, on the exact same mechanism.

F-064 closes it for both surfaces in one change, because the design distinction between them is one paragraph (lifetime: streaming snapshots live in the iterator state machine; durable snapshots are persisted and pinned), not a feature boundary.

**Design.** Two strictly-additive primitives sharing one registry-side helper:

1. **Unconditional streaming-scan isolation.** `KeysAsyncCore` / `EntriesAsyncCore` capture a `Dictionary<Guid, TxStatus>` via `FetchRegistrySnapshotAsync()` once at scan entry (before the initial shard fan-out), open a single `LatticeRegistrySnapshotContext.BeginScope` for the lifetime of the `IAsyncEnumerable`, and dispose it in the iterator's `finally`. Every per-shard `ScanKeysAsync` / `ScanEntriesAsync` page issued during the enumeration - including reconciliation drains under concurrent splits (`DrainSlotsToBufferAsync` on lines 184-194 of `LatticeGrain.Keys.cs`) - reads the same decision view. No retry loop is needed: the snapshot is frozen by construction, so every page is internally consistent against it, and the only divergence a multi-page reader can observe is its own snapshot lagging the registry's live state (the same window any reader has between `GetManyAsync` calls). The change is binary-source-compatible (no public signature changes) and behaviourally aligned with the already-shipped `GetManyAsync` contract - a single-call fan-out and a streaming scan now share one definition of "what the registry says". Default behaviour, no opt-in.

2. **Opt-in durable-cursor point-in-time isolation.** New `[Id(N)] bool PointInTime` slot on `LatticeCursorSpec` (default `false`, wire-compatible). When `true`, `LatticeCursorGrain.OpenAsync` captures the registry snapshot via `FetchRegistrySnapshotAsync()`, persists it onto a new `[Id(N)] Dictionary<Guid, TxStatus>? RegistrySnapshot` slot on `LatticeCursorState` alongside the existing `Spec` / `LastYieldedKey` / `Phase` / `DeletedTotal` fields, and on every `Next*Async` / `DeleteRangeStepAsync` rehydrates it via `LatticeRegistrySnapshotContext.BeginScope(state.RegistrySnapshot)` for the duration of the per-step fan-out. Persistence cost is `O(in-flight + retention-window saga count)` per cursor - bounded by the saga-throughput envelope and the `TxDecisionRetention` window, typically under 200 KB serialized on the F-055 acceptance benchmark. Default `PointInTime = false` preserves bit-for-bit the current cursor semantics; existing cursor consumers see no behaviour change.

3. **Registry-side pin with three independent expiry caps.** A `PointInTime = true` cursor must keep the snapshot's referent decisions queryable for the cursor's lifetime, but the tombstone-prune path (`TxRegistryGrain.PruneExpired` invoked inline from `ForgetAsync`, bounded by `LatticeOptions.TxDecisionRetention` default 60 s) does not know about cursors. New seam: `ITxRegistryGrain.PinSnapshotAsync(Guid pinId, IReadOnlyCollection<Guid> txids, TimeSpan ttl)` records a `TxRegistryState.SnapshotPins: Dictionary<Guid, SnapshotPin>` entry (new `[Id(5)]` slot, alias `ol.sp`, fields `Txids: HashSet<Guid>, ExpiresAt: DateTimeOffset`). `PruneExpired` is tightened to skip any tombstoned decision whose txid is in the union of every unexpired pin's `Txids` set; expired pins prune themselves on the same `PruneExpired` pass. The persisted footprint is bounded absolutely by a new `LatticeOptions.MaxPinnedSagaDecisions` cap (default 100 000) - `PinSnapshotAsync` throws `LatticeCursorRegistryPinExhaustedException` if accepting the snapshot would exceed the cap, so an `OpenAsync(PointInTime: true)` against a degraded registry fails fast rather than silently degrading or unbounded-growing.

   **Pin lifetime defence-in-depth (three independent caps).** A naive design pins for the cursor's full lifetime and unpins in `CloseAsync` / `OnTtlExpiredAsync`. That fails open under a single failure mode: an operator who configures `CursorIdleTtl = Timeout.InfiniteTimeSpan` (a supported configuration per `docs/lattice/durable-cursors.md`) plus an Orleans reminder-service degradation leaves a forgotten cursor pinning decisions forever. F-064 therefore uses three *independent* caps, any one of which is sufficient to bound pin lifetime:

   * **Cap 1 - cursor-driven (cleanup path).** `LatticeCursorGrain.OnTtlExpiredAsync` (the existing `TtlGrain` reminder hook that already clears forgotten cursor state) calls `UnpinSnapshotAsync(pinId)` before `ClearStateAsync` and reminder-unregister. Bounded by `CursorIdleTtl` (default 48 h).
   * **Cap 2 - registry-driven (defence-in-depth).** Every pin carries a server-recorded `ExpiresAt = now + LatticeOptions.MaxCursorSnapshotPinTtl` (new option, default 7 d, hard cap regardless of cursor TTL). `PruneExpired` evicts pins past `ExpiresAt` directly. A live cursor refreshes its pin on every `Next*Async` (slide alongside the existing `SlideTtlAsync()` reminder slide), so an actively-used long cursor extends its pin indefinitely. A cursor that misses a refresh window (silo failover slower than the pin TTL, or a client that quiesces for a week) throws `LatticeCursorSnapshotExpiredException` on the next step - the alternative of silently degrading to live reads would violate the point-in-time contract the cursor was opened under.
   * **Cap 3 - footprint (back-pressure).** `MaxPinnedSagaDecisions` absolute cap on the registry-wide pin footprint. `OpenAsync(PointInTime: true)` is the only failure surface; `Next*Async` of an already-open cursor never throws for pin-exhaustion reasons.

   The three caps compose: cap 1 covers the steady-state cleanup path, cap 2 covers reminder-degradation and infinite-`CursorIdleTtl` modes, cap 3 covers operator misconfiguration / saga-flood scenarios. No single failure mode leaves a permanent pin.

**Atomicity scope.** Both surfaces inherit the same scope already established for `GetManyAsync`: linearizability is against **saga decisions only**. A plain `SetAsync` / `RemoveAsync` (non-saga) issued between pages becomes visible on the next page in both the streaming and the durable case. Callers who need stronger isolation (no observable foreground writes during iteration) route their writes through `SetManyAtomicAsync`; the point-in-time view then hides the entire batch consistently. A future WAL-replay-based snapshot (level 3 in the design exploration that produced this entry) is the path to "zero observable writes" semantics, but that requires F-049 / F-052 to land first and is out of scope here.

**Cross-cluster.** The `F-062 ✅` `AtomicShardCount` / `TerminalArrivals` / `ExpectedTerminals` tally lives inside `ITxRegistryGrain.GetStatusAsync` / `SnapshotAsync`, so the captured snapshot already reflects the all-or-nothing cross-cluster gate. No additional wire surface, no additional replication-side change.

**Surface.** Public-additive only:
* `Orleans.Lattice.LatticeCursorSpec.PointInTime: bool` - new opt-in slot.
* `Orleans.Lattice.LatticeCursorSnapshotExpiredException` - thrown by `Next*Async` / `DeleteRangeStepAsync` when the registry pin TTL has elapsed without a slide.
* `Orleans.Lattice.LatticeCursorRegistryPinExhaustedException` - thrown by `OpenAsync(PointInTime: true)` when accepting the snapshot would exceed `MaxPinnedSagaDecisions`.
* `LatticeOptions.MaxCursorSnapshotPinTtl: TimeSpan` (default 7 d) - registry-side hard cap on pin lifetime.
* `LatticeOptions.MaxPinnedSagaDecisions: int` (default 100 000) - registry-wide footprint cap.

Internal-additive:
* `ITxRegistryGrain.PinSnapshotAsync(Guid pinId, IReadOnlyCollection<Guid> txids, TimeSpan ttl)` / `RefreshPinAsync(Guid pinId, TimeSpan ttl)` / `UnpinSnapshotAsync(Guid pinId)`.
* `TxRegistryState.SnapshotPins: Dictionary<Guid, SnapshotPin>` (new `[Id(5)]` slot; legacy decode-default = empty).
* `LatticeCursorState.RegistrySnapshot: Dictionary<Guid, TxStatus>?` (new slot; only populated for `PointInTime: true` cursors).
* `TypeAliases.SnapshotPin = "ol.sp"`.

**Observability.** Two new tag values on existing instruments rather than new meters:
* `orleans.lattice.cursor.duration` gains a `point_in_time` tag (`true` / `false`) so operators can size point-in-time cursor steps against ordinary cursor steps.
* `orleans.lattice.registry.snapshot.pin_count` (new `Gauge` on the existing `LatticeMetrics` source, tagged `tree`) tracks the current pin footprint so operators can alert before `MaxPinnedSagaDecisions` is hit.

**Acceptance.**
- *Streaming-scan isolation.* A fixture commits a 16-key `SetManyAtomicAsync` saga while a continuous reader holds a single `EntriesAsync` enumeration open. Across 50 trials the reader observes either zero or all keys from the saga at the boundary between pages, never a `pre=N, post=M` split.
- *Durable-cursor isolation.* The equivalent fixture using `OpenEntryCursorAsync(spec with { PointInTime = true })` + `NextEntriesAsync` paging exhibits the same all-or-nothing behaviour across pages, and a `PointInTime = false` control reproduces the pre-F-064 torn-view behaviour (regression-pin against silent change).
- *Pin lifetime - cleanup path.* A cursor opened, paged twice, then deactivated via `CloseCursorAsync` releases its pin within one registry round-trip; `TxRegistryGrain.SnapshotPins` is empty after the close.
- *Pin lifetime - TTL expiry.* A `PointInTime` cursor whose host-side TTL exceeds `MaxCursorSnapshotPinTtl` and which goes idle past the pin TTL throws `LatticeCursorSnapshotExpiredException` on the next step, and the registry's `SnapshotPins` footprint returns to baseline.
- *Pin lifetime - footprint cap.* A fixture configures `MaxPinnedSagaDecisions = 8` and opens point-in-time cursors against a workload exercising 12 in-flight sagas; the 9th `OpenAsync` throws `LatticeCursorRegistryPinExhaustedException` and existing cursors continue paging.
- *Cross-cluster atomicity.* A two-cluster fixture commits a multi-shard atomic saga while a remote reader holds an `EntriesAsync` enumeration; across pages the reader observes zero or all of the saga's keys, exercising the `F-062 ✅` `AtomicShardCount` tally through the snapshot.
- *Reminder-degradation defence-in-depth.* A fixture suppresses reminder firing for a cursor whose host-side TTL is `Timeout.InfiniteTimeSpan` and confirms the registry-side `MaxCursorSnapshotPinTtl` cap independently expires the pin within the configured window.
- *Replication interaction.* The replication apply path is unchanged; receivers observe the snapshot-pinned decisions through the same `ITxRegistryGrain` seam as locally, so no companion R-### entry is required.
- *Hygiene.* `LatticeCursorState`'s persisted footprint is asserted to round-trip through Orleans serialization with the new `RegistrySnapshot` slot decoding to empty for legacy persisted state.

**Out of scope.** Snapshot-isolated `GetManyAsync` (already shipped under F-055 / F-058 / F-062), zero-observable-writes semantics (tracked separately as F-065 below; depends on F-049 ✓ + F-052 ✓), per-page snapshot refresh (defeats the point), cross-cluster snapshot rendezvous across multiple clusters' independent registries (a future entry if a real consumer asks for it).

---

### 5 · F-065 ✅ shipped
**Feature / reliability / medium impact (zero-observable-writes snapshot reads via WAL-replay materialisation; the strict-isolation step F-064 explicitly defers)** *(depends on Core F-049 ✓, F-053 ✓, F-056 ✓, F-064 ✓, F-052 ✓)*

Strict snapshot-isolation reads: a multi-page enumeration whose view of every key is fixed at `OpenAsync` time and *not* perturbed by any concurrent write - foreground `SetAsync` / `RemoveAsync`, saga `SetManyAtomicAsync`, range deletes, or replication apply. F-064 closes the saga-decision dimension; F-065 closes the only remaining dimension: foreground non-saga writes between pages. Together they deliver the literal "point-in-time, tree-wide atomic" contract the design exploration that produced this entry sketched at level 3.

The mechanism is **deterministic WAL replay**, not MVCC. Lattice values are LWW, not versioned, so there is no per-key version history to walk - but the per-shard WAL established under F-049 ✓ is the deterministic source of truth for the projection (F-049d ✓ flipped the default and removed the legacy state-row commit path), and F-053 ✓ + F-056 ✓ give every reader the primitives needed to replay the WAL up to a chosen offset and verify the result. A snapshot is the projection produced by replaying records `0..P` for every shard, where `P` is captured atomically across shards at `OpenAsync` time. The expensive parts - the per-shard replay coordinator (`ILeafReplayCoordinatorGrain`), the WAL-as-rebuild-source contract (F-049c ✓), the chained projection digest (F-053 ✓), and the operator-side `RebuildLeafProjectionAsync` / `GetMaterialiserLagAsync` surface (F-052) - are either shipped or scheduled, so F-065 is mostly composition.

**Design.** Three primitives:

1. **Tree-wide snapshot offset capture.** New `IShardRootGrain.SnapshotWalHeadAsync(CancellationToken)` returning the shard's current WAL head offset (the next-to-be-assigned sequence number). At `OpenAsync` time `LatticeGrain.OpenSnapshotCursorAsync` fans this out across every shard the routing map currently advertises, then captures the `LatticeRegistrySnapshotContext` snapshot (F-064 / F-055 path) in the same scope. The captured tuple is `(treeMapVersion, perShardWalOffsets: Dictionary<int, long>, registrySnapshot)` - an immutable `LatticeSnapshotCoordinate` (alias `ol.lsc`) carried on `LatticeCursorState`. The fan-out is **not** linearizable across shards in real time (shards are independent grains, the fan-out is concurrent), but it does not need to be: every shard's WAL is independently deterministic, and a saga that committed mid-fan-out is resolved consistently across shards by the `registrySnapshot` half of the coordinate - if the captured snapshot shows the saga as `Committed`, every shard's WAL prefix replayed up to its captured offset will include the saga's prepared mutations and the snapshot view will see the post-saga state on every shard the saga touched; if `InFlight`, every shard hides the saga uniformly. The two halves of the coordinate together encode "the projection as of this tree-wide moment", not "the projection at any single wall-clock instant".

2. **Materialised per-shard snapshot leaves.** A snapshot cursor's `Next*Async` does not delegate to live `ScanKeysAsync` / `ScanEntriesAsync` (which see the live `Entries` dictionary on each leaf). Instead, the cursor's first read materialises **per-shard snapshot leaves** on demand: for each shard the cursor visits, a new internal `ISnapshotLeafGrain` keyed `{treeId}/{shardIndex}/{snapshotCoordinateHash}` rebuilds the shard's leaf-projection state from the WAL by calling `ILeafReplayCoordinatorGrain.ReadSliceAsync(fromOffsetExclusive: 0, toOffsetInclusive: capturedOffset, budget)` in a streaming loop, applies each record through the same `((ILeafProjection)this).Apply` path the live leaf uses, and exposes a read-only `ScanKeysAsync` / `ScanEntriesAsync` surface against the rebuilt state. The snapshot leaf is **transient** (in-memory only, no `IPersistentState`, no WAL of its own); a grain reactivation re-runs the replay - which is deterministic and idempotent by construction. Replay cost is amortised across pages of a single snapshot cursor: the snapshot leaf stays activated for the cursor's lifetime, so subsequent `Next*Async` calls against the same shard hit the already-rebuilt state. A snapshot cursor that spans every shard pays `O(sum(capturedOffset_s))` total replay cost up-front, not per page.

3. **Pinned WAL retention via the existing cursor registry.** A snapshot must keep the WAL prefix `0..max(capturedOffset_s)` alive for the cursor's lifetime, or replay re-runs (silo failover, snapshot leaf eviction) will fail with `WalEntryTrimmedException`. The existing `IWalCursorRegistry` is already the GC's `min(cursor)` input - F-065 reuses it verbatim: `LatticeCursorGrain.OpenAsync(PointInTime: true, SnapshotMode: ZeroObservableWrites)` calls `registry.ReportCursorAsync(treeName, consumerId: $"snapshot:{cursorId}", cursor: capturedHlc, blockedAtHlc: capturedHlc)` once per touched shard. The HLC-shaped report half pins the GC predicate's `min(cursor)` floor; the `blockedAtHlc` half pins the strict-less `entry.Timestamp < blockedFloor` clause. Every `Next*Async` re-reports (slides the pin, defends against TTL expiry of an inactive consumer registration). `CloseAsync` / `OnTtlExpiredAsync` unregister the consumer, releasing the WAL prefix back to the GC.

   This is *the same shape* as F-064's registry-pin design (three caps: cursor TTL, registry TTL, footprint cap), with the registry being `IWalCursorRegistry` instead of `ITxRegistryGrain`. The seam is already plumbed through `LatticeWalGc`'s trim predicate - no GC changes are needed; the snapshot consumer is just another registered cursor. A snapshot that ages past `MaxCursorSnapshotPinTtl` (F-064 option, default 7 d) without a slide unregisters itself; the next `Next*Async` throws `LatticeSnapshotExpiredException` because the WAL prefix the replay needs has been trimmed.

**Lifecycle.**

```
OpenSnapshotCursorAsync(spec, ct)
    ↓
LatticeGrain captures (treeMapVersion, perShardWalOffsets, registrySnapshot)
    ↓
LatticeCursorGrain.OpenAsync persists LatticeSnapshotCoordinate onto LatticeCursorState
    ↓
For every shard the spec touches:
    IWalCursorRegistry.ReportCursorAsync(treeName, "snapshot:{cursorId}", capturedHlc_s, blockedAtHlc = capturedHlc_s)
    ↓
Cursor returns to client; opaque cursorId returned

NextKeysAsync(cursorId, pageSize)
    ↓
LatticeCursorGrain resolves which shard(s) to consult next based on LastYieldedKey and Spec
    ↓
For each shard s involved in this page:
    snapshotLeaf = grainFactory.GetGrain<ISnapshotLeafGrain>($"{treeId}/{s}/{coordHash}")
    page = await snapshotLeaf.ScanKeysAsync(effStart, effEnd, ...)   // first call rebuilds via ILeafReplayCoordinatorGrain
    ↓
k-way merge across shards under LatticeRegistrySnapshotContext.BeginScope(coord.RegistrySnapshot)
    ↓
WriteStateAsync (checkpoint LastYieldedKey)
    ↓
IWalCursorRegistry.ReportCursorAsync (slide pin)
    ↓
return LatticeCursorKeysPage

CloseAsync()
    ↓
For every shard: IWalCursorRegistry slide pin to TimeSpan.Zero (unregister)
    ↓
ClearStateAsync + DeactivateOnIdle
```

**Atomicity scope.** Strict isolation against every dimension the live read path is subject to:
* **Saga decisions** - frozen by the captured `registrySnapshot` (inherited from F-064).
* **Foreground non-saga writes** - frozen because the snapshot leaves replay only the WAL prefix `0..capturedOffset_s`; writes that append after the capture are invisible by construction.
* **Range deletes** - same as above; tombstone entries appended after the capture are not in the replay set.
* **Replication apply** - the receiver's WAL is the same WAL the snapshot replays from, so cross-cluster writes that land after the capture are invisible. A snapshot opened against a follower cluster shows the follower's `capturedOffset_s` view; cross-cluster snapshot rendezvous (one HLC barrier across multiple clusters' independent WALs) remains explicitly out of scope.
* **Topology changes (split / reshard / resize)** - the captured `treeMapVersion` pins the routing snapshot. A split that commits after the capture is invisible: the snapshot cursor still routes to the pre-split shard layout because the materialised snapshot leaves rebuilt the pre-split projection. The migrated keys are present on the source shard's snapshot leaf (they were in `Entries` at the captured WAL offset) and absent on the destination shard's snapshot leaf (the destination's WAL had no entry for them at that offset). The k-way merge sees each key exactly once. A reshard that committed after the capture is similarly invisible.

**Cost vs. F-064 isolation.** F-064 pages cost roughly `O(live-fan-out)` per page plus a single registry-snapshot capture at open. F-065 pages add a one-time replay cost at the first page that touches a given shard, then amortise to roughly `O(materialised-fan-out)` per page - comparable to live fan-out once the snapshot leaves are warm. The replay cost is `O(sum(capturedOffset_s))` total, scaling with WAL depth at open time. F-052's `GetMaterialiserLagAsync` is exactly the right knob to size this upfront: `OpenSnapshotCursorAsync` consults it and fails fast with `LatticeSnapshotReplayBudgetExceededException` if the projected replay would exceed `LatticeOptions.MaxSnapshotReplayEntries` (new option, default 10 000 000), giving operators a budget cap analogous to the existing `MaxLeafReplayEntries` on activation-time replay.

**Surface.** Public-additive only:
* `Orleans.Lattice.ILattice.OpenSnapshotCursorAsync(LatticeCursorSpec spec, CancellationToken ct)` - new method, returns opaque cursor id. Distinct from `OpenKeyCursorAsync` / `OpenEntryCursorAsync` because the snapshot variant has a different cost profile, a different failure mode (`WalEntryTrimmedException`), and a different acceptance gate (`MaxSnapshotReplayEntries`); making it a distinct entry point lets the public surface document the trade-offs without overloading `LatticeCursorSpec.PointInTime` from F-064 with a second-tier opt-in. (`OpenSnapshotKeyCursorAsync` / `OpenSnapshotEntryCursorAsync` / `OpenSnapshotDeleteRangeCursorAsync` if the surface needs to mirror the existing trio.)
* `Orleans.Lattice.LatticeSnapshotCoordinate` - readonly record struct (`treeMapVersion`, `perShardWalOffsets`, `registrySnapshotHlc`). Exposed publicly so operators can diagnose snapshot lifetime against `GetMaterialiserLagAsync`.
* `Orleans.Lattice.LatticeSnapshotExpiredException` - thrown by `Next*Async` when the WAL prefix has been trimmed past the captured offset (pin TTL elapsed without slide, or operator forced a trim via `LatticeOptions.WalGc.AggressiveTrim`).
* `Orleans.Lattice.LatticeSnapshotReplayBudgetExceededException` - thrown by `OpenSnapshotCursorAsync` when the projected replay would exceed `MaxSnapshotReplayEntries`.
* `LatticeOptions.MaxSnapshotReplayEntries: long` (default 10 000 000) - per-shard upfront replay-cost cap.
* `LatticeOptions.SnapshotLeafIdleTtl: TimeSpan` (default 30 min) - eviction window for transient snapshot leaves (cheaper than holding all snapshot state for the full `CursorIdleTtl` window; the next `Next*Async` after eviction rebuilds via `ILeafReplayCoordinatorGrain` on demand).

Internal-additive:
* `IShardRootGrain.SnapshotWalHeadAsync(CancellationToken)` - per-shard offset capture.
* `ISnapshotLeafGrain` keyed `{treeId}/{shardIndex}/{coordHash}` - transient per-snapshot leaf, no `IPersistentState`.
* `LatticeCursorState.SnapshotCoordinate: LatticeSnapshotCoordinate?` - new persisted slot (only populated for snapshot cursors).
* `TypeAliases.LatticeSnapshotCoordinate = "ol.lsc"`, `TypeAliases.ISnapshotLeafGrain = "ol.slg"`.

**Observability.**
* `orleans.lattice.snapshot.replay.entries` (new `Histogram<long>`, tagged `tree, shard`) - per-shard replay record count when a snapshot leaf rebuilds. Operators size `MaxSnapshotReplayEntries` against the long tail.
* `orleans.lattice.snapshot.replay.duration` (new `Histogram<double>` ms, tagged `tree, shard`) - per-shard rebuild wall-time.
* `orleans.lattice.snapshot.pin_count` (new `Gauge`, tagged `tree`) - count of active WAL-cursor pins owned by snapshot consumers. Alerts before WAL retention blows up.
* `orleans.lattice.cursor.duration` (existing histogram) gains a `mode={live|frozen-registry|frozen-wal}` tag so all three cursor flavours share one instrument with clean attribution. (`live` = F-033 ✓ baseline, `frozen-registry` = F-064 snapshot, `frozen-wal` = F-065 snapshot.)

**WAL GC interaction.** The snapshot pin is already covered by the existing GC predicate - `LatticeWalGc` trims entries with `entry.Timestamp <= min(reported cursors) AND entry.Timestamp < min(blockedAtHlc)`, both of which the snapshot consumer participates in. No predicate changes are needed. The footprint is bounded by `MaxCursorSnapshotPinTtl` (F-064 option, reused here) - the longest a snapshot can pin WAL retention is the same hard cap a F-064 cursor can pin registry decisions.

**Replication interaction.** Universal - snapshot reads are local to the cluster the cursor was opened against, and they consume the same WAL the replication apply path writes to. No transport-level changes, no new replication-apply DTOs, no per-tree opt-in. A future cross-cluster snapshot rendezvous (synchronised `LatticeSnapshotCoordinate` across two clusters' independent WALs) is out of scope here and would require a new replication-side handshake; the design does not preclude it.

**Acceptance.**
- *Foreground-write isolation.* A fixture opens a snapshot cursor while a continuous writer issues `SetAsync` against keys in the cursor's range at 100 Hz; across 50 trials every page of the snapshot cursor returns the captured-time value for every key it observes, never a post-capture value.
- *Saga isolation (composition with F-064).* A fixture commits a multi-key `SetManyAtomicAsync` saga between two pages of a snapshot cursor; the cursor's view does not change. A control fixture with `SnapshotMode: ZeroObservableWrites = false` (i.e. a F-064 cursor) sees the saga's keys flip from pre-saga to post-saga as expected.
- *Topology-change isolation.* A fixture triggers a 4 → 8 reshard between two pages of a snapshot cursor that touches every shard. The cursor's view does not change: every key is yielded exactly once at the pre-reshard owner.
- *Replay budget gate.* A fixture sets `MaxSnapshotReplayEntries = 1 000` against a tree whose deepest shard's WAL has 5 000 entries since the last checkpoint; `OpenSnapshotCursorAsync` fails with `LatticeSnapshotReplayBudgetExceededException` and no snapshot leaf is materialised.
- *WAL retention pin.* A fixture opens a snapshot cursor, waits for two `LatticeWalGc` cycles, and confirms the WAL prefix below `capturedOffset_s` is intact on every touched shard. A control fixture closes the cursor and confirms the GC trims past the previously-pinned offset on the next cycle.
- *Pin lifetime (three caps, mirrored from F-064).* (a) `CloseAsync` releases the pin within one registry round-trip. (b) An idle snapshot cursor whose host-side TTL exceeds `MaxCursorSnapshotPinTtl` is auto-unpinned by the registry; the next step throws `LatticeSnapshotExpiredException`. (c) A footprint test sets `MaxSnapshotReplayEntries` low and confirms the open-time fail-fast keeps the registry pin footprint bounded.
- *Crash-recovery determinism.* A fixture opens a snapshot cursor, pages twice, kills the silo hosting the snapshot leaves, and on reactivation confirms the next `Next*Async` rebuilds the snapshot leaf and yields the same keys the pre-crash pages would have yielded next (deterministic replay assertion).
- *Snapshot-leaf eviction.* A fixture opens a snapshot cursor, pages once, lets the snapshot leaves idle past `SnapshotLeafIdleTtl`, and confirms the next `Next*Async` transparently rebuilds the snapshot leaf - same yielded keys, same digest, with a fresh `orleans.lattice.snapshot.replay.entries` measurement.
- *Hygiene.* `LatticeCursorState` round-trips through Orleans serialization with the new `SnapshotCoordinate` slot decoding to `null` for legacy persisted state; `ISnapshotLeafGrain` activations carry the same `LatticeMetrics` instruments as `BPlusLeafGrain` for the rebuilt scan paths.

**Out of scope.**
* *Cross-cluster snapshot rendezvous.* Synchronised `LatticeSnapshotCoordinate` across two clusters' independent WALs - requires a new replication-side handshake; a future entry if a real consumer asks for it.
* *Writable snapshots.* A snapshot cursor is strictly read-only. `OpenDeleteRangeCursorAsync` with `SnapshotMode: ZeroObservableWrites` is rejected at `OpenAsync` (a delete-range against a frozen view would either be a no-op or write through to the live tree; both are surprising).
* *Per-cursor WAL retention override.* The snapshot inherits `MaxCursorSnapshotPinTtl` (F-064) as its hard cap. A cursor that needs longer retention rebuilds the snapshot at a fresh coordinate.
* *Live-vs-snapshot diff.* A consumer that wants "what changed since the snapshot" runs a F-064 `PointInTime: false` cursor against the same range; the diff is the caller's problem to compute. F-065 does not expose a diff API.
* *Snapshot reuse across cursors.* Two cursors opened at logically identical coordinates do not share their materialised snapshot leaves - each cursor's `coordHash` is salted by `cursorId`, so two simultaneous "snapshot of tree at offset X" cursors pay independent replay cost. A future entry can add coordinate-keyed sharing if a real consumer asks for it.

---

### F-066 - Caching call-path co-location candidates
**Performance / investigative / memory + CPU (audits the four-layer read cache stack identified in a vertical call-path review and proposes each co-location-friendly elimination as an independently measurable candidate)**

The current read path traverses four distinct caching layers (`BPlusLeafGrain.state.State.Entries` source-of-truth, `LeafCacheGrain._cache` per-silo read-through mirror, `ShardRootGrain._routingTableCache` per-activation routing-snapshot map, and the `ShardRootGrain._cachedLeaf` / `_cachedLeafCache` / `_cachedInternal` single-slot grain-ref LRUs). The vertical audit that produced this entry confirmed each layer is functionally correct and that the same-silo `LeafRevisionRegistry` cookie eliminates the steady-state CPU cost of the `LeafCacheGrain` refresh on the primary silo. It also identified that the *memory* dimension is unmitigated: on the silo hosting the primary leaf, `LeafCacheGrain._cache` is a structural duplicate of `state.State.Entries` (dictionary buckets + key strings duplicated regardless of whether Orleans elides the value-byte copy), and several smaller hot-path elisions remain on the table. This entry catalogues the candidates and the measurement protocol for each; it does **not** stipulate an expected outcome (each must stand or fall on its own measurement).

**Scope.** Investigative; ship only the candidates whose measured delta justifies the carrying cost in code complexity.

**Candidates.**

1. **Co-located cache pass-through.** When the local `LeafCacheGrain` activation can prove the primary `BPlusLeafGrain` is also activated on this silo (the exact condition that `BPlusLeafGrain.TryGetLeafRevision(primaryId, out _)` returns `true` for), serve reads directly from the leaf's `state.State.Entries` (and `_pendingTx`) via an internal cross-grain seam, bypassing the `LeafCacheGrain._cache` dictionary entirely. The seam must preserve the existing pending-key / `IsMigrated=true` delegation semantics by design - i.e. it is a *physical* short-circuit, not a *semantic* one. Implementation surface: add an internal static `BPlusLeafGrain.TryGetLocalReadAccessor(GrainId leafId, out IBPlusLeafLocalReadAccessor accessor)` registered alongside `LeafRevisionRegistry` from `OnActivateAsync` and cleared in `OnDeactivateAsync`; the accessor exposes the same surface as `IBPlusLeafGrain.GetAsync` / `ExistsAsync` / `GetManyAsync` but skips the cross-grain dispatch turn (still a single-threaded read against the leaf's mailbox via the standard grain dispatcher to avoid violating turn semantics). When the accessor is available, `LeafCacheGrain.GetAsync` / `ExistsAsync` / `GetManyAsync` route to it and skip the mirror dictionary maintenance entirely for that call. On the silo whose activation hosts the cache but not the primary leaf, the existing delta-refresh path continues unchanged.

   *Measurement.* Add a `Bench.LeafCacheCoLocation` micro-probe that pins both the primary leaf and the local cache to a single-silo cluster, runs a 1M-`GetAsync` burst against a 10k-entry leaf, and reports four numbers: per-call wall-time p50 / p95, allocations per call (via `ETW` or `EventListener` on `dotnet-counters`), and steady-state working-set delta of the silo process (`Process.WorkingSet64` sampled at the burst's start and end). Compare baseline (current cache mirror) against the co-located pass-through path under the same load. The acceptance threshold is: ship the change only if the working-set delta is materially smaller (the precise threshold is left to the maintainer review of the measurement, not stipulated here) and the per-call wall-time does not regress beyond measurement noise.

2. **`LeafCacheGrain` skip-when-co-located activation gate.** Stronger variant of (1): when `LatticeOptions.CacheTtl == TimeSpan.Zero` *and* the primary leaf is on the same silo *and* no cross-silo readers are observed for the leaf, skip activating the `LeafCacheGrain` for that leaf entirely. Reads route directly to the leaf via `ShardRootGrain._cachedLeaf`. The gate must be conservative: any cross-silo read against a given leaf forces the cache to be activated for the rest of the leaf's lifetime, because the mirror is the only place a cross-silo cache can fall back to once it has been disabled.

   *Measurement.* Same probe as (1), with the additional dimension of `LatticeOptions.CacheTtl` set to zero. Report the same four numbers. Also instrument `LatticeMetrics.CacheHits` / `CacheMisses` and confirm they drop to zero under the gate (verifying the gate engaged) and that the existing `LatticeMetrics.LeafGetDuration` (or equivalent direct-leaf-read histogram) tracks the reads under the gate. Acceptance threshold: maintainer judgement on measured working-set + wall-time deltas; ship only if the gate adds no observable regression on the cross-silo workload covered by the existing `LeafCacheGrainTests` integration fixtures.

3. **Routing-table snapshot cache LRU.** `ShardRootGrain._routingTableCache` currently grows monotonically; it is bounded in theory by tree fanout but has no eviction. For trees with extreme internal-node counts (deep, narrow fanout) or for long-lived shard roots that observe many internal-node rotations through structural ops, the dictionary can drift larger than necessary. Replace the unbounded `Dictionary<GrainId, RoutingTableSnapshot>` with a fixed-capacity LRU (cap is a new `LatticeOptions.MaxRoutingTableCacheEntries`, default 1024). The fanout-bounded theoretical bound makes the cap effectively unreachable on production-shaped trees; the cap exists for pathological workloads and for memory-attribution clarity.

   *Measurement.* Add a `ShardRootGrainTraversalMicrobench` that traverses a synthetic depth-4 tree (~10k internal nodes) under a uniformly random access pattern and reports the per-traversal wall-time p50 / p95 under the unbounded baseline and under each candidate cap value `{256, 1024, 4096}`. Also report the cache's steady-state entry count (the existing `_routingTableCache.Count` field, observed via a debug-only `IShardRootGrain` test seam). Acceptance threshold: ship the cap if the LRU's per-traversal wall-time regression at the chosen cap is below 5% relative to the unbounded baseline. Pick the smallest cap that meets the threshold.

4. **Single-slot grain-ref LRU widening (root-anchored 2-slot).** `ShardRootGrain._cachedInternal` is a single-slot LRU keyed by `GrainId`. The traversal pattern always hits the root first and then steps through one or more level-1 internals, so the slot flips on every traversal in depth-3+ trees. Widen to a 2-slot LRU: slot 0 always holds the root (immutable for the activation's lifetime, established on first use), slot 1 is the most-recently-used non-root. Same invalidation semantics (implicit on GrainId equality mismatch); explicit `InvalidateRoutingTable` already calls a sibling seam that can simultaneously evict the matching grain-ref slot.

   *Measurement.* Reuse the `ShardRootGrainTraversalMicrobench` from candidate 3. Report per-traversal wall-time and per-traversal `IGrainFactory.GetGrain<IBPlusInternalGrain>` call count (count via a custom listener around the factory). Acceptance threshold: ship only if the `GetGrain` call count per traversal drops materially (the measured factor is left to maintainer review) and per-traversal wall-time improves at p50 *and* p95.

5. **Lazy `GetTreeIdAsync` elision.** `LeafCacheGrain.GetCacheTtlAsync` performs a one-time `primaryLeaf.GetTreeIdAsync()` RPC per activation (to derive the per-tree `LatticeOptions` for the `CacheTtl` value). The cache already has the primary leaf's `GrainId`; the tree id is derivable from the grain key without the round-trip if the grain-key encoding exposes it. Add an internal `GrainId.TryParseTreeId(out string)` helper that decodes the embedded tree id from the leaf grain key directly and consult it from the cache instead of the RPC. The RPC is a one-shot, so the *steady-state* CPU win is zero; the win is one fewer cross-grain hop on first activation per cache, which manifests as cold-start latency reduction.

   *Measurement.* Add a `LeafCacheGrainColdStartMicrobench` that runs 1000 first-`GetAsync` calls across freshly-activated cache grains and reports the p50 / p95 / p99 first-call latency. Acceptance threshold: ship only if the p99 drops materially below the baseline (a cold-start tail reduction); steady-state latencies should be unchanged within noise.

**Out of scope.**
- *Cache LRU / eviction.* Tracked separately as F-067 below; that entry has different acceptance criteria because the candidate has Orleans-semantics interactions that this entry's purely-local candidates do not.
- *Cache write-through / write-back.* The current read-through-with-delta-refresh design has no coherence protocol to maintain; introducing one would be a separate feature, not a co-location optimisation.
- *Per-call positive-result negative-cache.* Adding a "saw the miss this turn" flag inside a single `GetAsync` is an in-method optimisation, not a caching-layer change; if measurement under candidate (1) or (2) surfaces it as the dominant cost, address it under a separate entry.
- *Cross-silo cache coordination / push invalidation.* Pull-based revision-cookie + cursor delta refresh is the current contract; any push-based change would alter wire surface and is out of scope for an optimisation entry.

**Acceptance.**
- Each candidate has a self-contained micro-probe under `benchmark/host/` (one per candidate) that produces the numbers stipulated above. The probes do not need to be solution-registered; they follow the `Bench.WalAzureTable` shape (standalone, run manually, gitignored solution-file edits).
- The micro-probes are committed alongside the candidate they motivate, even if a candidate is ultimately not shipped, so that a future re-investigation has the measurement harness ready.
- For every candidate that *is* shipped, the corresponding probe's numbers (baseline vs. shipped) are captured in the PR description so the next reviewer can re-run the probe and reproduce the gain.
- No candidate ships without its probe demonstrating the threshold described in its `Measurement` paragraph; "looks faster locally" is not acceptance.
- The four-layer read cache stack table in this entry's preamble is reproduced (or linked) in `docs/lattice/caching.md` so the architecture-level picture stays a discoverable single source-of-truth.

**Risk surface.** Candidate (1) introduces a new "co-located accessor" seam on `BPlusLeafGrain` that bypasses the normal grain-dispatch envelope. Orleans turn-isolation guarantees must be preserved (the accessor must dispatch through the leaf's mailbox, not directly into `state.State.Entries` from an arbitrary thread). Candidate (2) is conditional on the absence of cross-silo readers; the gate's "promote to activated once a cross-silo reader is observed" promotion must be one-way (no demotion) to avoid an oscillation under bursty traffic. Candidates (3) - (5) are purely-local refactors with no Orleans-semantics interaction.

---

### F-067 - Bound `LeafCacheGrain._cache` size without violating Orleans semantics
**Performance / memory / investigative (cap the per-activation read-through cache mirror to a configured size, *only* if an eviction policy can be defined that does not interact with the delta-refresh / pending-key / moved-away protocols)**

The `LeafCacheGrain._cache` dictionary mirrors the primary leaf's live entry set 1:1 and grows monotonically over the cache activation's lifetime (only split, moved-away, and epoch-flip events shrink it). Per-silo per-tree memory therefore scales linearly with the touched-leaf-set's combined entry count, with no operator-side cap. This entry tracks the work to bound that footprint - **but only if** the eviction policy can be designed to leave the four existing correctness contracts intact:

1. **Delta-refresh cursor contract.** `LeafCacheGrain._deliveryCursor` is the activation-scoped pair `(Epoch, Sequence)` against which the leaf decides which entries to ship in the next `GetDeltaSinceCursorAsync` call. Evicting a key from `_cache` does *not* rewind the cursor; the next refresh will not re-deliver the evicted key unless its per-key sequence is bumped on the leaf side. An eviction protocol that does not account for this would silently produce false-misses on the next read of an evicted key.

2. **Pending-key set contract.** `_pendingKeys` is refreshed every time the cache takes the cross-grain refresh path. Evicting a key whose row is in `_pendingKeys` is safe (the delegation path runs anyway), but evicting *and then* observing the same refresh-path miss must not create a window where the cache fails to delegate a pending key.

3. **Moved-away pruning contract.** `_movedAwaySlots` is the cumulative set of virtual slots the primary leaf has reported as migrated. A key whose slot is in this set must surface `StaleShardRoutingException`. Evicting cached entries inside a moved-away slot is the existing behaviour (`keysToRemoveMoved` loop in `RefreshAsync`); evicting cached entries *outside* a moved-away slot must not interact with the slot bitmap.

4. **Migrated-entry delegation contract.** A cached row with `IsMigrated = true` delegates reads to the primary leaf. Evicting such a row is correctness-preserving (the next read takes the delegation path on a miss), but evicting it *and* the per-key sequence not being re-delivered on the next refresh would lose the `IsMigrated` flag and allow the cache to start serving the value directly, bypassing the shadow guard.

**Investigation.** Determine, before committing to ship anything:

1. Whether the leaf's per-key sequence-bump protocol can be extended so that an evicted-and-re-fetched key is correctly re-delivered to the cache on the next refresh, **without** requiring the cache to issue a per-key targeted fetch (which would defeat the point of the delta protocol).
2. Whether a "negative-LRU" approach - retain only the row's metadata (`Timestamp`, `IsTombstone`, `IsMigrated`, `IsExpired` ticks) and evict only the value-`byte[]` payload - preserves all four contracts while still shrinking the dominant memory cost. The metadata is bounded (≤ ~80 bytes per row including the LWW envelope); the `byte[]` payload is the unbounded dimension. If this hybrid works, a sub-megabyte budget per cache activation may be reachable.
3. Whether eviction must be coordinated with the leaf (e.g. via a cache-side `IBPlusLeafGrain.NotifyCacheEvictedAsync(IReadOnlyList<string> keys)` hint so the leaf re-publishes those keys' per-key sequences on the next state advance) or can remain purely cache-local. A coordinated approach changes the wire surface; a cache-local approach must prove correctness through the four contracts above.

**Candidate eviction policies to evaluate (each must clear the four-contract gate above).**

1. *Value-payload-only LRU (metadata-retained).* Cap the total `_cache[key].Value.Length` aggregate at a configured byte budget. Evict the `Value` byte array of the least-recently-read row while retaining the `LwwValue<byte[]>` envelope with `Value = null`. Reads against an envelope-only row miss the cache and go to the leaf; the leaf's existing per-key delivery path repopulates the row on the next refresh.
2. *Full-row LRU (envelope + value).* Cap the total `_cache.Count` at a configured entry count. Evict the least-recently-read row entirely. Must clear contract (1) above - the next refresh must re-deliver the evicted key.
3. *Memory-pressure-driven eviction.* Subscribe to `GC.RegisterForFullGCNotification` or equivalent; drop a configured fraction of the cache on full-GC trigger. Cheapest to implement; harder to reason about because the eviction set is observation-dependent.

**Measurement protocol.**
- Build a `Bench.LeafCacheGrowth` standalone probe (same `benchmark/host/` shape as `Bench.WalAzureTable`) that activates a single `LeafCacheGrain` against a primary leaf hosting `{1k, 10k, 100k}` entries with average value size `{64 B, 1 KB, 64 KB}`, runs a uniform-random read workload for a fixed duration, and reports:
  - `Process.WorkingSet64` and `GC.GetTotalMemory(forceFullCollection: false)` sampled at 1-second cadence;
  - `_cache.Count` and `sum(_cache[k].Value.Length)` sampled at the same cadence (via a debug-only test seam);
  - per-read p50 / p99 latency.
- Run the probe under the baseline (unbounded cache) and under each candidate policy at each value-size point.
- For each candidate, derive the marginal cost (per-read latency increase, per-read miss-rate-induced refresh load) per unit memory saved.

**Acceptance.**
- This entry ships **only** if at least one candidate clears the four-contract correctness gate AND the measurement protocol demonstrates a worthwhile memory / latency tradeoff at production-shaped scales. If no candidate clears the gate, the entry is closed with `[x]` and a written justification of which contract each candidate violated; the unbounded behaviour is then documented in `docs/lattice/caching.md` as an intentional design property.
- The chosen policy's `LatticeOptions.MaxCacheMemoryBytes` (or `MaxCacheEntries`, depending on which candidate ships) is documented in `docs/lattice/configuration.md` with an explicit "default = unbounded; set to bound the per-silo per-leaf memory footprint" note.
- A new chaos test under `test/lattice/BPlusTree/Grains/LeafCacheGrainTests.Eviction.cs` exercises the chosen policy under concurrent split, moved-away, migration, and pending-tx events and confirms strict semantic equivalence to the unbounded baseline across each of the four contracts.
- A regression test pins the per-activation memory budget so that a future refactor that re-introduces unbounded growth is caught at CI time.

**Out of scope.**
- Cross-silo cache coordination / shared LRU across silos (would alter the `[StatelessWorker]` design contract).
- Push-based invalidation from the leaf to its caches (would alter the leaf's per-write cost envelope; the existing pull-with-cookie design is the contract).
- Eviction of the routing-table snapshot cache - that is candidate (3) of F-066.
- Bounding the `_pendingKeys` set - it is bounded by in-flight saga count, which is already capped by the per-tree `TxRegistryGrain` retention envelope (F-055 / F-064 envelopes).

**Risk surface.** Any policy that does not preserve all four correctness contracts must not ship. The `LeafCacheGrain` is the single most-touched grain on the read path; a regression here would surface as silent stale reads or violation of strict-isolation guarantees the F-055 / F-064 / F-065 contracts already deliver. The investigation can produce a "no candidate clears the gate" outcome and that outcome is an acceptable closure of this entry.

---

### F-068 - Order-independent WAL provider registration *(shipped)*

`ISiloBuilder.AddWalStorage(factory)` previously used `TryAddSingleton` for both the no-factory baseline path and the host-supplied factory path. Because `AddLattice` self-registers the in-memory baseline as part of its own setup, any later host call to `AddWalStorage(factory)` (or to a package-level overload that wraps it, such as `AddAzureTableWalStorage`) silently no-opped: the baseline had already won the `TryAdd` race. The host saw a successful build, the silo ran on `InMemoryWalStorageProvider`, and the durable backend the host configured was never touched. This was a footgun specifically because the failure mode is silent - no log line, no exception, no telemetry difference until a host operator notices the durable store is empty.

**Fix.** `AddWalStorage(factory)` now registers via `Services.Replace(ServiceDescriptor.Singleton<IWalStorageProvider>(factory))`. The no-factory baseline path keeps `TryAddSingleton` so `AddLattice`'s self-call does not displace a real provider registered earlier by the host. Net effect: the host's choice of WAL provider is order-independent with respect to `AddLattice`, and last-call-wins applies when multiple factory registrations are stacked. The `AddAzureTableWalStorage` extension inherits the new contract automatically because it forwards through `AddWalStorage(factory)`.

**Acceptance.** Pinned by `LatticeServiceCollectionExtensionsTests` (four cases: baseline-then-baseline idempotency, baseline-then-factory replacement, factory-then-baseline preservation, factory-then-factory last-call-wins) and by `LatticeAzureTableServiceCollectionExtensionsTests.AddAzureTableWalStorage_replaces_prior_in_memory_baseline` / `AddAzureTableWalStorage_wins_when_called_before_baseline`. The XML docs on `AddLattice`, `AddWalStorage`, and `AddAzureTableWalStorage`, plus `docs/lattice/wal-storage-providers.md`, document the new contract explicitly so the failure mode the fix prevents is also visible to a reader scanning the API surface.

---

### F-069 - Batched WAL append on the leaf write path *(candidate 2 shipped)*
**Performance / write throughput / investigative (collapse per-key WAL round-trips on `SetManyAsync` / `DeleteRangeAsync` / saga-prepare into a single batched append per leaf, where the underlying `IWalStorageProvider` can express batched durability semantics)**

`BPlusLeafGrain.SetManyAsync` currently iterates `entries` and calls `SetAsync` per key (see `src/lattice/BPlusTree/Grains/BPlusLeafGrain.cs`, the per-key loop around the leaf's bulk-write entry point). Each iteration runs the full per-key commit pipeline: HLC tick, mutation envelope construction, `await writer.AppendAsync(mutation)`, in-memory projection apply, version-advance publish, and digest publication. For a WAL provider whose `AppendAsync` is a network round-trip (Azure Table, future SQL/Cosmos providers, future cross-region replication ship), the per-call latency dominates - a 16-key batch costs 16 sequential HTTP round-trips against the WAL even though the leaf already has the whole batch in hand.

The measurement that motivates this entry is the Azure throughput harness run on 2025-11-XX against `bench/azure-throughput/` with `BatchSize=256`, `FlushIntervalMs=50`, and a single silo backed by `AzureTableWalStorageProvider`: a producer sending at ~5,000 msg/s saturated the silo's input channel in ~19s, after which the consumer steady-state throughput was **728 commits/s** averaged over 136.3s (99,000 events / 136.3s). The same harness against the in-memory WAL ceiling (no network) is constrained only by Orleans grain RPS and is materially higher. The candidate work catalogued below identifies the per-leaf batched-append seam(s) that could close that gap.

**Scope.** Investigative; ship only the candidates whose measured delta justifies the carrying cost in code complexity and the additional WAL-provider contract surface. The shape mirrors F-066: each candidate has its own micro-probe and its own acceptance threshold; "looks faster locally" is not acceptance.

**Candidates.**

1. **`IWalStorageProvider.AppendManyAsync` seam.** Add an optional batched-append method to the WAL storage provider contract (`IWalStorageProvider`) with a default-interface implementation that falls back to per-mutation `AppendAsync` so existing providers (`InMemoryWalStorageProvider` and any out-of-tree third-party providers) remain wire-compatible. Override in `AzureTableWalStorageProvider` to pack the batch into a Table `TransactionalBatch` (Azure Tables supports up to 100 ops per partition per batch, with all rows sharing the same `PartitionKey`). The leaf's per-shard partitioning means a single `SetManyAsync` call already shares one WAL `PartitionKey` per shard, so the batching is a natural fit.

   *Measurement.* Build a `Bench.WalAppendMany` standalone probe (same `benchmark/host/` shape as `Bench.WalAzureTable`) that runs a fixed 100,000-mutation workload through `IWalStorageProvider` against `{InMemoryWalStorageProvider, AzureTableWalStorageProvider}` at batch sizes `{1, 8, 16, 64, 256}` and reports per-mutation p50 / p95 / p99 latency and aggregate throughput. Acceptance threshold: ship the Azure override only if the 16+ batch sizes show a wall-clock improvement that exceeds the additional code complexity (`TransactionalBatch` failure-handling, partial-batch rollback semantics, the 100-op-per-batch ceiling, the same-PartitionKey constraint, and the seam's surface area on the `IWalStorageProvider` contract). The in-memory override is shipped or not on its own measurement - it may turn out that per-mutation `AppendAsync` against an in-memory list is already at the noise floor.

2. **`BPlusLeafGrain.SetManyAsync` consumer of the batched seam.** Rewrite the per-leaf bulk-write path to construct the full mutation list up-front (one HLC tick advancement per key, but a single batched `AppendAsync` / `AppendManyAsync` call), then apply all entries to the in-memory projection in a single pass, with a single digest-publish at the end. The current per-key projection apply is fast (it's a dictionary insert plus an LWW merge); the saving is in the WAL round-trip and the digest-publish fanout, not in the per-key apply. The leaf's `SplitResult?` return contract must be preserved - if any entry causes a split, the batched path must surface the same `SplitResult` the per-key path would have surfaced. The exact split-handling shape (split mid-batch, then continue the remaining entries against the post-split state, vs. apply all entries against the pre-split state and report only the final split) is a design decision to be settled by the measurement and a written justification in the PR description.

   *Measurement.* Extend `Bench.WalAppendMany` (or add a sibling `Bench.LeafSetMany`) that drives `BPlusLeafGrain.SetManyAsync` directly (unit-test-shape, with `FakePersistentState<LeafNodeState>` and the WAL provider under test) and reports per-key p50 / p95 latency, allocations per call, and aggregate throughput at the same batch sizes. Compare baseline (current per-key loop) against the batched implementation. Acceptance threshold: ship only if per-key wall-time drops materially at batch sizes >= 16 *and* the in-memory projection's correctness is preserved by a regression test (set 16 keys, read 16 keys, every value matches; set 16 keys where 8 are tombstoned, read each, every entry's `IsTombstone` matches).

3. **`ShardRootGrain.SetManyAsync` per-leaf grouping.** Today `ShardRootGrain.SetManyAsync` already groups the input by routed-leaf and forwards each leaf's slice in a single call (see `src/lattice/BPlusTree/Grains/ShardRootGrain.cs`, the entry point around the per-leaf forwarding loop). The grouping itself is correct; the candidate is to confirm that the grouping is the dominant cost (vs. the per-leaf RPC) and, if not, to identify the seam where multiple leaves on the same silo can be addressed in a single grain dispatch envelope. The Orleans grain model does not natively support multi-grain dispatch, so this candidate may close without shipping; the measurement is the deciding factor.

   *Measurement.* Add a `Bench.ShardRootSetMany` probe that drives `ShardRootGrain.SetManyAsync` against `{1, 4, 16, 64}` leaves with batches of `{16, 64, 256}` keys per leaf and reports per-leaf wall-time, per-batch wall-time, and the ratio of (per-leaf RPC count : per-batch RPC count). If the per-leaf RPC count is already 1 per leaf per call (it should be, given the existing grouping), this candidate closes with `[x]` and a written justification; if not, the measurement surfaces the gap.

4. **Saga-prepare batched append.** The F-055 saga-prepare path appends prepared mutations through the same `BPlusLeafGrain.SetCoreAsync` / `DeleteAsync` per-key entry points. A saga touching 16 keys on the same leaf appends 16 prepared mutations sequentially. If candidate 1 ships, candidate 2 should be extended to cover the prepare path so that saga throughput inherits the same batching gain. Saga-terminal appends (`AppendTxTerminalAsync`) are already single-mutation-per-shard and do not benefit from this candidate.

   *Measurement.* Run the existing F-055 throughput-parity benchmark (`Bench.Microbench` `SetManyAtomic_4Shards` and `SetManyAtomic_Concurrent`) before and after the batched-prepare change at `BENCH_MICROBENCH_FIDELITY=quick`. Acceptance threshold: ship only if the `SetManyAtomic_4Shards` mean drops materially at the same n=3 ShortRun fidelity, and the saga's continuous-reader invariant (the F-055 fixture under `test/lattice/BPlusTree/AtomicWrite/`) remains green across 50 repetitions.

**Out of scope.**
- *Cross-leaf batching across shard boundaries.* The WAL is partitioned by shard; a batch spanning multiple shards must split into per-shard sub-batches by construction. Cross-shard atomic semantics are F-055's province, not this entry's.
- *Cross-cluster replication ship batching.* R-080 / R-081 already ship mutation envelopes one-by-one. Batching on the ship side is a separate replication entry; the batched-WAL-append work here is strictly source-side commit-path.
- *Compaction / GC batching.* The WAL GC predicate (R-083) runs on its own cadence and is not on the hot write path.
- *Bulk-load API surface.* A future "bulk import" API that bypasses LWW semantics and writes directly to the leaf's projection (e.g. for the F-021 migration guide) is its own feature; the candidate here strictly preserves LWW and per-mutation WAL durability.

**Acceptance.**
- Each candidate has its own micro-probe under `benchmark/host/` and a written summary of baseline vs. shipped numbers in the PR description.
- The `IWalStorageProvider` contract change in candidate 1 is additive (default-interface fallback to per-mutation `AppendAsync`) so existing providers remain wire-compatible. A regression test confirms the default fallback is functionally equivalent to the per-mutation loop for a provider that does not override `AppendManyAsync`.
- A new test under `test/lattice/BPlusTree/Grains/BPlusLeafGrainTests.SetMany.cs` (or extension of the existing `SetMany` partial) confirms that `BPlusLeafGrain.SetManyAsync` against `{1, 16, 256}`-key batches produces a projection identical to the per-key loop for the same inputs, including split-mid-batch behaviour.
- A new test under `test/lattice.storage.azuretable/` confirms the `TransactionalBatch` failure modes (over-100-op batch, mixed-partition batch, mid-batch ETag conflict) are surfaced to the leaf in a way that preserves the existing per-mutation error contract.
- The producer-side Azure throughput harness (`bench/azure-throughput/`) is re-run at the same producer-rate ladder before and after the change and the new steady-state consumer ceiling is captured in the PR description.

**Risk surface.** Candidate 1 widens the `IWalStorageProvider` contract surface; a default-interface implementation keeps the wire compatible but third-party providers may want to override the new method for non-Azure backends with native batch support, so the seam shape must be reviewable. Candidate 2 must preserve the leaf's split-mid-batch contract; a regression here would corrupt the leaf's invariant that the projection reflects every successfully-appended mutation. Candidate 3 may close without shipping if measurement shows the grouping is already optimal. Candidate 4 must preserve the F-055 continuous-reader invariant; the saga-prepare batched append must be atomically-visible at the per-shard linearization point exactly as the per-key prepare path is today.

**Status (candidate 2 shipped).** The leaf-side consumer of the batched seam landed without first widening the `IWalStorageProvider` contract (candidate 1). The seam was placed one layer higher, where it pays off independently of provider-side batch support:

- New `ICommitLogWriter.AppendManyAsync(IReadOnlyList<LatticeMutation>, CancellationToken)` on the leaf-side commit-log writer. The default `WalCommitLogWriter` groups the input batch by WAL partition and dispatches one `IWalShardGrain.AppendBatchAsync` call per touched partition, reassembling the dense per-input offsets in input order.
- New `IWalShardGrain.AppendBatchAsync(IReadOnlyList<WalRecord>, CancellationToken)` on the per-shard WAL grain. The whole input batch coalesces into a single provider flush when it fits inside `WalMaxBatchEntries` / `WalMaxBatchBytes`; over-budget batches cut over across multiple flushes using the same in-flight cap as `AppendAsync`.
- Three leaf entry points now consume the batched seam: `BPlusLeafGrain.SetManyAsync` (foreground bulk write), `BPlusLeafGrain.MergeEntriesAsync` (sibling redistribute, snapshot restore, replication-apply, and the bulk-load topology assembly invoked by `ShardRootGrain.BulkLoadAsync` / `BulkLoadRawAsync` / `BulkAppendAsync`), and `BPlusLeafGrain.MergeManyAsync` (cross-shard migration on shard split and online-reshard). For an N-key batch routed to a single WAL partition the grain-hop count drops from O(N) to one.

Candidate 1 (provider-side `AppendManyAsync` on `IWalStorageProvider`) remains open: the existing provider contract already exposes `AppendBatchAsync` / `AppendEncodedBatchAsync` and the WAL grain already coalesces an under-cap batch into one provider call, so the provider-side widening is a separate question of whether `AzureTableWalStorageProvider` can pack a multi-record `TransactionalBatch` instead of looping per record inside `AppendEncodedBatchAsync`. Candidates 3 (shard-root grouping) and 4 (saga-prepare) remain open under their original acceptance thresholds.

The `Bench.WalAppendMany` / `Bench.LeafSetMany` measurement probes called for in the candidate-1 and candidate-2 acceptance criteria were not built for this ship; the regression coverage is the test-level invariants (`AppendManyCallCount == 1` per batched dispatch on the three consumer paths, projection identity against the per-key loop, dense offsets in input order, asymmetric-guard filtering before the WAL append on the migration path). The end-to-end Azure throughput re-run from the candidate-2 acceptance list is gated on candidate 1 shipping (it is the producer-side measurement, not the leaf-side measurement) and is deferred with candidate 1.

---

### F-070 - Pipelined phase-2 commit on the Azure Table WAL provider *(shipped)*
**Performance / write latency / opt-in (overlap the next batch's phase-0+1 with the previous batch's phase-2 manifest commit on `AzureTableWalStorageProvider`, behind a default-off options flag, without weakening durability, ordering, or recovery semantics)**

`AzureTableWalStorageProvider` commits each appended batch in three phases: phase 0 stamps a candidate-row in the shard's manifest partition, phase 1 writes the entry rows in a single Tables transaction against the batch's own partition, and phase 2 commits the manifest row + advances `TAIL` against the manifest partition (serialised through the per-shard `PhaseTwoWorker` so manifest rows land in strict ascending start-offset order). The default `AppendBatchAsync` / `AppendEncodedBatchAsync` path waits for the *current* batch's phase-2 to reach the worker's terminal state before returning, so the caller's wall-clock cost is `phase-0 + phase-1 + phase-2`. Phase 2 is itself a network round-trip against Azure Tables, and on the Azure throughput harness it is the dominant component of that wall-clock at large `BatchSize` values - the manifest partition serialises one HTTP round-trip per batch regardless of how many entry rows phase 1 wrote.

This entry adds an opt-in **pipelined phase-2** mode that returns the caller as soon as the *previous* batch's phase-2 commits, while the current batch's phase-2 continues asynchronously through the same per-shard worker. Phases 0 and 1 remain synchronous and durable on every call, so the durable state machine the activation-time reconciler walks (`AzureTableWalStorageProvider.Reconcile.cs`) is unchanged: a crash at any point still leaves either (a) a candidate-row whose start-offset contiguously extends `TAIL` (rolled forward) or (b) a candidate-row above a gap (rolled back). Pipelining only changes *when the caller of the current `AppendBatchAsync` learns that phase-2 succeeded*; it does not relax atomicity, ordering, or recovery, and the per-shard worker continues to commit manifest rows in strict ascending start-offset order so a concurrent reader of the manifest never observes a non-monotonic `TAIL`.

**Scope.**

1. **Default-off options flag.** `AzureTableWalStorageOptions.PipelinePhaseTwoCommits` (default `false`). The provider's slot bookkeeping (`_pipelinedPhaseTwoTasks`) is allocated unconditionally so toggling the flag at host start is a behavioural switch only, not a wiring switch.
2. **Per-shard slot.** A `ConcurrentDictionary<string, Task>` keyed by manifest-partition-key holds the previous batch's phase-2 task per shard. `AwaitPreviousPipelinedAsync` swaps the current batch's phase-2 task into the slot and awaits the displaced occupant under a `Task.WaitAsync(CancellationToken)` so the caller's `cancellationToken` cancels the *wait* without disturbing the predecessor task itself (the predecessor remains observable to other consumers - the next call, the fault handler, and `DisposeAsync`).
3. **Quiescent-shard fault observer.** `AzureTableWalStorageOptions.PipelinedPhaseTwoFaultHandler` (`Action<Exception>?`, default `null`). When non-null, every slot occupant gets a fire-and-forget `OnlyOnFaulted | DenyChildAttach` continuation that invokes the handler exactly once per faulted phase-2 task on a thread-pool thread with `ExecutionContext` flow suppressed (so no `AsyncLocal` state leaks into the observer). The continuation closes the otherwise-silent failure mode where the *last* batch on a shard faults in phase 2 and no successor `AppendBatchAsync` ever arrives to observe it. Handler exceptions are swallowed so a misbehaving observer cannot corrupt the pipeline's task graph; the handler is never on any append's request path.
4. **Cancellation semantics.** `AppendBatchAsync` / `AppendEncodedBatchAsync` thread the caller's `CancellationToken` through `DispatchPhaseTwoAsync` and into `AwaitPreviousPipelinedAsync` via `Task.WaitAsync(CancellationToken)`. The phase-2 commit itself is not cancellable (the `PhaseTwoWorker` owns it); cancellation only releases the caller's blocking wait. In default mode this cancels the wait on the call's own phase-2 task; in pipelined mode it cancels the wait on the previous batch's phase-2 without touching the worker.
5. **Disposal drain.** `DisposeAsync` snapshots `_pipelinedPhaseTwoTasks` and awaits each occupant under a try/swallow because the canonical surface for those faults is the next `AppendBatchAsync` on the shard (which can no longer be issued, the provider being disposed), and the data is recoverable on next activation through the existing reconciler. Hosts that need a fault signal at shutdown register the fault handler.

**Out of scope.**
- *Cross-shard pipelining.* Phase 2 is partitioned by shard (one slot per manifest partition); cross-shard ordering is not affected and is not relaxed.
- *Phase-1 pipelining.* Phase 1 is the durable entry-row write; pipelining it would let a producer return before its data is durable. Not in scope; the durability boundary stays at phase 1.
- *Replication-side ship pipelining.* R-080 / R-081 ship envelopes against their own ordering invariants; this entry is strictly source-side WAL commit-path.

**Acceptance.**
- `AzureTableWalStorageOptions.PipelinePhaseTwoCommits = false` is the default and reproduces pre-pipelining wall-clock and failure-surfacing behaviour exactly (every caller observes its own phase-2 fault).
- `PipelinePhaseTwoCommits = true` returns the caller after the *previous* batch's phase-2 lands; the slot bookkeeping enforces strict per-shard succession (no two phase-2 commits on the same shard land out of start-offset order).
- White-box tests under `test/lattice.storage.azuretable/AzureTableWalStorageProviderTests.PhaseTwoPipelining.cs` cover: default value, single-shard succession, propagation of a previous-batch fault to the next caller, per-shard slot isolation across distinct shards, two-append chaining through the slot, `DisposeAsync` drain (including faulted occupants), the quiescent-last-batch fault-handler invocation, handler non-invocation on success, handler-exception swallowing, and the cancellation-token seam (caller's wait cancelled, predecessor task untouched).
- The recovery semantics are unchanged: existing `AzureTableWalStorageProvider` reconciler tests pass without modification, and the `PhaseTwoWorker` invariants (strict ascending start-offset ordering, manifest-row coalescing up to 49 rows, monotonic `TAIL` advance, fault propagation to all enqueued completions) are preserved verbatim.
- Documentation in `docs/lattice/wal-storage-providers.md` describes the throughput-vs-latency tradeoff, the registration shape for `PipelinedPhaseTwoFaultHandler`, the cancellation semantics, and the per-call comparison table (default vs pipelined).

**Risk surface.** A faulty implementation of the pipelining slot could (a) break per-shard phase-2 ordering by allowing the worker to commit out-of-order, (b) drop a fault on the floor by losing the slot occupant before any observer chains a continuation, or (c) leak the slot's `Task` graph by allowing handler exceptions to corrupt the continuation chain. Each is closed by an explicit invariant: ordering is owned by `PhaseTwoWorker` (the slot only changes *when callers learn of completion*, not the order of completion itself); fault observability is closed by the optional handler and the per-call successor surfacing; handler exceptions are swallowed inside the continuation. The cancellation seam uses `Task.WaitAsync(CancellationToken)` which produces a *new* awaitable - it does not cancel the underlying predecessor task and cannot orphan or fault it.

**Status (shipped).** All scope items above are in `src/lattice.storage.azuretable/AzureTableWalStorageProvider.cs` (phase-2 dispatch, slot bookkeeping, fault-observer continuation, cancellation-aware wait, disposal drain) and `AzureTableWalStorageOptions.cs` (`PipelinePhaseTwoCommits`, `PipelinedPhaseTwoFaultHandler`). Tests pass in 15/15 (`PhaseTwoPipelining`) and 161/161 (full storage non-`Chaos` suite). A local single-silo Azurite sweep (current-state-no-replication, 30 s window) showed the win regime is bounded by backend write concurrency: ~+2 % commits/s well below the calibrated saturation knee, ~+11 % commits/s and ~-36 % WAL-append-p99 near the knee, and a ~-16 % commits/s and ~+45 % WAL-append-p99 regression past the knee on a single-writer backend. The end-to-end Azure throughput harness measurement that originally motivated the work showed a large steady-state consumer-throughput improvement at `BatchSize=4096` with pipelining on; the exact numbers are not pinned to this roadmap entry because the harness is a producer-rate-vs-consumer-ceiling sweep whose absolute numbers depend on the deploy region and the Azurite-vs-real-Tables target. The "when to enable" section in `docs/lattice/wal-storage-providers.md` documents the saturation-sensitivity contour so operators can validate the knob against their own workload before flipping it on.

---

### F-071 ✓ shipped - Compaction policy controls and telemetry *(closes G-002)*
**Operability / observability / opt-in (give operators ratio- and size-based triggers for tombstone compaction, surface space-amplification and pass-level telemetry on the `orleans.lattice` meter, and extend the Grafana Overview dashboard with a dedicated compaction row)**

`TombstoneCompactionGrain` ships a single configurable knob today: `LatticeOptions.TombstoneGracePeriod` (default 24h, `Timeout.InfiniteTimeSpan` to disable). Its reminder fires every grace period and the resulting pass walks every shard, calling `BPlusLeafGrain.CompactTombstonesAsync(gracePeriod)` on every leaf in shard order, which scans `Entries` and reaps tombstones whose `WallClockTicks` is past the cutoff plus expired-TTL live entries past the same cutoff. Three operational gaps fall out of this shape:

1. **No volume-based trigger.** A bursty delete workload that produces a large tombstone backlog inside a single grace window has to wait until the full period elapses before any reap happens, even when the tombstone-to-live ratio on a shard has already passed any reasonable threshold. The pass is purely time-driven.
2. **No size-based safety valve.** A leaf accumulating tombstones up to (but never quite reaching) the leaf-split threshold inflates per-write cost (every `Apply` walks a larger `Entries`) and inflates WAL replay distance on activation, but compaction does not pre-empt that pressure - it strictly waits for the next reminder tick.
3. **Insufficient telemetry for tuning.** The existing instruments (`leaf.compaction.duration`, `leaf.tombstones.reaped`, `leaf.tombstones.expired`, `leaf.tombstones.created`, `coordinator.completed{kind=compaction}`) report _post-hoc per-leaf throughput_ but not the _decision inputs_: how many leaves the pass actually visited, how many it skipped because nothing had changed since the last pass (the existing `LastCompactionVersion.DominatesOrEquals(Version)` short-circuit), how many shards retried or were skipped after retry exhaustion, the wall-clock of a full pass, and per-leaf space-amplification ratios. Operators tuning the grace period or sizing capacity have no signal beyond reap counts to work from.

This entry adds the missing **policy controls** (ratio-based and size-based pre-emptive compaction triggers), the missing **telemetry surface** (pass-level counters and histograms, plus a per-leaf tombstone-ratio gauge usable from the F-029 instrument catalog), and a corresponding **Overview dashboard row** that visualises the new signals alongside the existing tombstone-churn and compaction-duration panels. All scope is strictly additive: the default behaviour matches v3.4.0 exactly (time-driven reminder, no per-shard pre-emption, no new tags on existing instruments), and every new option defaults to "off" so operators opt in deliberately.

**Scope.**

1. **Ratio-based pre-emptive trigger.** New `LatticeOptions.MinTombstoneRatioForCompaction` (`double`, default `0.0` = disabled). When `> 0`, `BPlusLeafGrain.CompactTombstonesAsync` is no longer the only entry point - `BPlusLeafGrain` records `Tombstones / max(LiveKeys + Tombstones, 1)` on the existing `LeafStats` accounting and, when a write commit pushes the ratio above the threshold, emits a `LatticeTreeEventKind.CompactionTriggered` event (additive enum value) carrying the leaf id and observed ratio. `TombstoneCompactionGrain` subscribes via the existing `IMutationObserver` event surface, dedups by `(treeId, leafId)` for a configurable cooldown (next item), and enqueues an out-of-cycle compaction pass scoped to the offending shard. The trigger is best-effort: if a regular reminder pass is already in flight on the same shard the request is dropped (the in-flight pass observes the elevated ratio organically).
2. **Size-based pre-emptive trigger.** New `LatticeOptions.MaxLeafEntriesBeforeForcedCompaction` (`int`, default `0` = disabled). Same trigger plumbing as ratio, but the predicate is `Entries.Count > threshold && Tombstones > 0` evaluated on the leaf's own commit path. Distinct from `MinTombstoneRatioForCompaction` so a small leaf at 50 % tombstones can be reaped while a large leaf at 5 % tombstones can also be reaped (without conflating the two).
3. **Trigger cooldown.** New `LatticeOptions.CompactionTriggerCooldown` (`TimeSpan`, default `TimeSpan.FromMinutes(5)`). The minimum interval between out-of-cycle compaction passes for the same `(treeId, shardIndex)`, enforced inside `TombstoneCompactionGrain` against a per-shard last-trigger timestamp persisted into `TombstoneCompactionState` (`[Id(5)] Dictionary<int, DateTimeOffset> LastTriggerAt`). Prevents an event-storm from one hot leaf monopolising the compactor's grain timer. The cooldown is independent of the regular reminder cadence; a regular reminder pass always proceeds regardless of the cooldown map.
4. **Pass-level telemetry on `orleans.lattice`.**
   - `orleans.lattice.compaction.pass.duration` - `Histogram<double>` (ms) - duration of a full `RunCompactionPassAsync` invocation, recorded on `CompleteCompactionAsync`. Tagged `tree`, `trigger={reminder|ratio|size}`.
   - `orleans.lattice.compaction.leaves.visited` - `Counter<long>` - leaves whose `CompactTombstonesAsync` returned (count includes short-circuited "no-op" leaves so operators can distinguish work-skipped from work-done). Tagged `tree`, `outcome={reaped|skipped|noop}` where `noop` is the existing `LastCompactionVersion.DominatesOrEquals` fast path, `skipped` is a leaf whose `CompactTombstonesAsync` threw past `MaxRetriesPerShard`, and `reaped` is a leaf that actually removed at least one entry.
   - `orleans.lattice.compaction.shard.retries` - `Counter<long>` - increments once per per-shard retry inside `ProcessNextShardAsync` (independent of how many leaves a shard contains). Tagged `tree`.
   - `orleans.lattice.compaction.shard.skipped` - `Counter<long>` - increments once when a shard exhausts `MaxRetriesPerShard` and the cursor advances. Tagged `tree`.
   - `orleans.lattice.leaf.tombstone.ratio` - `ObservableGauge<double>` - per-leaf snapshot of `Tombstones / max(LiveKeys + Tombstones, 1)`, sampled lazily by `MeterListener`. Tagged `tree`, `leaf`. Cardinality follows the existing `leaf` tag conventions on `LeafSplits` etc.; the gauge respects the same "no-cost when no observer is registered" guard as the F-049b path (the per-leaf snapshot is built only when at least one listener has subscribed to the instrument).
5. **Trigger-source tag on existing instruments.** `LatticeMetrics.LeafCompactionDuration`, `LeafTombstonesReaped`, and `LeafTombstonesExpired` are extended with an optional `trigger` tag carrying the same `{reminder|ratio|size}` enumeration. The tag is **only emitted when `MinTombstoneRatioForCompaction > 0` or `MaxLeafEntriesBeforeForcedCompaction > 0`** so panels that filter on `trigger=""` (absent) match the v3.4.0 reminder-only behaviour exactly and existing dashboards keep working unchanged.
6. **Public API for operator-driven compaction.** New `Task ITombstoneCompactionGrain.RequestCompactionAsync(int shardIndex, CancellationToken ct)` which schedules an out-of-cycle pass for a single shard, bypassing the cooldown map (the cooldown is for event-storm protection; an explicit operator request is treated as authoritative). Reaches into `BeginCompactionStateAsync` / `ProcessNextShardAsync` through the existing path, scoped to a single-element `PhysicalShardIndices`. Surfaced via a new `Task ILatticeAdmin.CompactShardAsync(string treeId, int shardIndex, CancellationToken ct)` (admin surface introduced under F-014) so the diagnostics path can request a compaction for triage.
7. **Grafana Overview dashboard additions.** `src/lattice.dashboards/Grafana/OrleansLatticeOverview.json` gains a dedicated "Compaction" row beneath the existing "Tombstone churn" / "Compaction duration p95" panels, containing four panels:
   - **Compaction pass duration p95 (ms)** - `histogram_quantile(0.95, sum by (le, trigger) (rate(orleans_lattice_compaction_pass_duration_milliseconds_bucket{tree=~"$tree",cluster=~"$cluster",instance=~"$silo"}[5m]))) or vector(0)` broken out by `trigger` so the reminder-driven baseline and pre-emptive triggers are visually separated.
   - **Leaves visited per pass (rate, by outcome)** - stacked rate of `orleans_lattice_compaction_leaves_visited_total{outcome=~"reaped|skipped|noop"}` so the work-done vs work-skipped ratio is visible at a glance.
   - **Shard retries / skips (rate)** - rate of `orleans_lattice_compaction_shard_retries_total` and `orleans_lattice_compaction_shard_skipped_total` overlaid on a single graph; a non-zero `_skipped` rate is the operational alert signal that some shard is consistently failing past `MaxRetriesPerShard`.
   - **Tombstone ratio heatmap** - `orleans_lattice_leaf_tombstone_ratio` rendered as a heatmap with one row per leaf, value = ratio. The heatmap surfaces space-amplification hot spots without forcing operators to query individual leaves; the existing `tree` template variable filters it to one tree at a time.

   The new row is anchored beneath the existing "Compaction duration p95 (ms)" panel and reuses the dashboard's `$tree` / `$cluster` / `$silo` template variables verbatim. The companion `docs/lattice.dashboards/metrics-to-panel-map.md` table gets four new rows mapping each new instrument to its panel; the `DashboardJsonTests` regression guard automatically enforces that every metric name referenced in the new panel `expr` strings resolves to a live instrument on `LatticeMetrics.Meter`, so a future rename fails CI before the dashboard ships stale.

**Out of scope.**
- *Cross-shard compaction parallelism.* The existing one-shard-per-tick grain timer is preserved verbatim - widening it is a separate liveness/throughput question independent of the policy controls.
- *Per-tree compaction schedules.* `TombstoneGracePeriod` already supports per-tree overrides via the named-options pattern; no new schedule primitive is added. Operators tune the cadence by setting `TombstoneGracePeriod` per tree (as today) and the new ratio/size triggers on top of it.
- *Snapshot-grain compaction interaction.* Snapshot pin lifetimes (F-051) already gate WAL trimming; compaction reaps in-memory `Entries` and emits its own `Tombstone` WAL envelopes, so no snapshot-side change is required.
- *Replication-side compaction.* Reap envelopes already flow through the standard WAL → replication transport with `Category = MutationCategory.Maintenance`; producer-side replication filtering remains the only opt-in (no new replication-side knob).

**Acceptance.**
- Defaults reproduce v3.4.0 behaviour exactly: `MinTombstoneRatioForCompaction = 0.0`, `MaxLeafEntriesBeforeForcedCompaction = 0`, `CompactionTriggerCooldown = 5min`. With both triggers disabled, the reminder is the only entry point and existing tests under `TombstoneCompactionGrainTests` / `TombstoneCompactionIntegrationTests` pass without modification.
- White-box tests in `test/lattice/BPlusTree/Grains/TombstoneCompactionGrainTests.PolicyTriggers.cs` cover: ratio trigger crossing the threshold enqueues an out-of-cycle pass for the target shard; size trigger likewise; cooldown drops repeated triggers within the window; cooldown does not block the regular reminder; operator-initiated `RequestCompactionAsync` bypasses the cooldown; `LastTriggerAt` survives a `WriteStateAsync` failure (snapshot/restore symmetry matching the existing audit-bug pattern in `BeginCompactionStateAsync`).
- White-box tests in `test/lattice/BPlusTree/Grains/BPlusLeafGrainTests.CompactionTrigger.cs` cover: a commit that pushes the leaf past the configured ratio emits exactly one `CompactionTriggered` event; a commit that pushes past the size threshold emits exactly one event; a commit that pushes past both emits exactly one event (events are deduped at the source); a commit that does not cross the threshold emits zero events; the `IMutationObserver` event surface receives `LatticeTreeEventKind.CompactionTriggered` with the correct payload.
- White-box tests in `test/lattice/LatticeMetricsCompactionTests.cs` cover: `compaction.pass.duration` records once per `RunCompactionPassAsync` with the correct `trigger` tag; `compaction.leaves.visited` increments per leaf with the correct `outcome`; `compaction.shard.retries` and `compaction.shard.skipped` align with the existing `MaxRetriesPerShard` policy; the `leaf.tombstone.ratio` gauge is a no-op when no listener is attached and produces a per-leaf measurement when one is.
- The `DashboardJsonTests` regression suite passes with the four new panels in `OrleansLatticeOverview.json`, every new `expr` resolves to a live instrument, and `docs/lattice.dashboards/metrics-to-panel-map.md` is updated in the same commit.
- `docs/lattice/tombstone-compaction.md` documents the two new option knobs (with worked examples for "delete-heavy" and "TTL-heavy" workloads), the cooldown semantics, the new admin entry point, and the new metric names. `docs/lattice/metrics.md` instrument catalog gains rows for each new instrument and the `trigger` tag enumeration.
- Wire-format additivity: `[Id(5)] Dictionary<int, DateTimeOffset> LastTriggerAt` on `TombstoneCompactionState` decodes from legacy persisted state as an empty dictionary (the correct semantic default - "no triggers seen yet"), and `LatticeTreeEventKind.CompactionTriggered` is appended to the enum with a fresh discriminant so existing serialised events round-trip unchanged.

**Risk surface.** A faulty trigger could (a) thrash the compactor by re-firing on every commit if the cooldown map is not consulted, (b) starve the regular reminder pass if the trigger queue blocks shard advancement, or (c) leak per-shard state in `LastTriggerAt` if a tree is deleted while a trigger is in flight. Each is closed by an explicit invariant: (a) the cooldown is checked in `TombstoneCompactionGrain` before enqueueing and the leaf-side dedup short-circuits within a single commit; (b) trigger-driven passes share the existing single-grain-timer slot - if a pass is in flight, the trigger is dropped (the in-flight pass observes the elevated ratio/size organically); (c) `TreeDeletionGrain` drains `LastTriggerAt` alongside the existing compaction state on tree deletion (the same path that already drains `InProgress` / `NextShardIndex`).

---

### F-072 ✓ shipped - Configurable compaction tick cadence *(depends on F-071 ✓)*
**Operability / opt-in (replace the hard-coded 2-second per-shard tick interval inside `TombstoneCompactionGrain` with a per-tree option, and document why the cadence is a scheduler-fairness knob - not a grain-deactivation knob)**

`TombstoneCompactionGrain` walks one shard per grain-timer tick during a compaction pass and waits a hard-coded `TimeSpan.FromSeconds(2)` between ticks. The constant is duplicated three times (initial pass start, post-`RequestCompactionAsync` start, and `OnReminderTickAsync` mid-pass restart). The current docs justify the cadence by referencing Orleans' "normal idle-collection schedule" for leaves activated by the previous tick, but that justification is incorrect - Orleans' default `GrainCollectionOptions.CollectionAge` is **2 hours**, four orders of magnitude larger than the 2-second tick gap, so leaves activated on tick *N* are guaranteed still activated on tick *N+1* regardless of the gap. Three concrete operability problems fall out:

1. **No way to slow compaction on a busy cluster.** An operator who wants to throttle compaction has only `TombstoneGracePeriod` today, which conflates *when reaping is allowed* with *how aggressively the compactor walks the topology*. A 1-hour grace period and a 2-second tick are independent dials that the user cannot currently separate.
2. **No way to speed compaction up for triage.** The `CompactShardAsync(shardIndex)` operator path shipped in F-071 still waits 2s between shards even though the operator explicitly asked for triage urgency.
3. **Pass wall-clock scales with shard count, not with deletion volume.** A 1024-shard tree needs ~34 minutes per full pass at 2s/shard; doubling the shard count doubles pass duration regardless of how many tombstones the tree actually contains. Operators sizing capacity have no signal that "more shards = slower pass" and no knob to compensate.

This entry adds a single per-tree option (`LatticeOptions.CompactionShardTickInterval`, default 2 seconds, floor 100 ms) that flows through `LatticeOptionsResolver` into `TombstoneCompactionGrain`. The default reproduces v3.5.0 behaviour exactly so no existing deployment changes shape on upgrade. The companion docs are corrected to state explicitly that the cadence is a **grain-scheduler-fairness knob, not a grain-deactivation knob**: the gap exists so the compactor grain returns control to the Orleans scheduler between shards (avoiding a long-running grain call that could hit Orleans timeouts and starve `RequestCompactionAsync` callers), not so leaves have time to deactivate. Activation lifetime continues to be governed by Orleans' standard `GrainCollectionOptions` on the silo, which the operator tunes independently.

**Scope.**

1. **Per-tree option.** New `LatticeOptions.CompactionShardTickInterval` (`TimeSpan`, default 2 s, floor `TimeSpan.FromMilliseconds(100)`). Validated in the resolver; values below the floor are clamped with a one-shot warning per tree. Propagated through `ResolvedLatticeOptions` so `TombstoneCompactionGrain` can read the per-tree pin via `optionsResolver.ResolveAsync(TreeId)` rather than via the global `IOptionsMonitor` snapshot, matching the pattern established for the F-071 policy knobs.
2. **Single source of truth in the grain.** The three `TimeSpan.FromSeconds(2)` literals in `TombstoneCompactionGrain` are replaced with a single private `GetTickIntervalAsync()` helper that resolves the per-tree pin once at pass start and caches it for the duration of the pass. Mid-pass option changes do not retroactively reshape an in-flight pass; the next pass picks up the new value. This matches how `TombstoneGracePeriod` is sampled (snapshot-at-pass-start, not per-tick).
3. **Operator-initiated triage cadence.** `RequestCompactionAsync(shardIndex, "operator")` continues to use the same configured cadence. A separate "express lane" cadence for operator passes is **out of scope** here; if an operator wants a faster scan they can either lower `CompactionShardTickInterval` for the duration of the triage or set the value lower per-tree on the pinned tree.
4. **Documentation correction.** `docs/lattice/tombstone-compaction.md` - replace the existing "Memory" / "Orleans deactivates idle leaves on its normal schedule" sentence with an explicit two-paragraph block that (a) states the cadence is a scheduler-fairness knob, not a deactivation knob, (b) explains that activation lifetime is governed by Orleans' standard `GrainCollectionOptions.CollectionAge` (default 2 hours) on the silo, and (c) shows the worked example: 1024-shard tree at 2 s = ~34 min per pass; same tree at 200 ms = ~3.4 min per pass. Cross-link to the Orleans grain-deactivation docs so the reader understands the orthogonality.
5. **Telemetry.** No new instruments. The existing `compaction.pass.duration` histogram already captures end-to-end pass wall-clock, so an operator who lowers `CompactionShardTickInterval` immediately sees the histogram shift left without any new dashboard work.

**Out of scope.**
- *Per-shard tick interval override.* Adding "shard A ticks at 100 ms, shard B ticks at 5 s" is a niche use case; per-tree resolution is sufficient until evidence suggests otherwise.
- *Adaptive cadence based on load.* Auto-tuning the interval from observed shard duration (e.g. "if last shard's leaf walk took > 1 s, slow down") is appealing but requires evidence of operator demand and a stable feedback policy. Out of scope for the initial knob; revisit if the static option proves insufficient.
- *Lifetime of the keepalive reminder.* The 1-minute keepalive reminder period is preserved verbatim; it is the mid-pass-restart safety net for silo crashes and is unrelated to the per-shard tick cadence.

**Acceptance.**
- Default behaviour with `CompactionShardTickInterval = TimeSpan.FromSeconds(2)` reproduces v3.5.0 behaviour exactly: existing tests under `TombstoneCompactionGrainTests` and `TombstoneCompactionIntegrationTests` pass without modification.
- Setting `CompactionShardTickInterval = TimeSpan.FromMilliseconds(200)` on a multi-shard tree completes a full pass in under 1/10 the wall-clock of the default. Observable through the `compaction.pass.duration` histogram in an integration test.
- Setting `CompactionShardTickInterval = TimeSpan.FromMilliseconds(50)` (below the 100 ms floor) emits exactly one warning per tree per process and clamps the effective interval to the floor.
- A focused unit test `TombstoneCompactionGrainTests.TickInterval.cs` covers: clamp on below-floor values, default propagation, per-tree override propagation, snapshot-at-pass-start semantics (mid-pass option changes do not affect the in-flight pass).
- `docs/lattice/tombstone-compaction.md` is updated in the same commit with the correction and the worked-example numbers above.

**Risk surface.** A misconfigured tick interval (say 1 ms) could starve the rest of the compactor grain's scheduler quota by never yielding for long enough between shard walks. The 100 ms floor closes that risk explicitly. There is no wire-format risk: the option is silo-side configuration only and does not touch any persisted state. There is no replication risk: cadence is a producer-side concern that does not affect WAL envelope shape or replication semantics.

---

### F-073 ✓ shipped - Intra-shard leaf-walk batching for tombstone compaction *(depends on F-072 ✓)*
**Operability / opt-in (cap peak concurrent leaf activations during a compaction pass by inserting scheduler yields *within* a shard's leaf walk, not just between shards)**

`TombstoneCompactionGrain` walks one shard per timer tick, and within a shard it walks the entire leaf chain back-to-back via `IBPlusLeafGrain.GetNextSiblingAsync` + `CompactTombstonesAsync`. F-072 added a per-tree `CompactionShardTickInterval` to govern the gap *between* shards, but the leaf walk *within* a shard still runs as fast as the per-leaf RPCs complete. Two operability problems fall out:

1. **Peak concurrent leaf activations is unbounded by tree size.** Every leaf in a shard is activated in rapid succession; if the full pass completes inside one `GrainCollectionOptions.CollectionAge` window (default 15 minutes), the entire leaf set of the tree is effectively activated at once. On a 50 000-leaf tree this is a directory and silo-memory shock that scales with tree size, not with deletion volume. The F-072 documentation now calls this out honestly, but the only knob the operator has today is to widen `CompactionShardTickInterval` (which spreads shards but not leaves within a shard) or accept the activation peak.
2. **The peak is paid even on trees with no recent deletes.** A leaf with zero tombstones returns from `CompactTombstonesAsync` via the `LastCompactionVersion` fast-path with effectively no work, but the activation cost (state read + dispatcher overhead per leaf) is paid regardless. On a steady-state tree where most leaves have nothing to do, the dominant cost of a pass is activating idle leaves.

This entry adds a single per-tree option (`LatticeOptions.CompactionLeafBatchSize`, default 64, floor 1) that caps how many leaves the coordinator visits within a single shard before yielding for `CompactionShardTickInterval` (the same gap used between shards). The leaf walk resumes on the next timer tick from the persisted in-shard cursor (`NextLeafIdInShard`), so progress survives silo crashes the same way `NextShardIndex` does. The default 64 reproduces today's behaviour for typical shard sizes (most shards have <= 64 leaves), so existing deployments do not change shape. On large shards the new behaviour caps peak activations to roughly `min(leaves walked within last CollectionAge, CompactionLeafBatchSize * (CollectionAge / CompactionShardTickInterval))`, which on a 1024-shard tree at default settings drops peak concurrent activations from `O(leaves)` to a few thousand regardless of tree size.

**Scope.**

1. **Per-tree option.** New `LatticeOptions.CompactionLeafBatchSize` (`int`, default 64, floor 1). Validated in `LatticeOptionsResolver`; values below 1 are clamped to 1 with a one-shot warning per tree per process. Propagated through `ResolvedLatticeOptions` and snapshotted at pass start alongside `CompactionShardTickInterval` (matches F-072's snapshot-at-pass-start semantics).
2. **In-shard cursor on `TombstoneCompactionState`.** New `[Id(6)] string? NextLeafIdInShard` slot (default `null`, decodes from legacy persisted state as `null` = "start of shard"). When the per-shard timer tick fires, the coordinator walks at most `CompactionLeafBatchSize` leaves starting from `NextLeafIdInShard` (or the leftmost leaf if `null`), persists the new cursor, and yields. The next tick resumes from the cursor; when the walk reaches the end of the shard, the cursor clears and `NextShardIndex` advances.
3. **Coordinator pass loop.** `ProcessNextShardAsync` is split into `ProcessNextShardBatchAsync` (visits up to `CompactionLeafBatchSize` leaves, persists cursor, returns `done?`). The "shard advance" branch fires only when the batch returns `done=true`. The keepalive reminder period and the `compaction.pass.duration` histogram cover the now-longer wall-clock of a pass without modification.
4. **Telemetry.** No new instruments. `compaction.leaves.visited` already counts per-leaf outcomes; the operator can divide by `compaction.pass.duration` to observe the new effective leaf-rate. A new tag `batch_size` on `compaction.pass.duration` is **out of scope** - the pinned per-tree value is observable via `LatticeRegistry` if needed.

**Out of scope.**
- *Per-shard leaf-batch size override.* Not needed; per-tree resolution suffices.
- *Adaptive batch sizing.* Auto-tuning the batch from observed per-leaf latency is appealing but requires evidence of operator demand and a stable feedback policy.
- *Skipping fast-path leaves entirely.* See F-074 for the architecturally correct fix to "don't activate idle leaves at all".

**Acceptance.**
- Default behaviour with `CompactionLeafBatchSize = 64` reproduces F-072 behaviour exactly on shards with <= 64 leaves: existing `TombstoneCompactionGrainTests` and `TombstoneCompactionIntegrationTests` pass without modification.
- On a tree with > 64 leaves per shard, an integration test asserts that an in-flight pass yields between batches by observing the `compaction.pass.duration` histogram split into multiple grain-timer ticks (visible via `IReminderRegistry` interaction count) rather than one continuous walk.
- Setting `CompactionLeafBatchSize = 0` clamps to 1 and emits exactly one warning per tree per process.
- A focused unit test `TombstoneCompactionGrainTests.LeafBatch.cs` covers: cursor advance across batches, cursor clear on shard completion, snapshot-at-pass-start semantics for the batch size, and crash-resume from a persisted in-shard cursor.
- Wire-format additivity: `[Id(6)] string? NextLeafIdInShard` decodes from legacy persisted `TombstoneCompactionState` as `null`, which is the correct semantic default ("start fresh from the leftmost leaf when this shard's turn comes"). No alias renumbering.
- `docs/lattice/tombstone-compaction.md` is updated in the same commit; the worked-example table grows a third column for **peak concurrent leaf activations** that drops from `O(leaves)` to `O(CompactionLeafBatchSize * CollectionAge / CompactionShardTickInterval)`.

**Risk surface.** The persisted in-shard cursor adds one new persistence path on every batch boundary, increasing `TombstoneCompactionGrain` state writes from once per shard to once per `CompactionLeafBatchSize` leaves. The existing keepalive reminder absorbs the wall-clock of the now-longer pass; no new reminders are introduced. There is no wire-format risk for replication (the cursor is grain state, not a WAL envelope), and there is no consistency risk because each batch's leaves are still individually reaped under their own grace-period check.

---

### F-074 - Shard-root dirty-leaf tracking to skip idle leaves on compaction *(depends on F-073 ✓)*
**Architecture / scalability (drop pass-time activation cost from `O(leaves)` to `O(shards + dirty_leaves)` so default cadence can tighten without operator pain)**

The coordinator currently has no way to know which leaves have new tombstones without asking the leaf, so a full pass activates every leaf in the tree even when most have nothing to do. F-073 caps the *peak* concurrent activations but does not reduce the *total*: on a 50 000-leaf tree where 1% of leaves accumulated tombstones since the last pass, today's pass still activates 50 000 leaves. The architecturally correct fix is to push the "is this leaf dirty?" signal up to a coarser-grain owner that already sees every routed mutation - the shard root.

This entry teaches `ShardRootGrain` to maintain a small per-shard "dirty leaves since last compaction" set populated as the shard root routes `Delete` mutations down to leaves, and teaches `TombstoneCompactionGrain` to consult that set before walking. A pass on a tree with no recent deletes activates only the shard root grains (one per physical shard), not every leaf. Activation cost drops from `O(leaves)` to `O(shards + dirty_leaves)`. On the 50 000-leaf, 1024-shard, 1%-dirty example: 1024 + 500 = 1 524 activations, a ~33x reduction. With this in place, the F-072 default `CompactionShardTickInterval` can tighten substantially (e.g. to 500 ms) without operators having to think about peak activation count, and the F-073 `CompactionLeafBatchSize` becomes effectively a no-op on healthy trees.

**Scope.**

1. **Shard-root dirty-leaf set.** `ShardRootState` gains `[Id(N)] HashSet<string> DirtyLeavesSinceLastCompaction` and `[Id(N+1)] HybridLogicalClock LastDirtyAdvance`. The shard root populates the set as it routes `Delete` mutations down (one persisted state write per leaf per dirty-window, deduplicated by the in-memory hot path). The set is bounded by the shard's leaf count.
2. **Coordinator pull path.** A new `IShardRootGrain.GetDirtyLeavesSinceLastCompactionAsync()` method returns the set + `LastDirtyAdvance`. `TombstoneCompactionGrain.ProcessNextShardBatchAsync` (added in F-073) calls this once per shard at the start of the shard's first batch, walks only the named leaves, and on shard completion calls `IShardRootGrain.ClearDirtyLeavesUpToAsync(HybridLogicalClock advance)` to drain the set up to a watermark. The clear is HLC-gated so writes that arrived during the walk are retained for the next pass.
3. **Replication interaction.** Receiver-side leaves observe the same `Delete` mutations via the standard WAL replication transport; receiver-cluster shard roots populate their own dirty-leaves set from the same mutation stream and run their own compaction passes against it. No new wire envelope, no cross-cluster coordinator. (R-NNN-A may want a follow-on roadmap entry to make the cross-cluster cadence explicit, but it is not a wire-format change.)
4. **Backwards compat.** A pre-F-074 persisted `ShardRootState` decodes the new slots as an empty set + zero HLC. The coordinator detects "empty set + zero HLC" as "either a fresh tree or an upgraded tree with no signal yet" and falls back to the F-073 leaf walk for the first pass, then locks in to the dirty-set fast-path on the second.
5. **Telemetry.** New tag `path` on `compaction.leaves.visited` with values `walk` (legacy / fallback) and `dirty-set` (F-074 fast path). New histogram `compaction.shard.dirty_leaves` (per pass, per tree, per shard) for capacity planning. Existing `compaction.pass.duration` automatically reflects the wall-clock saving without modification.
6. **Tighten default cadence.** With F-074 in place, `LatticeOptions.DefaultCompactionShardTickInterval` is lowered from 2 s to 500 ms in the same change. F-072's worked-example table is rewritten so the "peak concurrent activations" column reflects the dirty-set fast path. Operators can still raise the cadence per tree if they have a reason to slow it down.

**Out of scope.**
- *Per-leaf tombstone counters on the shard root.* Just a presence-or-absence set is enough; the leaf still computes the actual reap inside `CompactTombstonesAsync`.
- *Eviction of the dirty set under memory pressure.* The set is bounded by leaf count, which is bounded by tree size; if leaf count grows unbounded the tree has bigger problems than this set.
- *Compaction-window scheduling.* Concentrating compaction into a quiet hour (e.g. `CompactionWindow = "02:00-04:00"`) is a separate operability feature; file as a follow-on if there is demand.

**Acceptance.**
- A tree with no recent deletes runs a full compaction pass in under 5% of the F-073-baseline wall-clock and activates only one grain per physical shard (asserted via `IGrainFactory` interaction count in an integration test).
- A tree with deletes scattered across 1% of its leaves activates `O(shards + dirty_leaves)` grains during a pass, not `O(leaves)`. Asserted via interaction count.
- Persisted `ShardRootState` from a pre-F-074 silo activates cleanly under F-074 and falls back to the F-073 walk for one pass, then exercises the fast path on the next.
- The HLC-gated `ClearDirtyLeavesUpToAsync` watermark is exercised by an integration test where deletes interleave with the in-flight compaction walk; deletes that arrived during the walk are still picked up by the *next* pass, not silently dropped.
- `LatticeOptions.DefaultCompactionShardTickInterval` is lowered to `TimeSpan.FromSeconds(0.5)` in the same change; the F-072 acceptance test that asserted "default = 2 s" is updated to assert "default = 500 ms" and the worked-example numbers in `docs/lattice/tombstone-compaction.md` are regenerated.

**Risk surface.** The shard root carries new persisted state on every routed `Delete`, raising shard-root state-write frequency on delete-heavy workloads. The hot-path dedup (in-memory set membership check before persist) closes the dominant cost; persists happen only when a previously-unseen leaf id enters the set within a dirty window. Wire-format additivity is the standard `[Id]` slot pattern - legacy decodes as empty set + zero HLC. The HLC-gated clear watermark prevents the classic "races between compaction and concurrent deletes" hazard explicitly: a delete that arrives mid-pass is preserved for the next pass via `LastDirtyAdvance` < `now`. There is no replication risk: dirty-set state is local to each cluster and reconstructed independently from the local WAL stream.

---

### 6 · Documentation & Developer Experience

- [ ] **F-021 - Migration guide**: Document how to migrate data from external stores (Redis, SQL, Cosmos DB) into Lattice using the streaming bulk-load API, including key-design and value-serialization best practices.
- [ ] **F-022 - Troubleshooting guide (`docs/troubleshooting.md`)** *(follows F-014 ✓)*: Cover common issues - storage provider exceptions from oversized grains, concurrent split activity, slow scans, stale cache behavior - and how to interpret the output of `DiagnoseAsync`.
- [ ] **F-023 - Sample applications (`samples/`)**: End-to-end examples (e.g. ASP.NET Core session store, leaderboard, distributed configuration service) showing real integration patterns beyond the Quick Start snippet.

---

### 7 · F-055 ✅ shipped
**Reliability / high impact (delivers universal cross-leaf, cross-shard, cross-cluster atomic-batch reader isolation)**

Reader isolation during in-flight sagas. Today the consistency contract documented in [`docs/lattice/consistency.md`](../../docs/lattice/consistency.md) ("What Lattice does **not** guarantee" - *Reader isolation during in-flight sagas or range deletes*) explicitly warns that readers concurrent with `SetManyAtomicAsync` may observe a partial view of the mutation, and recommends version-guarded reads (`GetWithVersionAsync` + `SetIfVersionAsync`) as the user-side workaround. F-055 closes the carve-out with a WAL-metadata primitive that delivers cross-leaf, cross-shard, and cross-cluster strict reader isolation by construction, with no per-tree opt-in, zero leaf-state I/O on the saga prepare path, and **zero grain hops on the read path**.

**Design.** Saga prepare routes every per-key write through `BPlusLeafGrain.SetCoreAsync` / `DeleteAsync` with the mutation carrying a new `[Id(14)] bool IsPrepared` slot (default `false`, wire-compatible) and the saga's `TransactionId`. The `IsPrepared` flag is threaded into the leaf via the existing `LatticeTransactionContext` ambient (extended with an `isPrepared` field alongside `TransactionId`) so saga prepare and saga commit/abort phases reach the leaf through the same call shape as every other write - no new public method, no new grain interface parameter on the per-key write path. The leaf's existing F-049b commit pipeline appends to the per-shard WAL via `ICommitLogWriter` exactly once, then `((ILeafProjection)this).Apply` branches on `IsPrepared`: prepared mutations route into a per-leaf in-memory `Dictionary<Guid, PendingTxEntries>` rather than `Entries`. No new direct-WAL call site exists - every saga write goes through the same grain entry point as a non-saga write, so the existing F-019 shadow-forward integration applies for free during topology changes (see "Shadow-forward and topology-change integration" below). Saga commit and abort go through a new `IShardRootGrain.AppendTxTerminalAsync(Guid txid, bool committed, CancellationToken)` per touched shard - one terminal append per WAL shard the saga touched, never per-key. For a typical 16-key batch hitting 1–4 shards this is 1–4 terminal appends after N parallel prepare writes; `AtomicWriteGrain` resolves the touched-shard set up-front via `LatticeGrain.GetRoutingAsync()` and retries each terminal append on `StaleShardRoutingException` and `StaleTreeRoutingException` (matching every other public-API write - the latter covers the resize / reshard alias-swap window) so a slot that migrated between prepare and commit, or a tree alias swap that committed mid-saga, lands the terminal mark on the new owner.

**Reads do not consult any ledger grain.** Each `BPlusLeafGrain` carries the in-memory pending-tx map populated as `IsPrepared=true` mutations replay through F-049c's per-shard replay coordinator. `GetAsync` / `GetWithVersionAsync` / `ExistsAsync` / `GetManyAsync` / `KeysAsync` / `EntriesAsync` / `CountAsync` filter pending entries out of the visible projection via a local hash lookup on the read path - zero RPC, zero allocation beyond what the v3.4.0 read path already does. When the per-shard replay coordinator surfaces a `TxCommit` mutation, the leaves on that shard flip every matching pending entry into `Entries` via the existing `LwwValue<byte[]>.Merge` path; on `TxAbort`, matching pending entries are dropped. The terminal mutation is the **single linearization point for the whole batch on its WAL shard** - every leaf on that shard observes either pre-saga state for every key the saga touched on that shard, or post-saga state for every such key, never a split. Because the saga broadcasts the same terminal mark to every WAL shard it touched, the cross-shard reader observes the same atomicity property tree-wide. There is no `ITxCommitLedgerGrain` and no per-tree linearization grain on the read path.

**Shadow-forward and topology-change integration.** Both halves of the saga compose with the existing shadow-forward machinery (shard split F-011, online resize F-019b, online reshard F-019a) without any new code path:

- *Prepare during a migration window.* `BPlusLeafGrain.SetCoreAsync` is reached via the standard `ShardRootGrain.SetCoreAsync` entry, which already invokes `ForwardLocalWriteToShadowIfNeededAsync` to mirror every write to the target shard's leaf. The forward replays the mutation under its `TransactionId` and `IsPrepared=true` flag - so the target leaf appends the same prepared entry to its own WAL, runs the same `Apply`, and adds the same entry to its own pending-tx map. Source and target leaves converge on identical pending-tx state for every saga in flight during the migration window.
- *Terminal mark during a migration window.* `IShardRootGrain.AppendTxTerminalAsync` is wrapped in the same `ForwardLocalWriteToShadowIfNeededAsync` integration as `SetCoreAsync` - when the source shard is shadow-forwarding, the terminal mark also forwards to the target shard, which appends it to its own WAL. Each shard's F-049c replay coordinator independently surfaces the terminal mark to its leaves, which flip pending entries identically. Post-swap, the saga's retry-on-`StaleShardRoutingException` loop on `AtomicWriteGrain` lands any straggler terminal appends on the new owner; LWW idempotence + the leaf-side terminal-set dedup makes double-delivery a no-op.
- *Online resize and reshard.* These compose: resize wraps the entire physical-tree replacement in a shadow-forward window, and reshard composes per-shard splits with their own windows. Saga writes in flight inherit the two compositions above through every layer, so destination leaves see the same `Prepared`-then-terminal sequence and converge identically. After alias swap or shard-map swap, the destination is authoritative and the saga's continuing terminal broadcast retries onto the new owner. `ResizeAsync` and `ReshardAsync` do not need to wait for in-flight sagas to drain - atomicity holds across the boundary by construction.

**Surface and crash recovery.** No public method signature changes. Internal additions: `MutationKind.TxCommit` and `MutationKind.TxAbort` marker variants; a new `[Id(14)] bool IsPrepared` slot on `LatticeMutation` (default `false`, wire-compatible) so existing `Set` / `Delete` mutation kinds carry the prepared/committed phase without enum proliferation; `IShardRootGrain.AppendTxTerminalAsync(Guid txid, bool committed, CancellationToken)` integrated with the shadow-forward tracker; a per-leaf in-memory `Dictionary<Guid, Dictionary<string, PendingEntry>>` (txid -> key -> entry) that is **not** persisted on `LeafNodeState` - under F-049d the WAL is the sole durable record and the pending map is rebuilt deterministically by F-049c's per-shard replay coordinator on activation, alongside `Entries`. To keep the WAL retention bounded by the slowest in-flight saga rather than the F-051 checkpoint cadence, projection-checkpoint advance is gated on prepare resolution: each leaf advances `ProjectionCheckpointOffset` only up to `min(highest contiguous Apply'd offset, (min unresolved prepare offset) - 1)`, and the F-050 cursor-registry hook publishes that bounded offset to `ILatticeReplicationCursorRegistry.AdvanceCursorAsync` so the WAL GC predicate (R-083) naturally retains every prepared entry whose terminal mark has not yet replayed. A small "recently terminal" `HashSet<Guid>` on each leaf, also in-memory, is populated as `TxCommit` / `TxAbort` mutations replay and GC'd as pending entries clear under each txid; it survives only as long as the activation. Worst-case crash-recovery replay distance is bounded by the existing `LatticeOptions.MaxLeafReplayEntries` budget (default 10 000) and `LatticeOptions.AtomicWriteRetention` saga-state lifetime (default 48h) - beyond those bounds, the F-049c snapshot-then-WAL recovery path takes over. Crash recovery is anchored on `AtomicWriteGrain`'s existing persisted `AtomicWriteState`: a saga whose phase is `Prepare` or `Execute` on reactivation re-runs the commit (or compensate) broadcast - both are LWW-idempotent (re-appending the same `TxCommit` to a shard that has already seen one is a no-op via the dedup in the leaf-side terminal set). On leaf activation the F-049c replay coordinator surfaces both prepared and terminal mutations from the persisted checkpoint forward, so a fresh leaf converges to the same pending-and-terminal state every other leaf has.

**Replication interaction (universal - atomic batch ships are entirely deleted).** Prepared and terminal mutations ride the standard WAL → replication transport (R-080 / R-081 already preserve `MutationKind`, and the new `IsPrepared` slot is a strictly-additive field on `LatticeMutation` flowing through verbatim). Receiver-side leaves consume prepared and terminal mutations through the standard projection path, populate their own pending-tx map, and flip pending entries via the same local hash lookup the source-cluster leaves use. **No receiver-side coordinator grain, no staging buffer, no per-tree opt-in, no atomic-batch detection on the wire.** Cross-cluster atomic visibility is the only behaviour. Every one of the following surfaces is **deleted from the replication package in the same change set** - not deprecated, not behind a flag, not retained for backwards compatibility:

- `LatticeReplicationOptions.AtomicBatchDelivery` (the property, its default-value comment, every test that toggles it, every doc paragraph that mentions it).
- The per-tree `IReplicationTxBufferGrain` staging-buffer interface, its `ReplicationTxBufferGrain` implementation, its alias entry in `TypeAliases`, its DI registration in `AddLatticeReplication`, and every `[Id]`'d state DTO it owned.
- `IReplicationApplyGrain.ApplyManyAtomicAsync` (the receiver-side apply-mode saga that F-054 wired up) and its `AtomicApplyEntry` / `AtomicApplyResult` DTOs (`ol.aae` / `ol.aar` aliases). The point-apply seam (`ApplySetAsync` / `ApplyDeleteAsync` / `ApplyDeleteRangeAsync`) is unchanged - F-046 / R-091 still apply for non-saga writes - but the atomic-saga apply seam is removed because every prepared mutation now flows through the same point-apply path with `IsPrepared=true`.
- The bootstrap saga-blacklist set on the replication apply state, the blocked-floor HLC machinery, the `SnapshotSagaQuiesceTimeout` configuration knob, and every guard that rejected snapshot bootstrap when an atomic batch was in flight.
- Every `apply.batch.*` metric tied to the staging buffer (counters and histograms covering buffer depth, batch-flush latency, atomic-apply outcomes); the `orleans.lattice.replication` meter retains only the per-mutation counters that survive the change.
- The receiver-side atomic-batch encoder/decoder paths in `ReplicationApplier.Batch.cs` that detected `TransactionId`-grouped batches and packed them into a single `ReplicationBatchEnvelope` field. (No corresponding gRPC ship-side surface exists today - atomic-batch detection was confined to the apply-side staging buffer - so this deletion is scoped to the apply-side encoder/decoder.) The transport ships `LatticeMutation` envelopes one-by-one as before; atomic ordering is enforced at the receiver leaf via the WAL's mutation order, not by the wire framing.

After F-055 ships, `ILattice.SetManyAtomicAsync` "just works" on every tree - local single-cluster, replicated source-cluster, and replicated receiver-cluster - through the single WAL-metadata primitive, with no caller-visible flag, no opt-in, and no replication-side detection of "is this batch atomic". The R-103 chaos fixture is correspondingly tightened to the originally-spec'd "continuous reader observes either zero or all keys at every poll" invariant, asserted universally with no bounded-window caveat.

**Acceptance.**
- A fixture writes `SetManyAtomicAsync` for a 16-key batch spanning ≥ 4 leaves on ≥ 2 shards while a continuous reader polls every key at 10 ms cadence; the reader observes either zero or all keys at every poll over the saga's lifetime, with no bounded-window caveat. Repeated 50 times to surface scheduling races.
- An equivalent two-cluster fixture against `Orleans.Lattice.Replication`'s in-process fixture asserts the same invariant on the receiver, with **no** `AtomicBatchDelivery` flag in the configuration (the option no longer exists).
- A topology-change fixture commits a saga whose touched-shard set straddles a shard split: prepare lands during the shadow-forward window, the split commits and the source enters Reject, then the saga's terminal broadcast retries onto the new owner via `StaleShardRoutingException`; a continuous reader observes either zero or all keys throughout. Equivalent fixtures for online resize (mid-saga `ResizeAsync`) and online reshard (mid-saga `ReshardAsync`).
- A saga whose execute phase fails mid-batch asserts a continuous reader sees the pre-saga state for every key, every time, throughout the compensation rollback (the `TxAbort` mark is the per-shard linearization point on the rollback side).
- A throughput-parity benchmark confirms saga-RPS scales linearly with batch concurrency - the WAL-only prepare path is no slower than v3.4.0 single-key write throughput multiplied by batch size, and saga concurrency is bounded by Orleans grain RPS for `AtomicWriteGrain` activations (per-saga, not per-tree) rather than capped at single-grain throughput. A separate read-path benchmark confirms `GetAsync` / `KeysAsync` show no measurable latency regression versus v3.4.0 baseline when no saga is in flight.
- The existing-test regression sweep is green; the deleted receiver-side atomic-batch surface is replaced by the equivalent assertion against the new universal behaviour. A code-search regression test (run as part of `Phase 6b` hygiene gates) confirms `AtomicBatchDelivery`, `IReplicationTxBufferGrain`, `ReplicationTxBufferGrain`, `ApplyManyAtomicAsync`, `AtomicApplyEntry`, `AtomicApplyResult`, `SnapshotSagaQuiesceTimeout`, and the `apply.batch.` metric prefix no longer appear anywhere under `src/lattice.replication/` or `test/lattice.replication/`.

**Shipped.** Acceptance benchmarks captured under `benchmark/host/Bench.Microbench` at `BENCH_MICROBENCH_FIDELITY=quick` (BDN `Job.ShortRun`, 14 benchmarks, 16:56 wall-clock, n=3 iterations × 3 warmups):

- *Throughput parity.* `SetManyAtomic_4Shards` mean = 127,756 ns ≈ 1.013 × (`PointWrite` mean 7,885 ns × 16-key batch) - decisive parity with v3.4.0 single-key write throughput multiplied by batch size.
- *Per-saga concurrency scaling.* `SetManyAtomic_Concurrent` per-saga mean stays flat at 125–145K ns across 1× → 64× concurrency; effective sagas-per-second rises from 7,031 (conc=1) to 7,936 (conc=64) with 88% wall-clock scaling efficiency at conc=64 - confirms saga concurrency is bounded by Orleans grain RPS for `AtomicWriteGrain` activations (per-saga, not per-tree), not capped at single-grain throughput.
- *Read-path non-regression.* `PointRead_AtomicTreeIdle` (F-055 path) vs `PointRead` (v3.4.0-equivalent path) shows p50 −13.6% and p95 −56.6% on the dominant-signal percentiles; p99/mean variance is ShortRun n=3 noise, corroborated by `PointRead_AtomicTreeWithActiveSaga` showing better p99 than `_AtomicTreeIdle` (statistically impossible if a true mean separation existed). The atomic-tree read path is at worst at parity with v3.4.0, and on the percentiles that matter for tail-latency SLOs is faster.

Integration acceptance (continuous-reader fixture, two-cluster fixture, topology-change fixtures, mid-batch failure compensation fixture, code-search hygiene against the deleted receiver-side surface) was covered in PR #197 and is asserted by `RoadmapIdentifierHygieneTests` plus the suite under `test/lattice/BPlusTree/AtomicWrite/` and `test/lattice.replication/AtomicVisibility/`.


### 8 · F-057 ✅ shipped
**Reliability / high impact (closes three concurrency bugs in the F-055 cross-migration LWW backstop that surfaced under sustained 4→8 reshard chaos load)**

Hardening of the cross-migration LWW backstop branch on `BPlusLeafGrain.ApplyTxTerminalAsync` introduced by F-055. The F-055 universal-saga design pairs an in-leaf pending-tx bucket with a backstop that fires when a saga terminal arrives at a leaf holding committedValues for keys it never saw the saga's prepare for (the prepare-phase shadow-forward was dropped by a mid-saga shard-split / drain race, or the destination leaf inherited keys whose prepare landed elsewhere). Under the `ReshardTopologyTests` 4→8 reshard chaos fixture (15 rounds × 16 keys × continuous reader at 10 ms cadence), the backstop branch exhibited three sequential races that combined to a 1-in-8 atomic-visibility failure rate on the destination shard. F-057 fixes all three; the residual mid-saga visibility window that the fixes cannot eliminate is structurally documented and tracked as F-059 below.

**Three concurrency bugs fixed.**

- *Per-saga vs. per-key backstop granularity.* The original implementation guarded the backstop on a per-saga `_recentlyTerminal: HashSet<Guid>` dedup, so the first terminal delivery for a saga that landed on a leaf with even one pending key short-circuited the entire branch - including for sibling keys on the same leaf whose pending mutations had been dropped. Fix: compute `missingKeys = committedValues.keys − bucket.keys` and apply the backstop per-missing-key, so a partial-pending bucket on a leaf still gets the backstop for the keys it missed.
- *Flip-dedup conflated with backstop-dedup.* The same `_recentlyTerminal` set was used for two distinct invariants: (a) "this leaf has already flipped pending → committed for this saga, do not re-flip on a duplicate terminal" and (b) "this leaf has already backstopped committedValues for this saga, do not re-apply on a duplicate terminal". The two are not the same - a leaf can legitimately flip its pending bucket, then receive a *later* terminal delivery that carries committedValues for keys whose prepare went to a different leaf, and that later delivery must still drive the backstop. Fix: introduce a second dedup set `_backstoppedTerminals` (separate from `_recentlyTerminal`) so flip-dedup and backstop-dedup decouple completely.
- *Per-(txid, key) dedup poisoning across disjoint terminal deliveries.* The split between the two dedup sets was not enough on its own: `_backstoppedTerminals` as a `HashSet<Guid>` still recorded "this saga has been backstopped on this leaf" as a per-saga atom, so a second terminal delivery for the same saga carrying a *disjoint subset* of committedValues (e.g. shard-router fan-out delivers `{a, b}` first and `{c, d}` second to the same leaf) short-circuited the backstop for `{c, d}` even though no prior delivery had ever stamped them. Fix: promote `_backstoppedTerminals` to `Dictionary<Guid, HashSet<string>>` so per-(txid, key) dedup tracks which specific keys have been backstopped under each saga; a replay of `{a}` for a saga that has already backstopped `{a, b}` short-circuits with zero WAL appends, but a fresh delivery of `{c, d}` for the same saga drives the backstop because none of those keys appear in the saga's per-key set yet.

**Acceptance.** The chaos suite (`ReshardTopologyTests` 4→8 shards × 15 rounds × 16 keys × continuous reader at 10 ms cadence) passes ~15-of-20 trials after the fixes versus 1-of-8 before - a 6× improvement that eliminates the 5-key shard-cardinality failure pattern. Three regression tests pin each of the three fixes:

- `ApplyTxTerminalAsync_with_partial_pending_bucket_backstops_only_missing_keys` - prepares one of two committed keys via a prepare-bucket entry, calls `ApplyTxTerminalAsync` with committedValues covering both keys, and asserts the in-bucket key wins via the pending-flip path while the migrated key reaches `Entries` via the backstop (per-key granularity).
- `ApplyTxTerminalAsync_after_null_committedValues_still_backstops_subsequent_subset` - delivers a null-committedValues terminal first (which sets the flip-dedup flag), then a payload-carrying terminal for the same saga, and asserts the backstop fires on the second delivery (decoupled flip/backstop dedup).
- `ApplyTxTerminalAsync_with_disjoint_committedValues_subsets_backstops_each_subset` - delivers two disjoint subsets `{a, b}` then `{c, d}` for the same saga and asserts each delivery applies one `WriteState`; a replay of `{a}` after both deliveries asserts zero additional `WriteState` and preserved stamps (per-(txid, key) dedup).

**Residual gaps explicitly out of scope.** Two structural issues that the three fixes cannot close are tracked as follow-on items below. F-058 retires the standalone `state.WriteStateAsync()` call at the tail of the backstop loop in favour of a WAL-routed commit (architectural-debt parity with F-049d's WAL-as-sole-commit-point invariant). F-059 closes the residual mid-saga atomic-visibility window observed on the destination shard for the `pre=1..3, post=13..15` pattern - the prepare-before-split race the backstop cannot retroactively repair, because it is post-terminal - via a retroactive shadow-forward of in-flight prepared mutations at split-begin.

### 9 · F-058 ✅ shipped
**Reliability / medium impact (closes the last leaf-side foreground commit path that bypasses the WAL; follow-on to F-057)**

Route the cross-migration LWW backstop write through the per-shard WAL instead of through `state.WriteStateAsync()`. The backstop branch on `BPlusLeafGrain.ApplyTxTerminalAsync` is the only remaining foreground leaf-write site in the core library that commits durable state via the Orleans persisted state row rather than via `ICommitLogWriter` - every other write path was converted to WAL-as-sole-commit-point under F-049d. F-057 corrected the per-key correctness of the branch but kept the legacy state-row commit point to scope the change minimally; F-058 closes the architectural-debt gap so every foreground commit obeys the same durability invariant.

**Design.** A new `[Id(15)] bool IsBackstop` slot on `LatticeMutation` (default `false`, strictly-additive and wire-compatible - adjacent to F-055's `[Id(14)] IsPrepared`) marks the per-key write as a cross-migration backstop rather than an ordinary `Set`. The backstop loop in `ApplyTxTerminalAsync` is rewritten to drive each missing-key write through the same `ICommitLogWriter.AppendAsync` path that `SetCoreAsync` already uses for ordinary `MutationKind.Set`, with `LatticeMutation { Kind = MutationKind.Set, IsBackstop = true, TransactionId = txid, Key = key, Value = value, Timestamp = sharedTickStamp, OriginClusterId = ambientOrigin, ... }`. The WAL append is the durability point; the in-memory `Entries` mutation happens through the existing `((ILeafProjection)this).Apply` path so the projection hash, the F-049c replay coordinator, and any future replication consumer all observe the backstop write through the same primitive as every other Set. The standalone `state.WriteStateAsync()` call at the tail of the backstop loop is **deleted** - the WAL append is the commit, and crash recovery rebuilds Entries from the WAL via the same replay path that recovers ordinary writes. The single shared `HybridLogicalClock.Tick` stamp is computed once per terminal delivery and re-used across every missing key's WAL entry (so the saga's per-shard linearization invariant - every backstopped key on this leaf observes the same revision tick - is preserved bit-for-bit). The F-057 per-(txid, key) dedup in `_backstoppedTerminals` continues to short-circuit replays of an already-backstopped key with no WAL append. Observers (F-035) receive the `LatticeMutation` with `IsBackstop = true` after the WAL append returns; consumers that need to distinguish ordinary writes from backstop writes filter on the flag, but the default observer contract treats backstop writes as ordinary `Set` mutations (which is what they semantically are at the projection level).

**Replication interaction.** The replication ship-side (R-080) and apply-side (R-091) paths consume `LatticeMutation` envelopes verbatim, so the `IsBackstop` flag propagates to receiver clusters at zero wire-format cost. Receiver-side leaves treat a backstop mutation identically to an ordinary Set at the projection level - the receiver's own pending-tx machinery resolves saga atomicity on the receiver via F-055's universal mechanism, and the backstop flag exists purely as a debugging / metrics tag on the ship side. The replication WAL GC predicate (R-083) treats backstop entries identically to ordinary Set entries for retention purposes.

**Surface and observability.** No public method signature changes. Internal additions: `[Id(15)] LatticeMutation.IsBackstop`; one new gauge tag value `kind=backstop` on the existing `orleans.lattice.leaf.write.duration` histogram (so operators can size the backstop traffic against ordinary writes without a new instrument); the docstring on `_backstoppedTerminals` is rewritten to point at the WAL-routed loop rather than the per-leaf state row. The legacy `state.WriteStateAsync()` site is removed and a regression test (`AuditHygieneRegressionTests.Backstop_terminal_path_does_not_call_WriteStateAsync`) reflection-asserts that the only `state.WriteStateAsync` call sites in `BPlusLeafGrain` and its partial files are the F-049c projection-checkpoint flush, the F-051 coalesced checkpoint, and the `OnDeactivateAsync` flush - every foreground commit path goes through `ICommitLogWriter`.

**Acceptance.**
- A fixture exercises the backstop branch (prepare lands on a doomed leaf, slot migrates via a mid-saga split, terminal carries committedValues for the migrated key) and asserts the leaf's WAL contains a `MutationKind.Set` entry with `IsBackstop = true` carrying the committed value at the expected HLC tick.
- A crash-recovery fixture commits a backstop write, simulates a silo crash before the F-051 checkpoint cadence fires, deactivates and reactivates the leaf, and confirms `GetAsync(key)` returns the committed value via the F-049c replay path - i.e. the WAL alone is sufficient for backstop durability.
- The `BPlusLeafGrainTests.BackstopTerminal` partial's three F-057 regression tests (which today assert via `FakePersistentState.WriteCount`) are converted to assert via a `FakeCommitLogWriter.AppendCount` so the regression coverage tracks the WAL path rather than the legacy state-row path.
- A code-search regression test (run as part of `Phase 6b` hygiene gates) confirms the legacy `state.WriteStateAsync()` call in the backstop branch is gone and that no new `state.WriteStateAsync` site has been introduced on any foreground write path.
- The chaos suite (`ReshardTopologyTests` 4→8 shards × 15 rounds × 16 keys) continues to pass at the same rate as the F-057 baseline, with the WAL-routed path observably exercising the same crash-recovery contract as every other commit path under chaos.

**Shipped.** F-058 was delivered as a single PR on the `feature/f058-wal-route-backstop` branch. The new wire slot landed at `[Id(18)] LatticeMutation.IsBackstop` (the F-058 spec drafted it at `[Id(15)]` but slots through `[Id(17)] ShardIndex` had already been allocated; `[Id(18)]` is strictly additive and wire-compatible). Implementation:

- `src/lattice/LatticeMutation.cs` - new `[Id(18)] bool IsBackstop` slot with XML doc describing the cross-migration-backstop semantics and the `kind=backstop` metric tag.
- `src/lattice/BPlusTree/Grains/BPlusLeafGrain.PendingTx.cs` - backstop loop in `ApplyTxTerminalAsync` rewritten to drive each missing-key write through `ICommitLogWriter.AppendAsync` (one append per key, shared HLC tick); the legacy `state.WriteStateAsync()` call is removed; per-append `LatticeMetrics.LeafWriteDuration` emission tagged `(tree, kind=backstop)`.
- `test/lattice/Fakes/FakeCommitLogWriter.cs` - new test fake with `AppendCount` / `Appended` / `ThrowOnAppend`.
- `test/lattice/BPlusTree/Grains/BPlusLeafGrainTests.cs` - `CreateGrain` helper takes an optional `commitLog: ICommitLogWriter`, wires it through `IGrainContext.ActivationServices`, and seeds `state.State.TreeId = "test-tree"` so the lazy resolver returns the fake.
- `test/lattice/BPlusTree/Grains/BPlusLeafGrainTests.BackstopTerminal.cs` - 8 tests converted from `state.WriteCount` assertions to `commitLog.AppendCount` assertions; the renamed `ApplyTxTerminalAsync_backstop_persists_via_WAL` test additionally pins the appended mutation's `IsBackstop=true`, `Kind=Set`, `TransactionId`, and `Timestamp == Entries[k].Timestamp` invariants.
- `test/lattice/AuditHygieneRegressionTests.cs` - new `Backstop_terminal_path_does_not_call_WriteStateAsync` regression that reads `BPlusLeafGrain.PendingTx.cs` source, strips C# comments, and reflection-asserts zero `state.WriteStateAsync(` call sites.

All 336 `BPlusLeafGrainTests` and 3 `AuditHygieneRegressionTests` green on `dotnet test --filter "TestCategory!=Chaos"`.

### 10 · F-059 ✅ shipped
**Reliability / medium impact (closes the residual mid-saga atomic-visibility window across concurrent shard splits; follow-on to F-057)**

Retroactive shadow-forward of in-flight prepared mutations when a shard split begins. The 4→8 reshard chaos fixture (`ReshardTopologyTests`) observes a residual atomic-visibility pattern that the F-055 + F-057 + F-058 fixes cannot eliminate on their own: a saga whose **prepare phase landed on the source shard before the split started** leaves no pending bucket on the destination leaf, because the destination did not exist as the slot's owner when the prepare's shadow-forward fired. The saga's terminal eventually backstops the migrated keys onto the destination, but for the **interval between split-commit and terminal-broadcast** the destination shard's `ShardMap`-routed reads see the drained pre-saga state (every key the saga touched is invisible on the destination, because the source's prepare-time shadow-forward stamped them into `_pendingTx` not `Entries`, and the source's drain copied only `Entries` to the destination). A continuous reader polling every key inside the 20 ms post-saga reader window observes the pre-saga state for a subset of the saga's keys - a `pre=1..3, post=13..15` visibility split - which is the precise atomicity invariant the universal-saga design F-055 claims to guarantee.

**Design.** Extend the shard-split state machine with a **retroactive pending-tx forward** step that fires at the entry of the existing `BeginShadowWrite` phase, immediately after the source enters the shadow-forwarding window but **before** any drain batch is dispatched. The step iterates every source leaf whose key range overlaps a migrating slot, calls a new internal `IBPlusLeafGrain.GetPendingMutationsForSlotsAsync(ImmutableHashSet<int> slots, int virtualShardCount, CancellationToken)` returning a per-(txid, key) snapshot of `_pendingTx[*][key]` for every prepared entry whose key hashes into one of the migrating slots, and replays each snapshot entry through the destination's standard `BPlusLeafGrain.SetCoreAsync` path under `LatticeTransactionContext.Set(txid)` + `LatticePreparedContext.BeginScope()` so the entry lands in the destination leaf's own `_pendingTx` bucket with the original HLC stamp and `OriginClusterId` preserved verbatim. The replay routes through the destination shard's `ShardRootGrain.SetCoreAsync` so the existing `ForwardLocalWriteToShadowIfNeededAsync` machinery (which already mirrors non-prepared writes during the shadow-window) composes naturally - chained splits that begin while the retroactive forward is in flight inherit the same propagation. The replay is LWW-idempotent and per-key by `(txid, key)`, so partial failure of the retroactive sweep is safe: the next saga terminal's backstop will fill in any key the sweep missed, but the sweep collapses the visibility window for every key it does land. The retroactive sweep runs **synchronously inside `BeginShadowWrite`** - the phase does not progress to drain until the sweep returns - so the source's "begin shadow forwarding" guarantee strengthens from "every write **arriving** during the window is mirrored" to "every prepared write **already on the source** is also mirrored". Once the sweep returns, every leaf-pair on the source/destination axis has identical pending-tx state for every saga touching this split, and the destination's terminal-time pending-flip path observes the same prepared-bucket-wins-over-backstop semantics that an unmigrated leaf would.

**Crash recovery and idempotence.** The retroactive sweep is crash-idempotent under the existing split coordinator: the coordinator persists its phase to `TreeShardSplitState` before invoking the sweep, and on reactivation re-runs the sweep before progressing to drain. The destination leaves' `_pendingTx` buckets are LWW-idempotent on `(txid, key)`, so a re-run is a no-op for entries it has already received. The source leaves' `GetPendingMutationsForSlotsAsync` snapshot is read-only and computed off the in-memory map, so re-reads observe a strictly-converging view (the only mutators are saga commits / aborts that drain the bucket monotonically, and ordinary `Set` writes that arrive via the in-flight saga's prepare path - both of which the shadow-forward window also mirrors). The sweep is scoped to migrating slots only, so the cost is `O(prepared-entries-per-migrating-slot)` per split - bounded by the saga concurrency observable on the source at the moment the split begins, which is sub-second-resolved by the saga's own throughput envelope (F-055 acceptance benchmark: 7,031–7,936 sagas/s × per-saga latency = active-saga floor of ~10 sagas in flight).

**Surface and metric.** Internal-only change. New internal grain method `IBPlusLeafGrain.GetPendingMutationsForSlotsAsync(ImmutableHashSet<int>, int, CancellationToken)` returning `IReadOnlyList<PendingMutationSnapshot>` (alias `ol.pms`, fields `TransactionId`, `Key`, `Value`, `Timestamp`, `IsTombstone`, `ExpiresAtTicks`, `OriginClusterId`, `WalOffset`). New phase-entry hook on `TreeShardSplitGrain.BeginShadowWriteAsync` that fans out across source leaves, batches the snapshots, and replays into the destination via existing primitives. One new counter `orleans.lattice.split.retroactive_forward.entries` tagged `tree, shard` so operators size the sweep against ordinary shadow-forward traffic; one new histogram `orleans.lattice.split.retroactive_forward.duration` tagged the same way so the split coordinator's dwell time inside the new sweep phase is observable. The two existing acceptance fixtures for F-019a and F-019b (`ChaosReshardIntegrationTests`, `ChaosResizeIntegrationTests`) continue to assert the broader topology-change invariants; F-059 adds a targeted fixture for the atomic-visibility-across-split slice.

**Replication interaction.** Receiver-side leaves observe the retroactive sweep through the same WAL the source-side replay writes to - the sweep is just a sequence of ordinary prepared-mutation appends from the receiver's perspective, indistinguishable from a saga that prepared after the split began. No new wire surface, no new replication-package code, no per-tree opt-in.

**Acceptance.**
- A fixture commits a 16-key saga across a 4→8 reshard where every key's slot migrates and prepares land on the source before the split begins. A continuous reader polls every key at 10 ms cadence throughout the split, the drain, and the saga's terminal broadcast. Across 50 trials the reader observes either zero or all keys at every poll, with no `pre=N, post=M` split (the `ReshardTopologyTests` classifier returns `clean` for every trial).
- A targeted fixture exercises the retroactive sweep in isolation: a saga prepares on shard 0, a split commits migrating the saga's keys to shard 1, and an explicit `IBPlusLeafGrain.GetPendingMutationsForSlotsAsync` poll on shard 0's leaves immediately after `BeginShadowWriteAsync` returns yields the saga's prepared entries; an immediate `BPlusLeafGrain.PendingTx.Snapshot` poll on shard 1 confirms the entries have been mirrored under the same `(txid, key)` keying.
- A crash-recovery fixture starts a split, kills the silo hosting `TreeShardSplitGrain` mid-sweep, and on reactivation confirms the sweep re-runs to completion and the destination's pending-tx state converges to the source's (the LWW-idempotence assertion).
- The chaos suite's atomic-visibility pass rate on the 4→8 reshard fixture (the surface that exposed the `pre=N, post=M` pattern) is targeted to return to 100 % across the existing 20-trial sample. **At F-059 ship time the pass rate had not measurably improved over the pre-F-059 baseline** (~50 %): F-059 closes the source-side prepared-pending mirroring window and the follow-up late-pickup fetch-loop on AtomicWriteGrain.BroadcastTerminalsAsync closes the saga's own orphan window, but a residual race surface independent of both fixes persists. The residual surface is tracked as FX-018.
- A throughput regression fixture confirms `TreeShardSplitGrain`'s end-to-end split duration grows by no more than the observable cost of the sweep (bounded by active-saga concurrency at split-begin × per-key replay cost), so the addition does not unacceptably extend the split window.

**Shipped.** F-059 was delivered on the `feature/f059-retroactive-pending-tx-sweep` branch. Implementation:

- `src/lattice/TypeAliases.cs` - new `PendingMutationSnapshot = "ol.pms"` alias constant.
- `src/lattice/BPlusTree/PendingMutationSnapshot.cs` - new `internal readonly record struct` DTO with `[Id]` slots for `TransactionId`, `Key`, `Value`, `Timestamp`, `IsTombstone`, `ExpiresAtTicks`, `OriginClusterId`, `VectorClock`, `WalOffset`. Tombstone snapshots null out `Value` so the destination's `DeleteAsync` path is taken on replay.
- `src/lattice/BPlusTree/IBPlusLeafGrain.cs` - new `GetPendingMutationsForSlotsAsync(int[] sortedMovedSlots, int virtualShardCount)` method on the internal grain interface, returning `List<PendingMutationSnapshot>`. The leaf filters `_pendingTx` by `ShardMap.GetVirtualSlot` + `Array.BinarySearch` over the sorted moved-slot set, surfaces per-saga `_pendingTxOffsets` as `WalOffset` when available, and preserves every metadata field verbatim. Steady-state fast path returns an empty list when no pending bucket exists or the moved-slots array is empty, so the call is free on leaves not participating in any saga.
- `src/lattice/BPlusTree/Grains/BPlusLeafGrain.PendingTx.cs` - leaf-side implementation at the new `GetPendingMutationsForSlotsAsync` method, with `ArgumentNullException.ThrowIfNull(sortedMovedSlots)` and `ArgumentOutOfRangeException` guards.
- `src/lattice/LatticeMetrics.cs` - two new instruments: `SplitRetroactiveForwardEntries` (`Counter<long>`, name `orleans.lattice.split.retroactive_forward.entries`) and `SplitRetroactiveForwardDuration` (`Histogram<double>` ms, name `orleans.lattice.split.retroactive_forward.duration`). Both tagged `tree` + `shard`.
- `src/lattice/BPlusTree/Grains/TreeShardSplitGrain.cs` - `RetroactiveSweepPreparedMutationsAsync` walks the source shard's leaf chain via `GetLeftmostLeafIdAsync` and `GetNextSiblingAsync`, snapshots each leaf's prepared mutations filtered to the moved slots, and replays each snapshot through the destination shard via `ReplayPreparedSnapshotAsync`. The replay opens nested `LatticeTransactionContext.Set` + `LatticePreparedContext.BeginScope` + `LatticeOriginContext.With` + `LatticeVectorClockContext.With` + `LatticeHlcOverrideContext.With` scopes so the destination leaf re-stamps the same `(txid, key, hlc, origin, vc, expiresAtTicks)` tuple at its HLC-tick site that the source leaf observed at prepare time. Tombstones route via `target.DeleteAsync`; live values route via `target.SetAsync(key, value, expiresAtTicks)` when `ExpiresAtTicks > 0` and `target.SetAsync(key, value)` otherwise. The sweep is wired into the `BeginShadowWrite` phase entry from both `InitiateSplitStateAsync` (line 189) and the crash-recovery resume branch in `RunSplitPassAsync` (line 225), so a coordinator that crashes mid-sweep re-runs it on reactivation - LWW-idempotent by `(txid, key)` so the re-run is a no-op for entries already received by the destination. Metrics emit unconditionally in `finally`.
- `test/lattice/BPlusTree/Grains/BPlusLeafGrainTests.PendingMutationSnapshot.cs` - 10 unit tests covering: empty-pending fast path, empty-moved-slots fast path, slot-inclusion, slot-exclusion, multi-txid attribution, metadata preservation (origin / timestamp), TTL preservation, tombstone preservation, null-array guard, non-positive-virtualShardCount guard.

The end-to-end `ReshardTopologyTests` chaos surface is covered by the existing in-tree fixture; the retroactive sweep itself is a structural shape change behind the existing split state machine.


### 11 · F-060 ✓ shipped
**Reliability / medium impact (closes the residual foreground-commit sites that bypass the WAL; follow-on to F-058)** *(depends on Core F-058 ✓)*

Route the remaining foreground leaf-write sites in `BPlusLeafGrain.MergeEntriesAsync`, `BPlusLeafGrain.MergeManyAsync`, and `BPlusLeafGrain.CompactTombstonesAsync` through `ICommitLogWriter` instead of through standalone `state.WriteStateAsync()` (via `PersistAsync()`). F-058 retired the cross-migration LWW backstop's legacy state-row commit but left an analogous architectural-debt gap on three sibling code paths: every merge of received entries (replication apply, sibling redistribute, split-recovery rebroadcast) and every tombstone-compaction pass mutates `Entries` directly and commits via the Orleans persisted state row, with no WAL append. A leaf reactivation that replays the WAL alone (the activation-time WAL materialiser established under F-049c) silently drops the merged or compacted state, because the projection checkpoint that captured the mutation has not yet flushed and the WAL holds no record of it.

**Design.** Each in-scope site emits a synthetic `LatticeMutation` envelope through `ICommitLogWriter.AppendAsync` before applying the in-memory mutation, mirroring the pattern F-058 established for the backstop. `MergeEntriesAsync` appends one `MutationKind.Set` envelope per incoming entry, tagged with a new `IsMerge` slot (default `false`) so the metric and observer paths can distinguish replication-apply / sibling-redistribute traffic from ordinary `Set` traffic without introducing a new `MutationKind`. `CompactTombstonesAsync` appends one `MutationKind.Tombstone` envelope per reaped tombstone - the envelope is semantically a tombstone-of-tombstone (the durable record that this tombstone is now gone), which the activation-time replayer interprets as "if a Tombstone-of-tombstone is observed for a key whose live entry is also a tombstone older than the grace cutoff, drop the entry". The two foreground-commit `PersistAsync` calls in `MergeManyAsync` (post-`MergeIntoState` persists at the split-recovery branch and the main path) become redundant once the per-entry WAL append covers the durability boundary; `MergeIntoState` is rewritten to drive each `StoreEntry` through the WAL-append path so the foreground-commit semantics collapse into the same envelope-per-key shape as `SetCoreAsync`.

**Out of scope.** The topology-only `PersistAsync` site in `BPlusLeafGrain.Split.cs` (split-intent persist of `SplitState` / `SplitKey` / `SplitSiblingId` / `NextSibling` / `OldNextSibling`) and the second `PersistAsync` site in `MergeManyAsync` at the split-recovery branch (post-`CompleteSplitAsync` flush of split-state transitions) remain on the state-row path by design - both persist non-WAL-replayable topology metadata that the per-shard WAL does not carry, and the projection-checkpoint flush covers them on the durability boundary together with sibling pointers, tree id, shard index, and key range. The projection-checkpoint flush sites (the pending-checkpoint advance and the idempotent re-assert) and the `OnDeactivateAsync` flush also remain - they are the canonical Pebble/RocksDB-style memtable-flush pattern and are not foreground commits.

**Replication interaction.** Receiver-side leaves observe the new merge / compaction envelopes through the same WAL the source-side writes to - the envelopes are ordinary `MutationKind.Set` / `MutationKind.Tombstone` records from the receiver's perspective, indistinguishable from a saga that committed the equivalent operations directly. The `IsMerge` flag (and the existing `IsBackstop` flag) exist purely as ship-side debugging / metric tags; the receiver's apply path treats all three flag combinations as ordinary writes. The replication WAL GC predicate (R-083) treats merge and compaction envelopes identically to ordinary `Set` / `Tombstone` entries for retention purposes.

**Surface and observability.** No public method signature changes. Internal additions: one new `[Id(N)] bool IsMerge` slot on `LatticeMutation` (strictly-additive, wire-compatible - slot index assigned at implementation time per the existing slot allocation); two new tag values `kind=merge` and `kind=compact` on the existing `orleans.lattice.leaf.write.duration` histogram (so operators size merge / compaction traffic against ordinary writes without new instruments). The hygiene regression that today pins the backstop-WAL invariant is broadened to assert the only `state.WriteStateAsync(` call sites in `BPlusLeafGrain` and its partial files are (a) the projection-checkpoint flush, (b) the coalesced checkpoint, (c) the `OnDeactivateAsync` flush, (d) the topology-only split-intent persist in `BPlusLeafGrain.Split.cs`, and (e) the topology-only split-recovery flush in `MergeManyAsync`. The broadened regression (`Foreground_commit_paths_route_through_WAL`) reflection-asserts that no `MergeEntriesAsync` / `MergeManyAsync` (entry-mutation branches) / `CompactTombstonesAsync` body contains a `state.WriteStateAsync` site.

**Acceptance.**
- A crash-recovery fixture commits a `MergeEntriesAsync` batch, simulates a silo crash before any projection checkpoint fires, deactivates and reactivates the leaf, and confirms every merged entry is visible after the activation-time WAL replay path returns.
- An analogous crash-recovery fixture commits a `CompactTombstonesAsync` pass that reaps two tombstones, crashes before the checkpoint, and confirms the reactivated leaf observes the reaped state (no tombstones resurrected via a stale state-row replay).
- A targeted fixture exercises `MergeManyAsync` under a concurrent in-progress split: the merge interleaves with the split-recovery `CompleteSplitAsync` and the per-entry WAL appends fire after the topology-only flush returns. The reactivated leaf observes both the split-state transition and every merged entry.
- The `BPlusLeafGrainTests.Compaction` and `BPlusLeafGrainTests.Merge` partials' regression tests are converted to assert `FakeCommitLogWriter.AppendCount` rather than `FakePersistentState.WriteCount` for the foreground-commit branches; the topology-only branches continue to assert `WriteCount` because the topology-flush path is preserved by design.
- The broadened `Foreground_commit_paths_route_through_WAL` hygiene gate runs as part of `Phase 6b` and asserts the only `state.WriteStateAsync(` call sites in `BPlusLeafGrain*.cs` are the five expected sites enumerated above.
- The chaos suite (`ReshardTopologyTests`, `ChaosReshardIntegrationTests`, `ChaosResizeIntegrationTests`) continues to pass at the same rate as the F-058 baseline, with the WAL-routed merge / compaction paths observably exercising the same crash-recovery contract as every other foreground commit path under chaos.


### 12 · F-061 ✅ shipped
**API / reliability / medium impact (opt-in idempotency-key surface + caller-controlled retry policy for transient storage faults on the public `ILattice` mutating methods)** *(depends on Core F-036 ✓, F-049 ✓)*

Today every public mutating method on `ILattice` (`SetAsync`, `RemoveAsync`, `SetManyAsync`, `SetManyAtomicAsync`, `IncrementAsync`, `DeleteRangeAsync`, `MergeAsync`, ...) follows the contract "on transient storage failure, throw to the caller and revert in-memory state to match disk". That contract is correct - cycles 1-30 of the bug-hunter sweep landed Class B revert-on-throw fixes across `BPlusLeafGrain`, `BPlusInternalGrain`, `ShardRootGrain`, `AtomicWriteGrain`, `WalShardGrain`, `BootstrapCoordinatorGrain`, and `ReplicationMaintenanceGrain`, so the in-memory / on-disk invariant holds at the activation boundary. What it does **not** do is give the caller a *safe* retry path: a naive caller-side `try / catch / retry` re-enters `SetAsync` and mints a fresh `HybridLogicalClock.Now()` for the retry, producing a second WAL entry, a second replication shipment, a second `IMutationObserver.OnMutation` callback, and (for `IncrementAsync` on a PnCounter) a real semantic double-count. F-061 closes that gap by exposing an explicit idempotency-key surface so a retried call replays under the same logical timestamp and the existing LWW / HWM / WAL-append dedup collapses it to a single observable mutation.

The shape is deliberately caller-controlled, not ambient. Silent retry inside the library compounds tail latency (a single retry can triple p99), amplifies storage load at precisely the moment the storage is degraded ("retry storm"), and destroys the "did the first storage attempt succeed?" signal that operators alert on. F-061 puts the retry decision at the caller boundary - the library defaults to today's throw-and-revert behaviour and the policy is opt-in per tree.

**Design.** Two strictly-additive layers, independent of each other:

1. **Per-call idempotency key**, defaulted such that existing callers see no behaviour change. New `LatticeIdempotencyKey` (readonly record struct, alias `ol.idk`, `[Id(0)] HybridLogicalClock Timestamp`) carried via an optional ambient `LatticeIdempotencyContext` scope (mirrors the existing `LatticeOriginContext` / `LatticeVectorClockContext` / `LatticeHlcOverrideContext` pattern, so no public `ILattice` signature changes). When the scope is set, every mutating grain method reads the supplied `Timestamp` instead of issuing a fresh `HybridLogicalClock.Tick()`; the authoring cluster id continues to flow exclusively through `LatticeOriginContext` / `ILatticeOriginClusterIdResolver` so origin provenance stays infrastructure-owned. When the scope is null, today's fresh-Tick behaviour is preserved bit-for-bit. Replay collapses naturally: the WAL append path is keyed `(treeId, hlc, originClusterId)` for HWM dedup (replication R-023 + R-080); a retry that hits the same `(hlc, origin)` is a no-op append. The LWW merge path is already idempotent on identical `(Timestamp, OriginClusterId)`. PnCounter is the one CRDT whose foreground-write surface needs an explicit dedup - F-061 adds an `(originClusterId, hlc)`-keyed guard inside `PnCounterAccessor.IncrementAsync` so a retried increment with the same key drops before the per-replica counter advances.

2. **Optional `ILatticeRetryPolicy`** resolved via DI and wired in through a new `LatticeOptions.RetryPolicy` slot. The shipped default `BoundedExponentialRetryPolicy` (hand-rolled, no third-party dep) takes `(maxAttempts, initialDelay, maxDelay, retryableExceptionClassifier)` and re-runs the caller's lambda under the *same* `LatticeIdempotencyKey` for the budget's duration; on exhaustion it surfaces the *original* failure verbatim. The policy is **not registered by default** - hosts must opt in via `siloBuilder.ConfigureLattice(opts => opts.RetryPolicy = new BoundedExponentialRetryPolicy(...))` - so the ambient-library default remains "throw and let the caller decide". Hosts that want to mint their own keys and drive retry from their own infrastructure (Polly, ASP.NET Core's `ResiliencePipeline`, a custom orchestrator) consume layer 1 alone; hosts that want a turnkey experience consume layer 2, which sits on top of layer 1.

**Surface.** Public-additive only:
* `Orleans.Lattice.LatticeIdempotencyKey` - readonly record struct.
* `Orleans.Lattice.LatticeIdempotencyContext` - static AsyncLocal scope helper, `Current` / `Push(LatticeIdempotencyKey)` pattern (mirrors `LatticeOriginContext`).
* `Orleans.Lattice.ILatticeRetryPolicy` interface.
* `Orleans.Lattice.BoundedExponentialRetryPolicy` default impl.
* `LatticeOptions.RetryPolicy { get; set; } = null;` - opt-in, default null.
* `ISiloBuilder.AddLatticeRetryPolicy(Action<BoundedExponentialRetryPolicyOptions>)` convenience DI extension.

**Replication interaction.** None at the wire level - the replication apply path already dedups by `(originClusterId, hlc)` (R-023 / R-080), so a retried foreground write that re-uses the same key produces a WAL entry the receiver dedups verbatim. The replication library already covers the equivalent retry / flow-control concerns on its own pipeline through **R-060** (poison-entry DLQ + `MaxApplyRetries`), **R-062** (receiver-side flow control via `SuggestedBatchSize` / `PauseForMs`), **R-064** (per-peer observability), and **R-068** (apply-duration histogram), so no companion replication roadmap entry is needed - operators who want a retry surface on the replication apply path use the existing `ILatticeReplicationDeadLetters.ReplayAsync` flow.

**Out of scope.**
* **Internal silent retry inside grain methods.** Explicitly *not* added - the throw-and-revert contract is the load-bearing invariant Class B has been hardening for 30 cycles; an ambient internal retry would amplify latency on transient storage degradation and destroy the failure signal operators alert on. The boundary for retry is the public-API entry point, not the grain method.
* **Saga retry under in-flight `BeginAsync` / `CommitAsync`.** The `AtomicWriteGrain` saga is already idempotent on the same `transactionId` (the TxRegistry is the deduplication point); F-061 does not add a second retry layer on top. A caller that wants saga retry mints a fresh `transactionId` for each saga and uses the idempotency-key surface on individual `SetAsync` / `RemoveAsync` calls inside the saga's prepared phase.
* **Per-method retry policy override.** Out of scope for v1; the tree-wide `LatticeOptions.RetryPolicy` covers the canonical case. A future entry can add per-call policy overrides if a real consumer needs it.
* **Retry across activations.** A retry budget that outlives a deactivation is not supported - the key + budget live in the caller's `LatticeIdempotencyContext` scope; if the activation deactivates between attempts, the next attempt reactivates and replays under the same key (correct), but cross-activation budget tracking is the caller's responsibility.

**Acceptance.**
- A fault-injection fixture configures `FakePersistentState.ThrowOnWrite` to throw on the first N attempts and succeed on the (N+1)-th; a caller-side `SetAsync` under `RetryPolicy` with budget > N collapses to exactly one observable mutation (one `IMutationObserver.OnMutation`, one WAL append, one persisted state-row write, one replication shipment).
- A negative-control fixture exercises the same scenario *without* an idempotency key (or with a fresh key per retry) and confirms the test observes N+1 distinct WAL appends and N+1 observer callbacks - proving the idempotency key is the load-bearing component, not retry alone.
- A `PnCounterAccessor.IncrementAsync` fixture issues two retries with the same idempotency key and confirms the per-replica counter advanced by exactly one increment; a counterpart fixture issues two retries with distinct keys and confirms the counter advanced by two.
- A latency-amplification regression fixture confirms `ILattice` calls with `RetryPolicy = null` (the default) have the same p50 / p95 latency as v3.4.0 - the opt-in nature of the policy is observable as zero ambient cost.
- A bug-hunter Class B regression sweep confirms every existing revert-on-throw test continues to pass - F-061's retry surface sits **above** the grain's revert contract, not in place of it; the grain still throws on a failed `WriteStateAsync`, and the policy is the only thing that catches and retries.
- A docs snippet under `docs/lattice/retry-policy.md` (new, with `csharp verify` fences) walks the opt-in flow, the idempotency-key lifetime contract, and the operational guidance "retry belongs at the boundary; never enable it ambiently in the library".



### 13 · F-062 ✅ shipped
**Reliability / high impact (closes the residual cross-cluster atomic-visibility window left by F-055 + F-059 under multi-shard saga replication)**

Two independent receiver-side correctness gaps in the universal-saga design caused a continuous remote reader, polling at 10 ms cadence during cross-cluster replication of a multi-shard `SetManyAtomicAsync`, to intermittently observe a partial subset of the saga's keys at the new value while the remaining keys still read pre-saga. F-055 + F-057 + F-058 + F-059 closed the equivalent local-cluster window, but the cross-cluster path inherited two latent issues that the local path did not exercise.

**Two bugs fixed.**

- *Receiver had no all-or-nothing gate across per-source-shard terminal arrivals.* The producer side of a multi-shard saga emits one `TxCommit` / `TxAbort` WAL record per source shard the saga touched, and each one ships independently through the change feed. The pre-F-062 receiver applied the per-tree `ITxRegistryGrain` decision mark on the *first* incoming terminal and immediately fanned the terminal out to every receiver leaf bound to that source shard. Because the source's per-shard terminal records ship in parallel under independent backpressure / batching cadences, the receiver could observe a strict subset of the saga's terminals (e.g. shard-0's terminal arrived but shard-1's was still in flight) and flip the registry mark for the whole saga while only a subset of leaves had drained their pending bucket. A reader concurrent with this window saw post-saga values on the drained-shard keys and pre-saga values on the still-pending-shard keys - the exact `pre=N, post=M` visibility split F-055 claims to make impossible.
- *Receiver-side batch classifier routed prepared LWW writes through the batched merge path.* `ReplicationApplier.ApplyBatchAsync` classifies inbound `WalRecord` entries into a fast-path batched LWW merge (`ApplyMergeManyAsync`) and a per-entry fallback (`ApplyPointAsync`). The pre-F-062 classifier predicate did not consult `WalRecord.IsPrepared`, so prepared saga writes were eligible for the batched path and committed directly into the receiver leaf's visible `Entries` rather than into the per-leaf pending-tx bucket. The saga's terminal mark on the receiver then found no matching pending entries to flip, so the cross-cluster reader saw the prepared write as visible *before* the registry gate ever flipped - a strict-isolation violation that depended only on whether the per-origin run happened to be batched (steady-state load) or single-entry (cold start).

**Design.** Two strictly-additive primitives, independent of each other:

1. **Producer-stamped touched-shard count on terminal records, gated against per-source-shard arrival tally on the receiver.** New `[Id(19)] int AtomicShardCount` slot on `LatticeMutation` and the equivalent slot on `WalRecord` (strictly-additive, wire-compatible - legacy producers decode the missing slot to `0` which the receiver interprets as "no gate; preserve legacy first-terminal-wins semantics"). The producer-side `AtomicWriteGrain.MarkOneShardAsync` opens a `LatticeAtomicShardCountContext.With(state.State.TouchedShards.Count)` scope around each per-shard terminal append; `ShardRootGrain.AppendTxTerminalAsync` reads the ambient and stamps it onto the terminal mutation. The receiver-side `TxRegistryGrain.RecordTerminalArrivalAsync(txid, sourceShardIndex, committed, atomicShardCount)` deduplicates source-shard arrivals into the new `TxRegistryState.TerminalArrivals: Dictionary<Guid, HashSet<int>>` slot, adopts `max(seen, incoming)` into `TxRegistryState.ExpectedTerminals: Dictionary<Guid, int>` so a producer-side mid-saga shadow-forward split that grows the touched-shard set between successive per-shard terminals is absorbed without ever under-counting, and returns a `TerminalTallyResult` with `IsFinal` plus the union `ObservedSourceShards`. `LatticeGrain.ApplyTxTerminalAsync` consults the tally first: a non-final tally leaves the per-tree linearization mark unset and the receiver leaves' pending buckets undrained so reads remain all-or-nothing; only on the final arrival does the receiver mark the registry's decision and pre-fan the terminal-mark closure across every observed source-shard's transitive split-forward closure in a single parallel hop. The gate handles producer/receiver shard-count divergence naturally because the tally key is the producer-stamped source-side shard index, not the receiver's shard layout.

2. **Prepared-entry exclusion from the batched LWW receiver path.** The `batchable` classifier predicate in `ReplicationApplier.ApplyBatchAsync` is tightened to `... && !entry.IsPrepared`: a prepared `Set` or `Delete` is forced onto the per-entry `ApplyPointAsync` path, where `ApplyPreparedSetAsync` / `ApplyPreparedDeleteAsync` route the write into the destination leaf's `_pendingTx[txid]` bucket. Unprepared writes continue to consume the batched merge path. The classifier change is a one-line guard with three regression tests pinning the routing contract (prepared-set, prepared-delete, mixed prepared / unprepared in a single inbound run).

**Crash recovery and wire-compatibility.** The `TerminalArrivals` and `ExpectedTerminals` slots are persisted on `TxRegistryState`; both are cleared by `ForgetAsync` alongside the decision entry so the persisted footprint stays bounded by in-flight + recently-completed sagas. A legacy persisted state with no `Id(3)` / `Id(4)` slot decodes to empty dictionaries, which is the correct semantic default ("no terminals tallied yet"). A receiver that observes a fully-tallied saga and later receives a duplicate-delivery retry of one of its terminals is a safe no-op (the registry's decision-repeat path is idempotent). A receiver running a pre-F-062 producer-side cluster sees `atomicShardCount == 0` on every terminal arrival, which the gate treats as "no expected-total information" and falls back to the legacy "mark on first terminal" semantics - so the wire is forward- and backward-compatible across mixed-version deployments.

**Bonus: leaf-cache delivery decoupled from LWW HLC ordering.** A third receiver-side stale-cache bug, surfaced during F-062 chaos validation but distinct from the two above, was closed in the same branch and is described here because it is the same shape of problem (LWW comparator dropping legitimate cross-cluster traffic on a receiver). The pre-F-062 `LeafCacheGrain.RefreshAsync` filtered the leaf's `GetDeltaSinceAsync` delta by the cache's accumulated `VersionVector`, comparing each incoming `LwwValue<byte[]>.Timestamp` against the cache's recorded per-replica HLC. Under cross-cluster atomic apply the leaf preserves the *source* cluster's HLC verbatim on every committed entry (required for LWW convergence across clusters with skewed wall clocks). Whenever the source HLC was below the destination leaf's already-published `Version[ReplicaId]`, the cache's HLC-based filter silently dropped the entry from the delta and continued serving the stale local value indefinitely. The fix is a new internal `LeafDeliveryCursor` value type (alias `ol.ldc`, fields `Epoch`, `Sequence`) plus an `IBPlusLeafGrain.GetDeltaSinceCursorAsync(LeafDeliveryCursor)` seam that returns full snapshots on epoch mismatch (fresh cache or stale leaf activation) and per-key sequence-based deltas otherwise. The cursor is activation-scoped in-memory state - bumped once per `StoreEntry` / `RemoveEntry` regardless of LWW HLC - so the cache pulls every write strictly newer than its last delivered sequence even when the underlying HLC has rewound. A leaf re-activation produces a fresh epoch, every cache holding a stale cursor falls back to a full-snapshot delivery on its next refresh, and the WAL replay path remains the sole projection source-of-truth. This keeps the cursor free of any per-write durable I/O.

**Surface and observability.** No public method signature changes. Internal additions: `[Id(19)] LatticeMutation.AtomicShardCount`; the matching `[Id(19)] WalRecord.AtomicShardCount`; `LatticeAtomicShardCountContext` ambient request-context helper (internal `Orleans.Lattice`); `TxRegistryGrain.RecordTerminalArrivalAsync(Guid, int, bool, int)` + `TerminalTallyResult` (alias `ol.ttr`) on the internal `ITxRegistryGrain` contract; `TxRegistryState.TerminalArrivals` / `ExpectedTerminals` slots; `LeafDeliveryCursor` (alias `ol.ldc`) + `StateDelta.DeliveryCursor` + `IBPlusLeafGrain.GetDeltaSinceCursorAsync`. The cross-cluster gate emits no new instruments; the existing replication-apply metrics (`orleans.lattice.replication.apply_duration`, the per-mutation counters) cover the new path because every terminal arrival routes through the same `ApplyAsync` -> `ApplyTxTerminalAsync` seam as before.

**Replication interaction.** Universal - the gate sits entirely on the receiver and consumes the producer-stamped `AtomicShardCount` slot the producer always emits. No transport-level changes, no new replication-apply DTOs, no per-tree opt-in. Receiver-side leaves observe the cross-cluster gate identically to the local-cluster strict-isolation path: a pending bucket dial-back through `ITxRegistryGrain.GetStatusAsync` returns `InFlight` for the whole saga until the tally is final, then flips to the recorded outcome in a single per-tree linearization point.

**Acceptance.**
- A two-cluster integration fixture commits a 16-key `SetManyAtomicAsync` saga whose touched-shard set spans `>= 2` source shards while a continuous reader on the remote cluster polls every key at 10 ms cadence. Across 10 trials the reader observes either zero or all keys at every poll, never a `pre=N, post=M` split - the universal-visibility invariant F-055 claims, now extended to the cross-cluster path. The full non-chaos replication suite (1341 tests across `Orleans.Lattice.Replication.Tests`) is green after the fix; the equivalent core suite (2611 tests across `Orleans.Lattice.Tests`) is unaffected.
- Three regression tests in `ReplicationApplierTests.Batch` pin the prepared-entry routing contract: `ApplyBatchAsync_routes_prepared_set_through_prepared_apply_seam_not_batched_merge`, `ApplyBatchAsync_routes_prepared_delete_through_prepared_apply_seam_not_batched_merge`, and `ApplyBatchAsync_mixed_prepared_and_unprepared_entries_route_to_correct_seams`. Each fails without the `!entry.IsPrepared` guard and passes with it.
- Seven `TxRegistryGrainTests.TerminalArrival` tests cover final-arrival gating, dedupe / idempotence on a per-`(txid, sourceShardIndex)` basis, `max(seen, incoming)` adoption for shadow-forward shard growth, mixed-outcome protection (commit terminal observed after an abort terminal preserves the abort), and the legacy `atomicShardCount == 0` path (every arrival is final, matching pre-F-062 behaviour).
- Four `LeafCacheGrainTests.CrossClusterLwwDelivery` tests verify stale-HLC cross-cluster delivery, overwrite-by-newer-cursor behaviour, first-contact epoch mismatch (forces full snapshot), and no re-delivery when the sequence is unchanged.
- A public-API contract test in `PublicReplicationApiContractTests.AtomicSaga` exercises the universal cross-cluster atomic invariant against the real public replication pipeline, with the saga's WAL records flowing through the canonical encode -> transport -> decode -> apply chain.

**Shipped.** Implementation:

- `src/lattice/LatticeMutation.cs` - new `[Id(19)] AtomicShardCount` slot.
- `src/lattice/BPlusTree/Grains/WalRecord.cs` - matching `[Id(19)] AtomicShardCount` slot.
- `src/lattice/BPlusTree/Grains/WalRecordConverter.cs` - propagate the slot through the mutation -> WAL conversion path.
- `src/lattice.replication/ReplicationMutationObserver.cs` - pass through `mutation.AtomicShardCount` into the emitted WAL record.
- `src/lattice/LatticeAtomicShardCountContext.cs` - new internal ambient request-context helper (`Current`, `With(int?)`).
- `src/lattice/BPlusTree/Grains/AtomicWriteGrain.cs` - wrap each per-shard terminal append in `LatticeAtomicShardCountContext.With(state.State.TouchedShards.Count)`; the value is read fresh on every call so late-pass shards observe the post-union count.
- `src/lattice/BPlusTree/Grains/ShardRootGrain.TxTerminal.cs` - stamp `LatticeMutation.AtomicShardCount = LatticeAtomicShardCountContext.Current ?? 0` on the assembled terminal mutation.
- `src/lattice/BPlusTree/State/TxRegistryState.cs` - new `[Id(3)] TerminalArrivals` and `[Id(4)] ExpectedTerminals` slots; cleared by `ForgetAsync`.
- `src/lattice/BPlusTree/ITxRegistryGrain.cs` + `src/lattice/BPlusTree/Grains/TxRegistryGrain.cs` - new `RecordTerminalArrivalAsync(Guid, int, bool, int)` returning `TerminalTallyResult` (alias `ol.ttr`). The non-final path uses `Array.Empty<int>()` so the common case allocates nothing; the final path materialises and sorts the union set once.
- `src/lattice/BPlusTree/Grains/LatticeGrain.ReplicationApply.cs` - `ApplyTxTerminalAsync` consults the tally first, holds back the registry mark + leaf fan-out until the tally is final, then resolves the transitive split-forward closure across the full `ObservedSourceShards` set in a single parallel hop.
- `src/lattice.replication/ReplicationApplier.cs` - pass `entry.AtomicShardCount` into `ApplyTxTerminalAsync`.
- `src/lattice.replication/ReplicationApplier.Batch.cs` - `batchable` predicate tightened with `&& !entry.IsPrepared` so prepared entries route to `ApplyPointAsync` (and thence to `ApplyPreparedSetAsync` / `ApplyPreparedDeleteAsync`).
- `src/lattice/Primitives/LeafDeliveryCursor.cs` - new internal `readonly record struct` (alias `ol.ldc`).
- `src/lattice/Primitives/StateDelta.cs` - new `DeliveryCursor` slot.
- `src/lattice/BPlusTree/IBPlusLeafGrain.cs` + `src/lattice/BPlusTree/Grains/BPlusLeafGrain.DeliveryCursor.cs` - new `GetDeltaSinceCursorAsync(LeafDeliveryCursor)` seam on the internal leaf interface; partial-class implementation maintains the per-activation epoch and per-key sequence map.
- `src/lattice/BPlusTree/Grains/BPlusLeafGrain.Digest.cs` - `StoreEntry` and `RemoveEntry` call `BumpDeliverySequenceFor(key)` after mutating `Entries` so cursor progress stays in lock-step with projection changes.
- `src/lattice/BPlusTree/Grains/LeafCacheGrain.cs` - `RefreshAsync` switches from `GetDeltaSinceAsync(_version)` to `GetDeltaSinceCursorAsync(_deliveryCursor)`, clears `_cache` on epoch flip, and adopts the leaf's cursor on every successful refresh.
- `src/lattice/TypeAliases.cs` - new `LeafDeliveryCursor = "ol.ldc"` and `TerminalTallyResult = "ol.ttr"` aliases.
- Tests: `ReplicationApplierTests.Batch.cs` (3 routing regressions), `TxRegistryGrainTests.TerminalArrival.cs` (7 gating regressions), `LeafCacheGrainTests.CrossClusterLwwDelivery.cs` (4 cursor delivery regressions), `PublicApiContract/PublicReplicationApiContractTests.AtomicSaga.cs` (cross-cluster atomicity contract).

The atomic-visibility behaviour observable to a remote reader now matches the local-cluster strict-isolation contract exactly; the cross-cluster carve-out F-055 left implicit (and F-059 partially closed on the local path) is now closed end-to-end.


### 14 · F-063 ✅ shipped
**Observability / low impact (instrument the per-write parent-digest publish hop on the leaf commit path so operators can attribute commit-pipeline latency to all four constituent stages)** *(depends on Core F-029 ✓, F-049 ✓)*

The leaf's `BPlusLeafGrain.CommitSetAsync` / `CommitDeleteAsync` / `DeleteRangeAsync` foreground write paths each end with an awaited cross-grain RPC to the parent internal node (`IBPlusInternalGrain.OnChildDigestPublishedAsync`) via `PublishDigestUpwardAsync()`. This hop runs on every write that mutates the running projection hash (i.e. effectively every Set / Delete / range Delete after the dirty-flag short-circuit gate at `BPlusLeafGrain.DigestPublication.cs:130`), but the existing `LatticeMetrics.LeafCommitDuration` histogram only attributes latency to three labels (`step=wal`, `step=apply`, `step=observer`). Under steady-state write load at the calibrated operating point, the digest hop is therefore the only unlabelled contributor to the full per-call wall-time (`orleans.lattice.leaf.commit.duration` aggregate vs. the labelled sum) and cannot be empirically separated from the labelled stages without this instrument.

**Change.** A strictly-additive fourth `step="digest"` label is recorded on the existing `LatticeMetrics.LeafCommitDuration` histogram. No new instrument, no new instrument name, no new tag schema - the existing `RecordCommitStep(string step, long startTicks)` helper at `src/lattice/BPlusTree/Grains/BPlusLeafGrain.Metrics.cs:150` is reused verbatim from the three foreground call sites:

- `BPlusLeafGrain.CommitSetAsync` (single-key foreground set).
- `BPlusLeafGrain.CommitDeleteAsync` (single-key foreground delete).
- `BPlusLeafGrain.DeleteRangeAsync` (per-leaf range-tombstone publish; one digest publish per range, matching the existing one-per-range WAL append and apply steps).

**Out of scope.** Seven cold / structural / maintenance digest publishers do **not** record a `step="digest"` measurement (none of them emit the existing `step=wal` / `apply` / `observer` labels either, so the histogram remains scoped to the per-write commit pipeline): `BPlusLeafGrain.Split.cs:175` (post-split topology publish, one-shot per split), `BPlusLeafGrain.Projection.cs:192` / `:203` (post-checkpoint-flush publish, runs on the timer cadence rather than per write), `BPlusLeafGrain.PendingTx.cs:1250` (saga terminal publish, one per transaction), and the three bulk / maintenance publishes inside `BPlusLeafGrain.cs` itself (`CompactTombstonesAsync` post-reap publish at line 1399, `MergeEntriesAsync` cross-shard-migration publish at line 1623, `MergeManyAsync` bulk-merge publish at line 2040). Recording these alongside the per-write hot path would skew the histogram with one-shot structural latencies that are not part of the per-write pipeline and would confound the very attribution the new label is added to enable.

**Acceptance.**
- A `BPlusLeafGrainTests.CommitStepDigest` partial attaches a `MeterListener` to `LatticeMetrics.LeafCommitDuration` and confirms that `SetAsync` / `DeleteAsync` against a leaf with a registered parent each emit exactly one `step="digest"` measurement with the expected tree tag.
- A `DeleteRangeAsync` test confirms a single `step="digest"` measurement per call (matching the one-shot per-range publish, not one per matched key).
- A negative-control test confirms that calling `PublishDigestUpwardAsync` from the cold structural paths does **not** record a `step="digest"` measurement, preserving the histogram's hot-path attribution.
- The `LatticeMetricsIntegrationTests` suite gains an end-to-end check that a `SetAsync` against the cluster fixture produces at least one `step="digest"` measurement on the `orleans.lattice.leaf.commit.duration` histogram alongside the existing `wal` / `apply` / `observer` measurements.
- `docs/lattice/metrics.md` row 60 (instrument definition) and row 29 (`step` tag enumeration) list `digest` alongside `wal` / `apply` / `observer`.


---

## 🛠 Audit Follow-up Fixes

Reliability and hygiene fixes surfaced by the repository audit. Items tagged **Fix** here are distinct from feature work (`F-###`) - they address latent bugs or robustness gaps rather than adding new capability. The highest-severity audit items (data-loss / liveness) are already addressed: F-031 (atomic multi-key writes saga) and F-033 (stateful cursor grain) are both complete.
PR #47 landed the first batch of audit fixes (HLC merge clock advancement, compaction short-circuit, alias / physical-shard resolution in `TombstoneCompactionGrain` and `TreeMergeGrain`, streaming per-leaf merge, and `HotShardMonitor` cooldown pruning) with 12 regression tests. A second batch closed the remaining reliability and hygiene items below (FX-001, FX-002, FX-003, FX-004, FX-005, FX-007, FX-008, FX-009, FX-011, FX-012). **v2.0.0** delivered the final two breaking-change items bundled together: grain-manifest identities are now anchored to stable `[Alias]` constants on every grain interface, and FX-006 adds `CancellationToken` to every public `ILattice` method. FX-010 (Roslyn snippet compilation harness for `docs/*.md`) landed as the closing audit item - every `FX-###` fix is now resolved.

### ✅ FX-001 - Leaf split publish ordering *(audit #8 - reliability / medium)*
Split now flushes the trimmed source leaf **before** publishing the new sibling to the parent, so a crash between the two writes can never leave duplicated keys in the tree. Regression test: `BPlusLeafGrainTests.Split_flushes_source_state_before_returning_SplitResult` asserts the source's persisted `Entries` contain no key ≥ the promoted split key and that `SplitState == SplitComplete` / `NextSibling == result.NewSiblingId` before `SplitResult` is returned.

### ✅ FX-002 - Reminder re-registration idempotency *(audit #9 - reliability / medium)*
`TombstoneCompactionGrain`, `TreeMergeGrain`, and `HotShardMonitorGrain` now compare `TickStatus.Period` to the configured option on every reminder firing and call `RegisterOrUpdateReminder` when they drift. Regression tests: `TombstoneCompactionGrainTests.ReceiveReminder_reregisters_when_period_drifts_from_options` and `ReceiveReminder_does_not_reregister_when_period_matches`.

### ✅ FX-003 - `VersionVector` pruning *(audit #14 - reliability / medium, memory)*
`VersionVector.PruneOlderThan(long minRetainedUtcTicks)` removes entries whose HLC wall-clock ticks fall below the cutoff. Gated behind `LatticeOptions.VersionVectorRetention` (default `Timeout.InfiniteTimeSpan` - opt-in, wire-compatible). Regression tests: `VersionVectorTests.PruneOlderThan_removes_entries_below_cutoff`, `_returns_zero_on_empty_vector`, `_keeps_entries_at_cutoff`, `_drops_all_when_cutoff_high`.

### ✅ FX-004 - `HotShardMonitor` sampling window survives silo restart *(audit #11 - reliability / medium)*
`HotShardMonitorGrain` now persists `HotShardMonitorState.ActivationUtc` (alias `ol.hms`) via `IPersistentState<HotShardMonitorState>` and only writes it on first activation; the grace window is computed from the persisted time so it survives silo restart. Regression test: `HotShardMonitorGrainTests.ActivationUtc_persists_and_survives_reactivation`.

### ✅ FX-005 - `TreeMergeGrain` crash-resume retry semantics *(audit #13 - reliability / medium)*
`ProcessCurrentShardAsync` now pre-increments `TreeMergeState.ShardRetries` and persists the increment **before** attempting the merge, so a non-throwing crash still burns retry budget on reactivation. When `ShardRetries` reaches `MaxRetriesPerShard` (2) the shard is poisoned (skipped, cursor advances, retry counter resets) with a warning log. Regression tests: `TreeMergeGrainTests.ProcessNextShardAsync_poisons_shard_after_retry_budget_exhausted` and `ProcessNextShardAsync_preincrements_retry_counter_before_attempt`.

### ✅ FX-006 - `CancellationToken` on `ILattice` *(audit #19 - API / breaking - delivered in v2.0.0)*
Every public `ILattice` method now accepts an optional `CancellationToken cancellationToken = default`, including scans (`KeysAsync`, `EntriesAsync`), range deletes, counts, fan-outs, cursors, bulk load, and all tree-lifecycle operations. `LatticeGrain` calls `cancellationToken.ThrowIfCancellationRequested()` at method entry, inside retry `when` filters, and at fan-out / pagination checkpoints, so a pre-cancelled token fails fast without contacting shards. The scan iterators apply `[EnumeratorCancellation]` at the implementation so `await foreach (...).WithCancellation(ct)` propagates into the orchestrator. Cooperative cancellation is scoped to the `LatticeGrain` orchestrator - downstream `IShardRootGrain` / `IBPlusLeafGrain` / coordinator grain interfaces are deliberately unchanged (their work is short-lived; propagation would balloon the breaking surface without observable benefit). The typed `ILattice<T>` extension overloads and both streaming `BulkLoadAsync` overloads in `LatticeExtensions` / `TypedLatticeExtensions` also thread the token. Binary-breaking (signatures changed) even though source-compatible for default-token callers; shipped in **v2.0.0**. Regression tests: `CancellationTokenIntegrationTests` (16 tests) - pre-cancellation fail-fast for every public method plus a mid-stream `KeysAsync_stops_mid_stream_when_token_cancelled` scenario.

### ✅ FX-007 - Logger category consistency *(audit #20 - hygiene / low)*
`TtlGrain` was generalised to `TtlGrain<TSelf>` exposing a `protected ILogger<TSelf> Logger`; `AtomicWriteGrain` and `LatticeCursorGrain` now share the typed-category pattern used by every other grain. Regression test: `AuditHygieneRegressionTests.Every_grain_uses_generic_ILogger_category` reflection sweep fails the build if any grain re-introduces a non-generic `ILogger` ctor parameter or field.

### ✅ FX-008 - Metrics naming prefix *(audit #21 - hygiene / low, blocks F-029)*
`LatticeMetrics.MeterName = "orleans.lattice"` pins the meter namespace ahead of F-029 locking public instrument names. Regression test: `AuditHygieneRegressionTests.LatticeMetrics_meter_name_is_orleans_lattice`.

### ✅ FX-009 - `TypeAliases` dead-entry audit *(audit #23 - hygiene / low)*
`TypeAliasesTests.Every_alias_constant_is_referenced_by_exactly_one_type` is a reflection sweep that fails the build on dead or orphan alias constants. Runs on every PR.

### ✅ FX-010 - Docs drift guard *(audit #24 - docs / low)*
`DocsSnippetCompilationTests` in `test/lattice/Docs/` compiles every opt-in `csharp verify`-fenced snippet under `docs/` against the current `Orleans.Lattice` public surface via Roslyn (`Microsoft.CodeAnalysis.CSharp` 5.0.0). Each snippet is treated as a method body and wrapped with ambient parameters (`IGrainFactory grainFactory`, `IClusterClient client`, `ISiloBuilder siloBuilder`, `ILattice tree`, `CancellationToken cancellationToken`) plus an ambient `record User(string Name, int Age)` so typed-helper examples compile without ceremony. References are sourced from the test host's `AppContext.BaseDirectory` so every Orleans and `Microsoft.Extensions.*` assembly is visible without explicit wiring. Opt-in by fence marker keeps the scope tight - authors annotate the snippets that are meant to be executable; docs that illustrate API signatures, tables, or pseudo-code are left alone. Seeded with two snippets (typed `SetAsync<T>` / `GetAsync<T>` in `docs/api.md`, `siloBuilder.ConfigureLattice(...)` in `docs/tree-deletion.md`); renames to `ILattice`, `TypedLatticeExtensions`, or `LatticeOptions` surfaces that break either snippet now fail CI. The full non-chaos suite (1086 tests) remains green.

### ✅ FX-011 - `ShardRootGrain.DeleteRangeAsync` under-deletion on sparse multi-shard trees *(discovered during F-033 split-test authoring - reliability / high, data-loss)*
`IBPlusLeafGrain.DeleteRangeAsync` now returns `RangeDeleteResult { Deleted, PastRange }` (alias `ol.rdr`); the leaf sets `PastRange = true` as soon as it encounters a key ≥ `endExclusive`. `ShardRootGrain.DeleteRangeAsync` terminates on `result.PastRange` instead of the old `deleted == 0` short-circuit, so multi-shard trees with sparse key distribution delete every matching key. Regression tests: `MultiShardDeleteRangeRegressionTests.DeleteRange_walks_past_empty_leaves_on_multi_shard_tree` (200 keys / 4 shards / `MaxLeafKeys=4`) and `RangeDeleteResult_reports_past_range_flag`.

### ✅ FX-012 - `CountAsync` over-counts during concurrent shard splits *(discovered by chaos test - reliability / high, correctness)*
Fixed in two passes. **v1** added a shard-map version stability check after the `CountWithMovedAwayAsync` fan-out but still relied on each source shard filtering moved-slot keys via its persisted `SplitInProgress.Phase`. That left a race window: the split coordinator publishes the new `ShardMap` (including the target shard) before advancing the source's phase to `Reject`, so a count arriving in the window saw slot-S keys on both the source (not yet filtering) and the target (already drained) while the map version was already at its new value - the stability check passed and the over-count was returned. **v2** (this fix) routes counts through the authoritative `ShardMap` directly: `LatticeGrain.CountAsyncCore` and `CountPerShardAsyncCore` partition virtual slots by current owner via `BuildOwnedSlotMap(map)` and ask each physical shard to count only its owned slots via `IShardRootGrain.CountForSlotsAsync(sortedSlots, virtualShardCount)`. Every virtual slot is counted exactly once, against whichever shard the map identifies as its owner, independent of the source shard's per-split phase. The version stability check is retained to guard against splits committing mid-fan-out and is bounded by `LatticeOptions.MaxScanRetries`. Regression tests: `CountAsyncSplitRegressionTests.CountAsync_matches_true_live_count_after_split`, `CountPerShardAsync_matches_true_live_count_after_split`, `CountAsync_never_overcounts_during_concurrent_split`, `CountPerShardAsync_never_overcounts_during_concurrent_split` (the last two spin the public counter while driving `SplitAsync` + `RunSplitPassAsync` so every observation inside the phase-machine window is checked), plus the `Chaos_concurrent_reads_writes_scans_counts_and_splits_preserve_invariants` and `CountAsync_returns_exact_count_during_active_split` integration tests that caught the v1 gap in CI.

### ✅ FX-013 - Observe shadow-forward tasks in `ShardRootGrain` mutation paths *(reliability / low - follow-up from F-019 review)*
`ShardRootGrain` now routes every `ForwardShadowAsync` dispatch through a new `TrackShadowForward` helper (partial file `ShardRootGrain.ShadowForwardTracking.cs`) that attaches an `OnlyOnFaulted` continuation accessing `Task.Exception` - so a retry-loop iteration that abandons its forward task can never fault silently via `TaskScheduler.UnobservedTaskException`. `SetManyAsync` was reshaped from `try { local } finally { await forwardTask; }` to a local-exception-preserving pattern using `ExceptionDispatchInfo`, so a local failure is no longer masked by a concurrent forward failure. `MergeManyAsync`'s grouped path now awaits `forwardTask` at the end (previously it fell off the end of the method, leaving the forward unobserved by the caller). `ShardRootGrain` gains an `ILogger<ShardRootGrain>` primary-constructor parameter; the fault-logger continuation emits one warning per abandoned faulted forward. No public API change. Regression tests: `ShardRootGrainFx013Tests` - `SetManyAsync_surfaces_local_exception_when_both_local_and_forward_fail`, `SetManyAsync_surfaces_forward_exception_when_only_forward_fails`, `SetAsync_happy_path_awaits_forward_before_returning`, `TrackShadowForward_logs_warning_when_abandoned_forward_faults`.

### ✅ FX-014 - Rejecting precedence over IsDeleted on shard preamble *(reliability / medium, found by chaos test)*
After an online resize commits, the OLD physical tree's shards transition Reject (sets ShadowForward.Phase = Rejecting) → Cleanup (calls ITreeDeletionGrain.DeleteTreeAsync, which sets IsDeleted = true). A stale in-flight scan iterator that was routed against the old physical tree before the alias swap now correctly surfaces as StaleTreeRoutingException so the calling LatticeGrain refreshes its alias and retries against the new physical tree, rather than as the terminal InvalidOperationException("This tree has been deleted and is no longer accessible."). Fix: swapped the order of ThrowIfTreeRejecting() and ThrowIfDeleted() in ShardRootGrain.PrepareForOperationAsync. A user-initiated DeleteTreeAsync never touches ShadowForward, so for that case ThrowIfTreeRejecting is a no-op and ThrowIfDeleted still fires correctly. Surfaced by Chaos_resize_under_concurrent_load_preserves_all_data in CI: a scanner concurrent with a fast resize-driver loop landed its next page after the source shard's Cleanup phase committed, and the deleted-flavoured exception escaped both LatticeGrain.KeysAsyncCore's StaleTreeRoutingException retry and ScanKeysAsync's EnumerationAbortedException catch. Regression tests: 4 ShardRootGrainShadowForwardTests partials (Get_throws_StaleTreeRoutingException_when_rejecting_and_deleted, Set_throws_StaleTreeRoutingException_when_rejecting_and_deleted, Get_throws_InvalidOperationException_when_deleted_without_rejecting, Set_throws_InvalidOperationException_when_deleted_without_rejecting).

### ✅ FX-017 - Bound saga-terminal RPC chain depth under cascading mid-saga splits *(reliability / high, found by `SetManyAtomicAsync` ~30s timeout in F-059 validation)*
Under a 4 to 8 reshard whose adaptive per-shard splits cascade (the original 4 shards split, then the 8 destinations themselves split mid-saga as the post-split key distribution triggers further hot-spot splits - observed terminal physical-shard count of 11+ in the chaos fixture), the saga-terminal broadcast was exposed to an **unbounded RPC chain depth on a single shard's turn**. The old code (`ForwardSplitTerminalAsync` in `ShardRootGrain.TxTerminal.cs`) was recursive: when a shard root received `AppendTxTerminalAsync` and observed an in-flight split (`state.SplitInProgress`) or any `MovedAwaySlots` entries, it re-issued `AppendTxTerminalAsync` against each split-destination shard, which in turn re-issued against ITS destinations, and so on. Each hop ran inside the receiving shard's request turn, so the wall-clock depth was N x per-hop RPC time. Deep multi-hop reshard chains (N >= 5 on the chaos fixture) compounded into Orleans' default 30s response timeout, surfacing as `SetManyAtomicAsync` hanging ~30s and the saga failing with `TimeoutException` instead of completing. **Fix.** Removed `ForwardSplitTerminalAsync` from `ShardRootGrain.TxTerminal.cs` entirely. Added internal `IShardRootGrain.GetSplitForwardTargetsAsync` which returns the union of `state.SplitInProgress.ShadowTargetShardIndex` and the distinct values in `state.MovedAwaySlots` (excluding `self`) - a pure, side-effect-free reporter of this shard's split-destination set. New internal `TerminalFanOutResolver.ResolveTransitiveAsync` (`src/lattice/BPlusTree/TerminalFanOutResolver.cs`) BFS-expands a seed shard set through repeated `GetSplitForwardTargetsAsync` wavefronts (parallel `Task.WhenAll` per wavefront, `HashSet<int>` as the cycle guard) and returns the transitive closure sorted ascending. The saga (`AtomicWriteGrain.BroadcastTerminalsAsync`) and the cross-cluster replication-apply path (`LatticeGrain.ApplyTxTerminalAsync`) now call the resolver to pre-resolve the closure of `TouchedShards`, then fan terminals out flat (parallel per-shard `AppendTxTerminalAsync`). Recursive depth N collapses to a single-hop parallel fan-out at the caller. Internal-only change - no public API surface, no wire-format change, no new metrics. Regression tests: `TerminalFanOutResolverTests` (12 tests covering empty seed, single shard, linear chain, fan-out, cycle, deep cascades, null guards, cancellation, parallel wavefront, sorted-output, and large-closure correctness); `ShardRootGrainSplitShadowForwardTests` rewritten (13 tests) to target `GetSplitForwardTargetsAsync` directly. The pre-existing chaos atomic-visibility flakiness on `ReshardTopologyTests.Continuous_reader_observes_zero_or_all_keys_through_mid_saga_reshard` (~50% pass rate on the branch base) is independent of this fix and persists at the same rate after the fix - tracked separately as a future follow-up; this fix changes the failure mode from "test hangs 30s on Orleans timeout" to "test fails in 1s on the atomic-visibility assertion" by removing the timeout path.

### ✅ FX-018 - Close residual orphan-pending-tx visibility-race on `ReshardTopologyTests` chaos surface *(reliability / medium, found by F-059 chaos validation)*
**Shipped.** Diagnostic instrumentation during F-059 chaos validation traced the residual `pre=N, post=M` visibility split on `ReshardTopologyTests.Continuous_reader_observes_zero_or_all_keys_through_mid_saga_reshard` to an orphaned `_pendingTx` bucket on a destination shard combined with a missing entry in the per-tree `ITxRegistryGrain` Decisions map. Sequence: (1) saga `MarkCommittedAsync` records the decision; (2) saga `BroadcastTerminalsAsync` snapshots `Participants` and fans out terminals, including the late-pickup fetch-loop; (3) saga `CompleteSagaAsync` calls `ForgetAsync` which deletes the decision from the registry; (4) concurrent `TreeShardSplitGrain.RetroactiveSweepPreparedMutationsAsync` finally completes `ReplayPreparedSnapshotAsync` on a destination shard the saga never reached, installing `_pendingTx[txid][key] = v` and re-registering the destination via `RecordAffectedLeafIfPreparedAsync`. Because step 3 already wiped `Decisions[txid]`, the sweep's post-sweep cleanup pass cannot resolve the saga's outcome -- it reads `TxStatus.InFlight` and leaves the orphan bucket pending, so a reader routed to that destination hides the key indefinitely (or until the next saga shadows it). **Fix.** `ITxRegistryGrain.ForgetAsync` now tombstones the decision with a TTL via a new `TxRegistryState.ForgottenAt: Dictionary<Guid, DateTimeOffset>` slot (`Id(2)`, wire-compatible default = empty) instead of removing it immediately. The retention window is `LatticeOptions.TxDecisionRetention` (default 60 seconds; `TimeSpan.Zero` restores legacy `Decisions.Remove` semantic). Tombstoned decisions remain queryable from `GetStatusAsync` / `GetStatusManyAsync` / `SnapshotAsync` for the retention window, so the sweep's post-sweep cleanup can dial back to the registry and apply the terminal directly to drain the orphan. The decision is physically purged by an inline `PruneExpired` pass folded into the next `ForgetAsync` call, so no separate timer is required. `MarkCommittedAsync` / `MarkAbortedAsync` clear the tombstone on a previously-forgotten txid (preserving the `Mark_then_Forget_then_Mark` semantic). `TxRegistryGrain` gains `IOptionsMonitor<LatticeOptions>` injection and an internal `TimeProvider` slot (`TimeProvider.System` default; test-substitutable). Regression tests: 10 new unit tests on `TxRegistryGrainTests` covering within-retention queryability, expired-tombstone masking on per-txid and batched APIs, inline-prune on the next-saga path, tombstone-stamp idempotency, mark-clears-tombstone, snapshot filtering on expired/active tombstones, and the `TxDecisionRetention=Zero` legacy path. Public surface change: new `LatticeOptions.TxDecisionRetention` property with `DefaultTxDecisionRetention = 60s` constant. No `TxStatus` enum change (the leaf-side `Committed` / `Aborted` interpretation becomes correct automatically once the registry returns the tombstoned outcome). Wire-format: additive only -- legacy persisted state decodes the missing `Id(2)` slot to an empty dictionary, which is the correct semantic default ("no tombstones yet"). End-to-end impact: the previously orphaned destination-shard reader path now resolves to the saga's committed outcome and surfaces the post-saga value. A 20-trial chaos sample on `ReshardTopologyTests.Continuous_reader_observes_zero_or_all_keys_through_mid_saga_reshard` after the fix shows 14/20 = 70% pass vs the 8/20 = 40% baseline measured after the late-pickup fetch-loop alone (and vs the ~50% pre-fix baseline). The improvement is substantial and the tombstone path closes the dominant orphan-resolution gap, the residual failure surface (~30% of trials) was subsequently traced to a shadow-forward race during the source shard's `ShardSplitPhase` Reject and post-Complete transitions and fixed by **FX-019** in the same branch (the multi-round saga overlap hypothesis was falsified by FX-019's diagnostic instrumentation -- 15 chaos trials showed no orphan outlived the 60s tombstone window). Implementation files: `src/lattice/BPlusTree/LatticeOptions.cs`, `src/lattice/BPlusTree/State/TxRegistryState.cs`, `src/lattice/BPlusTree/Grains/TxRegistryGrain.cs`, `src/lattice/BPlusTree/ITxRegistryGrain.cs`, `test/lattice/BPlusTree/Grains/TxRegistryGrainTests.cs`.

### ✅ FX-019 - Shadow-forward race during ShardSplitPhase Reject and post-Complete transitions *(reliability / medium, found by FX-018 chaos validation residual)*
**Shipped.** After FX-018 closed the orphan-pending-tx visibility race the chaos suite still showed ~30% failure rate on `ReshardTopologyTests.Continuous_reader_observes_zero_or_all_keys_through_mid_saga_reshard`. Diagnostic instrumentation (Console.WriteLine `[FX019-DIAG]` markers at the ShardRoot `SetAsync` / `AppendTxTerminalAsync` and Leaf `SetCoreAsync` / `ApplyTxTerminalAsync` entry points + the shadow-forward early-return branches) ran across 5 instrumented chaos trials. The smoking gun: in 3 of 5 failing trials the visibility-split key showed exactly one `ShadowFwd ... early=phase-Reject` log entry on the source shard for the failing key and round; in the 4th failing trial the symmetric `early=no-sip` log entry appeared 266 times. The pre-fix `ShardRootGrain.ForwardLocalWriteToShadowIfNeededAsync` admitted only `ShardSplitPhase.BeginShadowWrite` / `Drain` / `Swap` -- a write that passed `ThrowIfRejectedForKey` at phase ≤ Swap could land its prepared value in the leaf's `_pendingTx`, then race the coordinator advancing the phase to `Reject` (or all the way to `Complete`, clearing `SplitInProgress` and populating `MovedAwaySlots`) before the shadow-forward call observed the state. The forward early-returned in both cases, leaving the write stranded on the source while the `ShardMap` (already swapped at Swap entry) routed reads to the destination -- a mid-saga visibility split. **Fix.** `ForwardLocalWriteToShadowIfNeededAsync` now (a) admits `ShardSplitPhase.Reject` in the active-split branch so an in-flight write that races the Swap → Reject transition still forwards to the destination, and (b) adds a post-Complete fallback: when `SplitInProgress` is null but `ShardRootState.MovedAwaySlots` records the key's slot under `MovedAwayVirtualShardCount`, forward to the recorded post-split owner. The fallback mirrors the pattern already used by `GetSplitForwardTargetsAsync` for the saga's terminal fan-out, closing the prepare-side gap symmetrically. Both the prepared (`SetAsync` to preserve `_pendingTx` semantics) and non-prepared (`MergeManyAsync` to preserve source HLC) branches are preserved verbatim; only the target-resolution gate is widened. The self-forward guard (`newOwner != MyShardIndex`) prevents a corrupt `MovedAwaySlots` entry from looping back onto the same activation. Internal-only change -- no public API surface, no wire-format change, no new metrics. Regression tests: 5 new `ShardRootGrainSplitShadowForwardTests` cases race-simulating both transitions by mutating `state.State.SplitInProgress` from inside the leaf `SetAsync` NSubstitute callback -- which fires between `ThrowIfRejectedForKey` (already passed) and the shadow-forward call (the helper under test). Cases cover: Swap → Reject mid-write forwards via `MergeManyAsync`; Swap → Reject mid-write under `LatticePreparedContext` forwards via `SetAsync` (not `MergeManyAsync`); Reject → Complete mid-write forwards via `MergeManyAsync` through `MovedAwaySlots`; Reject → Complete mid-write under `LatticePreparedContext` forwards via `SetAsync`; defensive guard against corrupt `MovedAwaySlots` recording `MyShardIndex` as the post-split owner. End-to-end impact: the unit-test surface conclusively proves both race windows now forward to the destination, but a 30-trial chaos sample on `ReshardTopologyTests.Continuous_reader_observes_zero_or_all_keys_through_mid_saga_reshard` after the fix showed 14/30 = 47% pass -- statistically indistinguishable from the FX-018-only 14/20 = 70% baseline (Wilson 95% CI overlap: 30-64% vs 47-86%). The fix closes the diagnostic-confirmed race (3 of 5 instrumented failing trials showed exactly one matching `early=phase-Reject` log entry; the 4th showed 266 `early=no-sip` post-Complete events) but does not dominate the residual failure surface. The chaos failures now show a recurring `pre=1, post=15 + unknown-round (other=1)` pattern across multiple rounds, suggesting an additional, orthogonal race (most likely terminal-delivery / read-side projection consistency, or a multi-round saga interaction) that remains for a separate follow-up. Implementation files: `src/lattice/BPlusTree/Grains/ShardRootGrain.Split.cs`, `test/lattice/BPlusTree/Grains/ShardRootGrainSplitShadowForwardTests.cs`.

### ✅ G-012 - `CoordinatorGrain<TSelf>` base class + `IsIdleAsync` rename *(refactor / hygiene - no behaviour change)*
The reminder-anchored work-pump pattern shared by `TreeSnapshotGrain`, `TreeResizeGrain`, `TreeShardSplitGrain`, and `TreeReshardGrain` is now factored into a new `internal abstract class CoordinatorGrain<TSelf>(IGrainContext, IReminderRegistry, ILogger<TSelf>) : IRemindable, IGrainBase`. Derived grains supply three abstracts (`KeepaliveReminderName`, `InProgress`, `ProcessNextPhaseAsync()`) and call two helpers (`StartCoordinatorAsync()` after persisting intent; `CompleteCoordinatorAsync()` after persisting terminal state); the base class owns the 1-minute keepalive reminder, the 2-second phase grain timer (idempotent re-arm via `??=`), the reminder handler that re-arms the timer on silo reactivation or unregisters when idle, and a defensive `UnregisterKeepaliveAsync` that swallows transient storage failures on shutdown. Per-tick exceptions are logged-and-swallowed at the base so a single failed tick does not stop the pump. The signature was narrowed from the original `CoordinatorGrain<TSelf, TState>` shape in the proposal to just `CoordinatorGrain<TSelf>` because derived grains already hold their own `IPersistentState<T>` and a generic `TState` parameter would force the base to mediate persistence without adding leverage. `TombstoneCompactionGrain` was out of scope - its reminder semantics are compaction-trigger rather than keepalive and would distort the base contract. The rename `IsCompleteAsync` → `IsIdleAsync` lands on the same four internal coordinator interfaces (`ITreeSnapshotGrain`, `ITreeResizeGrain`, `ITreeShardSplitGrain`, `ITreeReshardGrain`); `IAtomicWriteGrain` and `ITreeMergeGrain` retain `IsCompleteAsync` because their semantics genuinely are per-operation completion. Breaking change at the Orleans wire level on those four internal interfaces; all callsites in `LatticeGrain.BulkLoad` / `LatticeGrain.Reshard` / `HotShardMonitorGrain` and every test harness were updated. Net ~300 lines of duplicated boilerplate removed across four grains with zero behaviour change; no public API change. The full non-chaos suite (1086 tests) remains green.

---

## 🔍 Gaps & Potential Additions

The following areas are not currently on the roadmap but represent meaningful opportunities, particularly as the library moves toward production adoption.

### G-001 - Leaf-level write batching (investigate before committing)
Sharding and adaptive splitting (F-011) already distribute write load across leaf grains, progressively undermining the need for in-grain write coalescing. The changes required for safe batching - `[Reentrant]` leaf grains, a `FlushAsync` drain contract for split/merge/snapshot coordinators, and an explicit `WriteMode` API to surface the weakened durability guarantee - touch almost every major system component. The failure modes (silent data loss on crash, stale state seeded into a new shard post-split) are hard to detect in tests and severe in production. Before pursuing, profile under realistic write-skew conditions with adaptive splitting active and confirm that leaf-level hotness persists after splits have stabilized. If poor key entropy is the root cause, key-design guidance is a lower-risk remedy. Only implement if profiling proves the leaf grain itself is the bottleneck.

### G-002 - Compaction policy controls *(promoted to F-071)*
F-016 (TTL) assumes that "existing compaction infrastructure" handles expired-key reaping, but there was no roadmap item covering compaction itself as a configurable feature. Operators had no way to tune tombstone reaping thresholds, set compaction schedules, or observe space amplification (beyond the tombstone ratio in F-014). Compaction policy controls - minimum tombstone ratio before reaping, maximum leaf size before forced compaction, and a compaction telemetry hook for F-029 - now have a dedicated entry: see **F-071 - Compaction policy controls and telemetry**, which adds ratio- and size-based pre-emptive triggers, pass-level telemetry on the `orleans.lattice` meter, a per-leaf tombstone-ratio gauge, an operator-driven `CompactShardAsync` admin entry point, and a dedicated Compaction row on the Grafana Overview dashboard.

### G-003 - Per-key change subscriptions
F-015 targets tree-level events. Reactive patterns (e.g. cache invalidation, real-time leaderboard updates) need per-key or prefix-scoped subscriptions - closer to Redis `SUBSCRIBE` or Cosmos DB change feed. This could be layered on top of F-015 with a filter registry grain, but the routing and fan-out semantics need explicit design.

### G-004 - Value compression / encryption
There is no transparent per-tree compression or encryption of stored `byte[]` values. For storage-cost-sensitive workloads (large values, many tombstones) or compliance-sensitive data, a pluggable `IValueTransformer` pipeline applied before `WriteStateAsync` and after read would address both concerns without changing the public API surface.

### G-005 - Quota / admission control per tree
Multi-tenant deployments need a way to cap a single tree's live key count or total estimated storage size and reject writes once the cap is breached. Without this, a misbehaving tenant can starve storage for all other trees in the cluster. A `MaxLiveKeys` or `MaxEstimatedBytes` option on `LatticeOptions`, enforced at the shard level via the hotness counters already present in F-013, would be low-friction to implement.

### G-006 - Admin CLI / `dotnet` tool
All management operations (run diagnostics, inspect shard map, trigger snapshot) require in-process code or test harnesses. A `dotnet tool` wrapping the `ILatticeAdmin` surface (once F-014 lands) would lower the operational bar significantly - particularly for diagnosing concurrent split activity or stale cache behaviour described in F-022. Shard splits remain an autonomic concern - `ITreeShardSplitGrain` is guarded and not part of any planned public surface.

### G-007 - Shard-affine grain placement
Each `ShardRootGrain` is currently placed by Orleans' default random placement director. Because `LatticeGrain` is a `[StatelessWorker]` that activates on every silo, every shard call that lands on a silo that does not host the target shard's activation pays a cross-silo round-trip. A custom `IPlacementDirector` that deterministically maps each `ShardRootGrain` (by shard index) to a specific silo - stable hash of shard index across the current membership ring - would co-locate the router and the shard on the same silo for the majority of requests. Realising the full benefit also requires `LatticeGrain` to forward each fan-out leg to the shard's home silo rather than executing the grain call from the local activation. The benefit is **limited to point operations** (`GetAsync`, `SetAsync`, `DeleteAsync`, `SetIfVersionAsync`, etc.) - scans fan out to all shards by definition and pay cross-silo hops regardless of placement. The magnitude of the gain is storage-latency-dependent: against a slow backend (Azure Table Storage, Cosmos DB) the cross-silo hop is noise; against a fast local backend it can be a meaningful fraction of total round-trip time. F-029 telemetry should land first so profiling can confirm whether the hop is actually showing up before committing to the placement director design. The main implementation complication is adaptive splitting (F-011): a `ShardMap` swap changes which shard indices exist, so the placement director must either re-evaluate home-silo assignments after each swap or accept degraded affinity until shard grains naturally migrate. `[PreferLocal]` is not sufficient - it only biases initial activation and has no effect once a grain is already activated on another silo.

### G-008 - Cluster-wide split concurrency control
`MaxConcurrentAutoSplits` is enforced per-tree: because `HotShardMonitorGrain` is keyed by tree ID, each tree independently counts its own in-flight splits. In a cluster hosting many trees simultaneously, aggregate storage I/O from drain phases across all trees is unbounded - a large number of concurrent trees each with 2 in-flight splits can saturate the storage provider or silo thread pool just as effectively as a single tree with a high limit. A cluster-level admission gate (e.g. a singleton `IClusterSplitConcurrencyGrain` tracking cluster-wide in-flight count, checked by each `HotShardMonitorGrain` before triggering) would cap aggregate drain I/O independently of tree count. Low priority in single-tree or low-tree-count deployments; becomes relevant in multi-tenant or many-tree scenarios.

### G-009 - `AtomicWriteGrain` generalization to non-tree mutations
The `IAtomicWriteGrain` interface and `AtomicWriteGrain` implementation are currently tied to per-tree mutation sagas. Generalize to a lightweight `IAtomicActionGrain` that just executes a body of code atomically with respect to other grains, not necessarily tied to a tree or a saga. The caller supplies a delegate or expression tree; execution faults or retries surface as `OrleansException` or `TimeoutException` on the grain call. Supports low-infra-cost distributed locks (inherently FIFO by design) and fine-grained multi-key updates without overwriting or reading stale values. Built on lightweight grain interfaces, no new public surface on `ILattice`. Lowers the latency of F-035 (replication grain-side observer) and unblocks G-013 (observer latency telemetry).

### G-010 - Repository of point-in-time tree snapshots
Materialize and commit the in-memory tree state at a given UTC point as a persistent snapshot (folder with tree-id blob + one blob per shard). Snapshots are read-only and directly usable by `ILattice` readers (get/state/exist) at the snapshot UTC or later. New shadow-forwards and stale reads fail with `StaleTreeRoutingException` until the observation period expires (default 5 minutes); triggers an immediate `RecordSplitAsync` if a split is in progress. `TTL` and selective pruning (by shard or globally) via `ILattice.DeleteRangeAsync`, callers opt in to snapshotting via a `WithSnapshot` extension on `ILattice`, diagnostics via `SnapshotDiagnostics` in `ILatticeStats`.
`IGrainSnapshot` for low-cost materialization of arbitrary grain states (partial or full).

### G-011 - Configurable mutation semantics per tree
The current all-or-nothing mutation model (single `TreeId` + `OperationId` per `IAtomicWriteGrain`) limits flexibility. Introduce `IMutationBufferGrain` (backed by a new `MutationBufferGrain` implementation) that receives a batch of operations with per-key lock/retry/commit semantics - individual key mutations can succeed or fail independently. The buffer grain itself can be replicated or sharded as desired. Initial use case: replication staging area for F-035 observer payloads before fan-out to avoid overloading transient message queues with large bursts. Bonus: non-replicated grains (e.g. `TreeSnapshotGrain`) can use it to stage writes from in-memory to persistent without an intervening mutable grain activation.

### G-012 - Time-based lease grants for admin operations
Long-running admin operations (resize, snapshot, reconfig) risk conflicting with normal traffic (reads/writes/scans) or with themselves (concurrent admin operations) if retry loops do not observe `IsIdleAsync`. Augment `IClusterClient` with a `RequestLeaseAsync` / `ReleaseLeaseAsync` pair to control access to a named logical resource (tree id, operation id, etc.) across the cluster; the lease is granted to the first request and times out after a preset interval (default 5 minutes). Every admin operation acquires a lease at the start and releases it on completion or error, prolonging on retry. Lowers the PCI surface of F-027 (grouped merge routing) and F-031 (atomic writes) by moving the admission check to the client side.

### G-013 - Observer-latency telemetry
`IMutationObserver` (F-035) runs inline on the grain write path and is documented with prominent pitfall guidance, but operators have no direct visibility into how much latency each observer is actually contributing. Add an `orleans.lattice.observer.duration` histogram (milliseconds) to the `orleans.lattice` meter, tagged `observer=<typename>` and `tree=<treeId>`, timed inside `MutationObserverDispatcher.PublishAsync` around the per-observer `await`. Keep the existing "zero-cost when no observer is registered" guarantee by checking `HasObservers` before starting the stopwatch; the measurement callback is further elided by `Meter` when no listener is attached, so the instrument is free on the hot path for callers who do not opt in. Surfaces misbehaving observers (synchronous I/O, blocking calls) on the same OpenTelemetry pipeline as the traffic they slow down. Complements F-029's existing instrument catalog without changing its public surface. Implement alongside or after the first replication consumer (R-010) lands, so the instrument ships with a real-world validation workload.

### G-014 - WAL-as-sole-commit-point promotion (substantially shipped via F-047 → F-052) ✅
Tracked as concrete work under the **Replication enablers - WAL-as-commit-point promotion (v2 commit path)** sub-section above and substantially shipped in v3.4.0. Pre-merge delta capture landed as F-047, the leaf-projection rebuild seam as F-048, the WAL-as-sole-commit-point flip as F-049 (with the shadow-write rollback path retired by F-049d's default flip and removal in v3.4.0), the background materialiser as F-050, the materialiser-side offset-keyed HWM as F-051, and the operator surface (rebuild / digest / lag) as F-052. The shipped runtime model is documented in [`docs/lattice/wal.md`](../../docs/lattice/wal.md). The consumer-side concerns (auditing the change feed and HWM surfaces for "remote peer" assumptions, widening R-061 / R-083's GC predicates to include the local materialiser, suppressing the redundant WAL append in `ReplicationMutationObserver` when the commit-log adapter is the source) were folded into the replication package's initial release shape rather than tracked as a separate follow-on.

### G-015 - Structural ops as WAL-replayable deterministic log entries
F-049 (decision C from its design review) deliberately scopes the WAL-as-sole-commit-point flip to **leaf data only**: `BPlusLeafGrain` flips, while `BPlusInternalGrain`, `ShardRootGrain`, `LatticeRegistryGrain`, and the nine coordinator grains (`AtomicWriteGrain`, `LatticeCursorGrain`, `HotShardMonitorGrain`, `TombstoneCompactionGrain`, `TreeDeletionGrain`, `TreeMergeGrain`, `TreeReshardGrain`, `TreeResizeGrain`, `TreeShardSplitGrain`, `TreeSnapshotGrain`) keep their existing `IPersistentState<T>` commit path. The original F-048 acceptance criterion - "a manufactured leaf split is committed to the WAL and replayed across three silos with bit-identical post-state" - is **retracted** under that decision. G-015 captures the deferred work to encode structural rewrites (leaf split, internal-node rebalance, tombstone compaction, online resize, online reshard, shard-split shadow-forward) as deterministic `MutationCategory.Maintenance` WAL entries that every silo replays identically. Two prerequisites must land before G-015 becomes implementable: (1) every structural op must be **purely deterministic** (no `DateTime.UtcNow`, no random shard placement, no non-deterministic iteration order over `Dictionary<,>`); under decision C this is not enforced today because structural ops continue to commit through `WriteStateAsync()` and the silo whose foreground path runs the op publishes the post-rewrite state. (2) The `MutationKind` enum must grow at least three new shapes (`StructuralLeafSplit`, `StructuralInternalRewrite`, `StructuralCompaction`) plus a stable serialisation envelope for the per-shape payload (split key, sibling node id, parent linkage, etc.) - this is a wire-format change to `LatticeMutation` and would coordinate with the replication package's `LatticeMergeMode` enum (R-030 / R-032). The benefit of pursuing G-015 is symmetry between leaf data and structural metadata under WAL-as-truth: a silo that boots with a stale internal-tree state replays structural ops alongside data ops and converges. The cost is the wire-format growth and the determinism contract enforcement (likely a Roslyn analyser flagging non-deterministic primitives inside any method tagged `[StructuralOp]`). Defer until F-049 is in production for at least one minor version and the WAL-as-leaf-truth path has accumulated operational evidence - the decision to retract structural-op WAL replay was made on risk grounds, not value grounds, and G-015 should be revisited once the leaf-only flip has shipped without incident.

### G-016 - Operator dashboards package (`Orleans.Lattice.Dashboards`) ✅ Shipped
Operators today wire `.AddMeter("orleans.lattice")` into an OpenTelemetry pipeline and assemble Grafana panels by hand against the F-029 / F-049b / F-049c instrument catalog. That is friction at every new deployment and a drift surface - a panel hand-authored against a metric name that later gets renamed silently goes blank. Ship a new optional sibling package `Orleans.Lattice.Dashboards` at `src/lattice.dashboards/` (mirroring the `src/lattice.replication.grpc/` layout precedent) that bundles three Grafana JSON dashboards as embedded resources covering every instrument in the `orleans.lattice` meter (and, when the replication package is registered, every instrument in `orleans.lattice.replication`): an **Overview** dashboard (per-tree throughput, p50/p99 of `leaf.write.duration`, cache hit-rate, tombstone churn split by `reaped` vs `expired`, splits committed, atomic-write outcomes, coordinator completions, tree-lifecycle, event publish / drop, config changes); a **Commit Path** dashboard targeting the F-049 WAL-only commit path (`leaf.commit.duration` broken out by `step={wal|apply|observer}`, `leaf.replay.duration` tagged by recovery outcome, `leaf.replay.entries` split `applied|skipped`, projection-digest panels - the visual evidence harness for the F-049d default-flip-and-removal release and ongoing rebuild-on-activation health); and a **Replication** dashboard (ship / apply / lag durations, WAL append vs trim throughput, dead-letter queue churn, apply-FIFO violations, causal-wait histograms, fall-off-log events, peer cursor lag). Public surface: `LatticeDashboards.GetGrafanaDashboardJson(LatticeDashboardKind kind)` returning the embedded JSON as a string, plus a `LatticeDashboardKind { Overview, CommitPath, Replication }` enum. No grain, no DI registration, no runtime cost - the package is pure JSON-and-string-helper. The package ships with Grafana provisioning yaml templates (`Provisioning/datasources.yaml`, `Provisioning/dashboards.yaml`) so a host can drop the dashboards into a Grafana sidecar without hand-editing UIDs. **Drift guard.** A regression suite in `test/lattice.dashboards/` parses every embedded dashboard JSON, extracts every metric name referenced in panel `expr` strings (Prometheus PromQL), and asserts each name resolves to a live instrument on `LatticeMetrics.Meter` (and, for the Replication dashboard, `LatticeReplicationMetrics.Meter`) via a `MeterListener` snapshot - so a future rename in the core or replication package fails CI before the dashboard ships stale. Asymmetric coupling: `Orleans.Lattice.Dashboards` consumes the meter (hard reference), the meter does not depend on dashboards. v0.1.0 takes a direct project reference on `Orleans.Lattice.Replication` so the Replication dashboard always resolves; splitting that into an optional reference (so a local-only deployment can skip the replication meter entirely) is planned for a follow-up once both packages are published to NuGet. Strictly additive, non-breaking, optional package - users who want hand-rolled dashboards keep them.

---

## Dependencies

| Feature | Depends on | Status |
|---|---|---|
| F-011 (Adaptive shard splitting) | F-013 (Shard hotness counters) | ✅ Both complete |
| F-011 (Adaptive shard splitting) | F-028 (Shard map indirection) | ✅ Both complete |
| F-011 (Adaptive shard splitting) | F-030 (Route `BulkLoadAsync` through shard map) | ✅ Both complete |
| F-029 (External metrics / telemetry) | F-013 (Shard hotness counters) | ✅ Both complete |
| F-025 (Continuous merge) | F-020 (Merge trees) | ✅ Prereq complete - F-025 unblocked |
| F-027 (Leaf-grouped merge routing) | - | ✅ Complete (amortises batched merge at scale for F-025) |
| F-019 (Online resize) | F-028 (Shard map indirection) | ✅ Both complete |
| F-022 (Troubleshooting guide) | F-014 (Tree diagnostics) | ✅ Prereq complete - F-022 unblocked |
