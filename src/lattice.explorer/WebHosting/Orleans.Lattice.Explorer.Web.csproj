<Project Sdk="Microsoft.NET.Sdk.Razor">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Orleans.Lattice.Explorer.Web</RootNamespace>
    <AssemblyName>Orleans.Lattice.Explorer.Web</AssemblyName>
    <PackageId>Orleans.Lattice.Explorer.Web</PackageId>
    <Version>7.9.1</Version>
    <Description>Opt-in embeddable hosting library for the Orleans.Lattice Explorer Blazor Server web head. AddLatticeExplorerWeb(...) registers the Razor components, shared explorer UI, state-API connection seam, configuration and capability stores, the Backups and Access areas, and the cookie / data-protection auth plumbing; MapLatticeExplorer(...) maps the interactive server components, static assets, and server-side sign-in / sign-out endpoints under a configurable base path so a consumer can co-host the read-only explorer inside their own ASP.NET app.</Description>
    <PackageProjectUrl>https://github.com/NSTA1/Orleans.Lattice</PackageProjectUrl>
    <RepositoryUrl>https://github.com/NSTA1/Orleans.Lattice</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageReadmeFile>NUGET_README.md</PackageReadmeFile>
  </PropertyGroup>

  <ItemGroup>
    <None Include="NUGET_README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Orleans.Lattice.Explorer.Tests" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\UI\Orleans.Lattice.Explorer.UI.csproj" />
  </ItemGroup>

</Project>
