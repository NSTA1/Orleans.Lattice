# Operator-specific overrides: subscription ID, SSH key path, source IP, etc.
parameters.local.ps1
# Per-rung sweep results written by ladder.ps1. Local-only.
.ladder-results.csv
.ladder-phaseA.csv
# Per-probe sweep directories written by ladder.ps1 when copying a
# rung's CSVs and logs aside for a single scaling.md probe.
.*-sweep/